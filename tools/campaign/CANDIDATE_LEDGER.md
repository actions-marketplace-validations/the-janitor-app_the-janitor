# Candidate Ledger

Valid findings from `janitor hunt` campaigns with an estimated Approval % between
10 and 84. These entries are not submission-ready yet: they are missing a fully
autonomous payload, require manual verification, or expose a mathematical proof
gap that must be closed before promotion to the Bounty Ledger.

| Date | Target Repo | Vulnerability Class | Severity | Expected Payout | Approval % | Exact Repro Command | R&D Follow-Up |
|------|-------------|---------------------|----------|-----------------|------------|---------------------|---------------|
| 2026-05-26 | https://github.com/hashicorp/vault | security:ssrf_dynamic_url — `builtin/credential/cert/backend.go:188` — `http.Get(crl.CDP.Url)` fetches the CRL Distribution Point URL extracted from a user-presented certificate's extension. Attacker presents a cert from any CA trusted by Vault's cert auth backend with a crafted CDP URL pointing to internal services (KevCritical, `reachability_proof`) | P2/High | $500–$3,000 | 0% → LOW_YIELD | N/A — INVALIDATED 2026-05-29 | Sprint 185 source trace at HEAD: `crl.CDP.Url` is set ONLY by `pathCRLWrite` (admin API `vault write auth/cert/crls/<name> url=<URL>`). The URL is admin-configured, NOT extracted from the client certificate's X.509 CRLDistributionPoints extension. `http.Get(crl.CDP.Url)` fetches from admin-configured storage entries only. Gate 2 fails: requires admin privilege to configure the URL that gets fetched. No X.509 CRL extension extraction path exists in the codebase. SSRF premise was incorrect. Routed to LOW_YIELD. |
| 2026-05-22 | https://github.com/pinterest/querybook | security:oauth_missing_state_validation (`querybook/server/lib/dag_exporter/exporters/demo_dag_exporter.py`) — OAuth authorization code extracted without state parameter validation in DAG export OAuth connector (CWE-352, `lattice_gap_proposal`) | P3/High | NO PAYOUT | Deferred 2026-05-29 | querybook is NOT listed as in-scope on Pinterest Bugcrowd (`bugcrowd.com/engagements/pinterest`). No monetary submission path confirmed. Defer until scope is added or Pinterest launches a separate querybook disclosure program. | N/A — no payout path. |
| 2026-05-22 | https://github.com/pinterest/querybook | security:embedding_trust_transposition (`querybook/server/lib/vector_store/base_vector_store.py:88`) — production vector-store retrieval at line 88 reaches LLM invoke path without a source-trust filter (CWE-502-adjacent, `reachability_proof`) | P2/High | NO PAYOUT | Deferred 2026-05-29 | querybook is NOT listed as in-scope on Pinterest Bugcrowd. No monetary path. Defer. | N/A — no payout path. |
| 2026-05-21 | https://github.com/ClickHouse/ClickHouse | Sprint 159 server memory-safety batch — `security:lcm_double_free` ×2, `security:lcm_use_after_free` ×1, `security:lcm_malloc_integer_truncation` ×4, `security:lcm_off_by_one_loop` ×42, `security:parser_exhaustion_anomaly` ×7, residual `security:unsafe_string_function` outside the existing `printf.cpp` candidate | P3/Medium | $100–$600 | 15% | `cargo run -p cli -- hunt /tmp/clickhouse-s159 --concurrency 4 > /tmp/hunt_clickhouse_s159.json && jq -r '.[] \| select((.file // "") \| test("^(src|base|contrib)/")) \| select(.id \| test("lcm_|parser_exhaustion|unsafe_string_function")) \| [.id,.file,.line,.proof_class] \| @tsv' /tmp/hunt_clickhouse_s159.json \| head -80` | ClickHouse scope includes OSS `clickhouse-server`, but the program requires release-build reproduction without debug/sanitized-only behavior. Promotion requires selecting one production server path, building a supported release, and proving a public SQL/client input reaches a crash, memory-corruption, or parser-exhaustion witness. Existing `printf.cpp` and PRQL raw-pointer candidate rows remain authoritative and are not duplicated here. |
| 2026-05-20 | https://github.com/gravitational/teleport | security:protobuf_any_unguarded_decode × 28 sites — `api/types/role.go` (multiple), `api/types/wrappers/wrappers.go` — protobuf Any deserialization without type-URL allow-list check in core access-control type definitions (CWE-502) | P2/High | $500–$3,000 | 0% → LOW_YIELD | N/A — INVALIDATED 2026-05-29 | Sprint 185 manual verification: `grep -rn "UnmarshalAny\|ptypes\."` in `/tmp/teleport-s184/api/types/` returns zero results. Teleport has migrated away from deprecated `ptypes.UnmarshalAny` since Sprint 152. The protobuf Any decode sites have been refactored to `anypb.UnmarshalTo` with typed destination structs or removed entirely. Finding is no longer valid at HEAD. Routed to LOW_YIELD. |
| 2026-05-20 | https://github.com/gravitational/teleport | security:ffi_unsafe_deref_unguarded × 2 sites — `lib/srv/desktop/rdp/rdpclient/src/lib.rs` (RDP client Rust FFI layer) — raw pointer dereference without null guard in RDP session handling (CWE-476) | P2/High | $500–$2,500 | 25% | `grep -n 'from_raw_parts\|unsafe.*deref\|from_ptr\|extern.*C' /tmp/teleport-s152/lib/srv/desktop/rdp/rdpclient/src/lib.rs \| head -10` — confirm unguarded unsafe dereference in production RDP code | Sprint 152 hunt: `reachability_proof` on 2 Rust FFI sites in RDP client. Proof gap: prove an attacker-controlled RDP server can send malformed data that triggers the unsafe deref path — requires RDP protocol-level fuzzing of the rdpclient decoder. |
| 2026-05-20 | https://github.com/hashicorp/vault | security:protobuf_any_unguarded_decode × 43 sites — `vault/identity_store.go` (×6 `ptypes.UnmarshalAny` confirmed `reachability_proof` on re-hunt) — deprecated protobuf Any deserialization on the identity ingestion path without type-URL validation (CWE-502) | P2/High | $500–$5,000 | 50% | `grep -n 'ptypes.UnmarshalAny\|anypb.UnmarshalTo' /tmp/vault-s152/vault/identity_store.go` — confirm unguarded Any decode sites on identity entity/alias/group paths; test with a crafted entity storage entry containing malicious `@type` URL | **Sprint 153 re-hunt**: `classify_protobuf_any_proof` shipped; `vault/identity_store.go` lines 1172,1188,1194,1271,1289 all emit `reachability_proof` — deprecated `ptypes.UnmarshalAny` confirmed, no type-URL allow-list. Approval upgraded 30%→50%. **Sprint 154 Phase 4 Docker PoC**: `@type` key in entity metadata HTTP write API → HTTP 500 (controlled error; Vault logs show no panic/stack trace). `ptypes.UnmarshalAny` sites at lines 1172,1188,1194,1271,1289,1309 are in the storage deserialization path (`identity_store.go` handles Consul/etcd/BoltDB reads), NOT the HTTP API input handler. Authenticated attacker with `identity-write` capability reaches the JSON API layer only — `@type` metadata DoS is a separate class (see LOW_YIELD). Approval held at 50%. Proof gap: (1) prove the storage-layer `ptypes.UnmarshalAny` path is reachable from a backend storage write primitive (requires Vault backend compromise — admin-equivalent); (2) identify a Go deserialization gadget chain reachable from the type-URL dispatch table. HashiCorp has a paid HackerOne program. |
| 2026-05-20 | https://github.com/hashicorp/vault | security:non_constant_time_comparison × 3 sites in `builtin/credential/approle/path_login.go` — `bytes.Equal` comparison on RoleID and SecretID in AppRole authentication path (CWE-208 timing side channel) | P2/High | $500–$3,000 | 0% → LOW_YIELD | N/A — INVALIDATED 2026-05-29 | Sprint 185 manual verification at HEAD: NO `bytes.Equal` call exists in `builtin/credential/approle/path_login.go`. AppRole login uses HMAC storage-key lookup (`createHMAC(role.HMACKey, secretID)` at line 162, 167) — HMAC hash comparison via storage key, not direct `bytes.Equal` on raw credentials. False positive: the `bytes.Equal` pattern the engine detected was either removed between Sprint 152 and HEAD, or was a context-window mismatch. Routed to LOW_YIELD. |

| 2026-05-01 | ClickHouse/ClickHouse | Unsafe string function — src/Functions/printf.cpp (×6 sprintf calls in SQL printf implementation) | P3/Medium | $100–$600 | 0% → LOW_YIELD | N/A — INVALIDATED 2026-05-29 | Sprint 185 follow-up source read at HEAD: all `sprintf` calls in `printf.cpp` are `fmt::sprintf(...)` from the `{fmt}` C++ library, NOT C's `sprintf`. `{fmt}` is type-safe and memory-safe — returns `std::string` with dynamic allocation; no stack or heap buffer overflow is possible. `SELECT printf('%s', repeat('A', 65536))` returns a valid 65536-byte string. False positive: `unsafe_string_function` detector fired on `sprintf(` substring without checking `fmt::` namespace qualifier. Routed to LOW_YIELD. |
| 2026-05-01 | ClickHouse/ClickHouse | Raw pointer dereference — rust/workspace/prql/src/lib.rs (FFI boundary) | P4/Low | $50–$100 | 0% → LOW_YIELD | N/A — INVALIDATED 2026-05-29 | Sprint 185 follow-up source read at HEAD: `slice::from_raw_parts(query, size)` at line 24 — `query` pointer is passed from ClickHouse's C++ layer which constructs it from `std::string_view::data()` (always non-null for valid C++ strings). No SQL user input reaches this FFI as a null pointer. `out`/`out_size` null checks use `assert!` (panics are caught by `panic::catch_unwind` at line 64). Gate 3 fails: no user-SQL-to-null-pointer path exists. Routed to LOW_YIELD. |
| 2026-05-08 | https://github.com/mattermost/mattermost | security:unpinned_asset (`api/v4/html/ssr_template.hbs:9`) | P3/Medium | $200–$500 | 0% → LOW_YIELD | N/A — INVALIDATED 2026-05-29 | Sprint 185 manual verification: `ssr_template.hbs` is a build artifact for API documentation generation only. `api/Makefile:75` copies it to `index.html` for Stoplight Elements OpenAPI viewer — this is NOT served by the Mattermost Go server in production. No `ListenAndServe` or file-serve route to `api/v4/html/` found in `server/`. Regular Mattermost users never receive this file. Gate 3 fails: script executes only in developer API documentation context, not in production application paths. Routed to LOW_YIELD. |
| 2026-05-20 | https://github.com/trustwallet/wallet-core | security:ffi_unsafe_deref_unguarded — `rust/tw_encoding/src/ffi.rs:72,97,123,138` and `rust/tw_hash/src/ffi.rs:40,55,77,78,91,108,109,119,129,139,149,159,169,179,189,199` (multiple unguarded raw pointer derefs in TrustWallet Core Rust FFI layer) | P2/High | $500–$2,500 | 25% | `grep -n 'from_raw_parts\|from_ptr\|extern.*C' /tmp/trustwallet-s149/rust/tw_encoding/src/ffi.rs \| head -20` — confirm `unsafe extern "C"` wrappers dereference raw pointer args without null check | Sprint 149 re-hunt: `detect_jni_boundary_calls` + `classify_ffi_deref_proof` fires at 22 sites in `tw_encoding` and `tw_hash` FFI wrappers. `extern "C"` confirmed in ±10-line context → `proof_class: ReachabilityProof` attached. Promotion requires proving that a mobile caller (iOS Swift / Android Kotlin) can pass a null or attacker-controlled pointer into these boundaries from a reachable wallet API call (e.g., `TWBase32Encode` called with null input). |
| 2026-05-20 | https://github.com/freedomofpress/securedrop-client | security:ffi_unsafe_deref_unguarded — `proxy/src/config_qubesdb.rs:18` (`null_ptr` cast) and `:26` (`CStr::from_ptr(value_unsafe)` with no null-pointer guard after `qdb_read`) | P2/High | $500–$2,000 | 25% | `grep -n 'qdb_read\|CStr::from_ptr\|null_ptr' /tmp/securedrop-client/proxy/src/config_qubesdb.rs` — confirm both lines; run Rust MIRI on `proxy` crate targeting `config_qubesdb::get` with a null-pointer QubesDB mock | Sprint 147 re-hunt confirmed KevCritical `ffi_unsafe_deref_unguarded` at TWO sites (`:18` and `:26`) via `detect_rust_extern_c_calls`. `qdb_read` returns raw pointer into Xen QubesDB; `CStr::from_ptr(value_unsafe)` dereferences without null check. `[lattice-gap: P17-3A security:ffi_unsafe_deref_unguarded]` — promotion requires proving an attacker in an adjacent Qubes VM (or compromised dom0) can write a null or adversarial pointer into the QubesDB key read at line 18. Second-stage exploit requiring Qubes VM compromise is the remaining gap. |
| 2026-05-20 | https://github.com/trustwallet/wallet-core | security:lcm_off_by_one_loop (CWE-193) × 8 surviving production sites — `src/EOS/Name.cpp:47`, `src/FIO/Actor.cpp:52`, `trezor-crypto/crypto/aes/aes_modes.c` (×2 surviving), `chacha_drbg.c:53`, `ed25519-donna/modm-donna-32bit.c:392`, `pbkdf2.c:90,166` — loop boundary arithmetic in block-cipher and KDF implementations | P2/High | $500–$2,500 | 0% → LOW_YIELD | N/A — INVALIDATED 2026-05-29 | Sprint 185 control-flow trace: (1) `pbkdf2.c:90,166` — 1-indexed block counter `for (blocknr = 1; blocknr <= blocks_count; blocknr++)` is standard RFC 2898 PBKDF2 spec; `blocks_count++` when `last_block_size != 0` ensures full coverage; final `memcpy(key + key_offset, digest, last_block_size)` handles remainder correctly. Not off-by-one. (2) `EOS/Name.cpp:47` — `str` size 13, loop i=1..12 writes `str[12-i]` = str[11..0], str[12] pre-set before loop = all 13 chars covered. Not off-by-one. `classify_lcm_off_by_one_loop_proof` fires on `<=` in loop condition without checking if 1-indexed RFC 2898 semantics apply. Routed to LOW_YIELD. |
| 2026-05-20 | https://github.com/okta/okta-auth-js | security:prototype_pollution_merge_sink @ `lib/oidc/getWithPopup.ts:34` — attacker-controlled URL parameters may be merged into the popup OAuth options object without `__proto__` sanitization (CWE-1321) | P3/Medium | $200–$1,000 | 0% → LOW_YIELD | N/A — INVALIDATED 2026-05-29 | Sprint 185 source review at HEAD: `Object.assign(params, {display: 'popup', responseMode: 'okta_post_message', popupWindow})` at line 34 assigns hardcoded values FROM a trusted object INTO `params`. This is not prototype pollution — prototype pollution requires `Object.assign(target, untrustedUserInput)` where untrustedUserInput contains `__proto__`. Here the source is hardcoded. FP: detector fires on `Object.assign` without checking whether the source argument is user-controlled. Routed to LOW_YIELD. |
| 2026-05-20 | https://github.com/Kong/kong | security:swarm_context_exfiltration @ `kong/llm/drivers/llama2.lua:27` — `ai_plugin_ctx` module access in Kong AI Gateway Llama2 driver without cross-request isolation guard (CWE-668) | P2/High | $500–$2,000 | 0% → LOW_YIELD | N/A — INVALIDATED 2026-05-29 | Sprint 185 manual verification: `kong/llm/plugin/ctx.lua` confirmed — ALL context storage uses `ngx.ctx` (per-request in OpenResty). `get_namespaced_ctx("_global", "request_body_table")` reads from `ngx.ctx["_global"]` which is isolated per-request. No Lua global variable shared across requests. `get_request_model_table_inuse()` returns model config from `ngx.ctx` namespaces only. `_M.get_global_ctx` function does not exist. FP: detector fired on import of `ai_plugin_ctx` module, not on cross-request state sharing. Routed to LOW_YIELD. |
| 2026-05-20 | https://github.com/okta/okta-auth-js | security:oauth_missing_state_validation × 4 files (`lib/oidc/endpoints/token.ts`, `lib/oidc/handleOAuthResponse.ts`, `lib/oidc/util/loginRedirect.ts`, `lib/oidc/util/prepareTokenParams.ts`) — OAuth callback handlers extract authorization code without confirmed state parameter validation at the extraction site (CWE-352) | P2/High | $500–$2,000 | 0% → LOW_YIELD | N/A — INVALIDATED 2026-05-29 | Sprint 185 source review at HEAD: `handleOAuthResponse.ts:39-40` has explicit state validation: `if (res.state !== oauthParams.state) { throw new AuthSdkError('OAuth flow response state doesn't match request state'); }`. State IS validated before code exchange. FP: engine fired on code extraction patterns at 4 sites without tracing to the centralized state check at line 39. Routed to LOW_YIELD. |
| 2026-05-20 | https://github.com/oauth2-proxy/oauth2-proxy | security:jwt_validation_bypass @ `providers/logingov.go:154` — JWT parsed without confirmed algorithm restriction; alg:none forge repro synthesized (KevCritical, `reachability_proof`) | P2/High | NO PAYOUT | Moved to NO_PAYOUT_LEDGER 2026-05-29 | Sprint 185 keyfunc body confirmed: `func(_ *jwt.Token) (interface{}, error)` — `_` discards token, no `jwt.WithValidMethods`. RSA→HMAC confusion confirmed. Finding is real but oauth2-proxy has no bug bounty program. Moved to NO_PAYOUT_LEDGER for deferred responsible disclosure. | N/A — routed to NO_PAYOUT_LEDGER. |
| 2026-05-20 | https://github.com/oauth2-proxy/oauth2-proxy | security:ssrf_dynamic_url @ `oauthproxy.go:807` — `backendLogoutURL` used in HTTP redirect without URL allowlist validation (KevCritical, `reachability_proof`) | P2/High | NO PAYOUT | Deferred 2026-05-29 | oauth2-proxy has no bug bounty program. Even if SSRF is confirmed (source tracing `backendLogoutURL` to HTTP request vs operator config), no monetary path exists. Defer until oauth2-proxy launches a bounty program. Source trace (operator config vs request) still unresolved. | N/A — no payout path. |
| 2026-05-20 | https://github.com/zitadel/zitadel | security:oauth_account_fusion_pretakeover × 3 — `backend/v3/storage/database/repository/user_idp_links.go` (3× `reachability_proof`) + `backend/v3/domain/user_identity_provider_link.go` — IDP account link merge pattern detected in Zitadel v3 pre-release storage layer (KevCritical) | P2/High | $500–$3,000 | 10% | `grep -n "AddIdentityProviderLink\|merge\|provider_link\|account" /tmp/zitadel-s155/backend/v3/storage/database/repository/user_idp_links.go \| head -20` — confirm whether `AddIdentityProviderLink` can be triggered without verifying the calling user owns the target `user_id`; trace from grpc handler → command layer → repository to identify if authenticated attacker can link their IdP account to a victim user_id | Proof gap: (1) `backend/v3` is a pre-release/unreleased code path — not in production Zitadel deployments; (2) `AddIdentityProviderLink` is an internal storage-layer function requiring application-level auth before reaching repository; (3) no unauthenticated API endpoint traced to this path. Approval 10% pending: manual trace from gRPC API handler to `AddIdentityProviderLink` confirming whether caller-owned `user_id` enforcement exists. Zitadel responsible disclosure: security@zitadel.com. |
| 2026-05-25 | https://github.com/electroneum/electroneum-sc | security:unauthenticated_debug_endpoint × 9 — Go pprof/debug HTTP handler bound without authentication in blockchain node RPC server — attacker on same network segment can read goroutine stack dumps, heap profiles, and internal mutex contention state (KevCritical) | P2/High | $300–$1,500 | 0% → LOW_YIELD | N/A — INVALIDATED 2026-05-29 | Sprint 185 manual verification: `internal/debug/flags.go:71` confirms `pprofAddrFlag.Value = "127.0.0.1"` — loopback binding by default. `--pprof` flag must be explicitly enabled AND `--pprof.addr` explicitly changed to `0.0.0.0` for network exposure. Default config is not vulnerable; requires operator misconfiguration. Gate 2 fails: not remotely exploitable without operator privilege to change the binding address. Routed to LOW_YIELD. |
