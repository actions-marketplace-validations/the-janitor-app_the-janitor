# Low-Yield Ledger

Findings with an estimated Approval % below 10 are archived here instead of deleted. They are not submission candidates; they are retained as negative training data for Omni-Audits, future AEG templates, and AST false-positive suppressions.

| Date | Target | Finding | Approval % | Reason Routed | R&D Follow-up |
|------|--------|---------|------------|---------------|---------------|
| 2026-05-29 | ClickHouse/ClickHouse | `security:unsafe_string_function` — `src/Functions/printf.cpp` ×6 `fmt::sprintf(...)` calls | 0% | FP: `fmt::sprintf` is `{fmt}` library (type-safe, memory-safe, dynamic allocation), NOT C's `sprintf`. No buffer overflow possible. `SELECT printf('%s', repeat('A', 65536))` succeeds and returns 65536-byte string. | Add `fmt::` namespace-prefix check to `unsafe_string_function` detector: if call is `fmt::sprintf(`, `fmt::format(`, classify as safe. See P2-35 in INNOVATION_LOG. |
| 2026-05-29 | ClickHouse/ClickHouse | `security:ffi_unsafe_deref_unguarded` — `rust/workspace/prql/src/lib.rs:24` `slice::from_raw_parts(query, size)` | 0% | FP: `query` pointer is internal C++ `std::string_view::data()` — never null for valid C++ string objects. No user-SQL-to-null-pointer path. `out`/`out_size` null checks via `assert!` caught by `panic::catch_unwind`. Gate 3 fails. | Add C++-internal-caller safety context guard to Rust `from_raw_parts` classifier. See P2-36 in INNOVATION_LOG. |
| 2026-05-29 | https://github.com/ory/hydra | Sprint 185 hunt: JWT validation — `fosite/authorize_request_handler.go:96,107` allows `alg:none` for OIDC Request Objects; `fosite/client_authentication.go:144` enforces `GetTokenEndpointAuthSigningAlgorithm()` match before asymmetric key dispatch. `fosite/token/jwt/token.go:164` custom `ParseWithClaims` delegates to go-jose `jwt.ParseSigned`. State validation at `authorize_request_handler.go:441` enforces ≥8 char entropy minimum. Redirect URI: `isMatchingRedirectURI` does exact match or loopback-path comparison. | 0% | All JWT paths properly restricted (alg:none for Request Objects is spec-compliant per OIDC 6.1; client assertion uses registered `TokenEndpointAuthSigningAlgorithm`). State validation present. Redirect URI exact-match only. Ory Apache 2.0 OSS users have no formal SLA and no confirmed paid bug bounty program. | Ory Network (cloud product) may have a private bug bounty — confirm at ory.com/contact before submitting. |
| 2026-05-29 | https://github.com/temporalio/temporal | Sprint 185 hunt: JWT validation at `common/authorization/default_jwt_claim_mapper.go:155` uses `jwt.NewParser(jwt.WithValidMethods(...))` — correctly constrained. TLS bypass at `common/auth/tls_config_helper.go:35,92` — operator-controlled `EnableHostVerification`. `bytes.Equal(current, token)` at `chasm/lib/scheduler/scheduler.go:889` — optimistic concurrency conflict token (ETag-equivalent), not a security credential. Nexus HTTP API `serveResolvedURL` proxies to internal gRPC Nexus handler, no outbound HTTP to user-controlled URLs. | 0% | JWT properly validated. TLS bypass operator-controlled. bytes.Equal on conflict tokens (not auth credentials) has no security impact. No Nexus SSRF. Temporal has security@temporal.io — no confirmed HackerOne/Bugcrowd paid program. | Temporal security policy at security@temporal.io — confirm paid bounty before investing further. |
| 2026-05-29 | https://github.com/cockroachdb/cockroach | Sprint 185 hunt: TLS at `pkg/security/dyncert/tls_config.go:118` — `InsecureSkipVerify: true` compensated by `VerifyConnection` callback with `verifyChain` + `verifyNames`. Cloud storage `pkg/cloud/cloud_io.go:123` — `InsecureSkipVerify` gated by `config.InsecureSkipVerify` flag (operator-controlled). OIDC at `pkg/security/oidcauth/authentication_oidc.go:195` uses `coreos/go-oidc IDTokenVerifier` (proper algorithm validation). JWT: no `golang-jwt/jwt` usage found in any auth-critical path. | 0% | All TLS bypass sites compensated. OIDC uses go-oidc (safe). No JWT algorithm confusion found. CockroachDB HackerOne program confirmed — high-value target but current detectors produce only FPs here. | Re-hunt CockroachDB with focus on multi-tenant SQL isolation and tenant-ID boundary enforcement. SQL injection in tenant routing paths. |
| 2026-05-29 | https://github.com/hashicorp/boundary | Sprint 185 hunt: No `golang-jwt/jwt` usage found. OIDC auth uses database-backed `authtoken.Repository.ValidateToken` and `coreos/go-oidc`. JWT: 0 `ParseWithClaims` hits. | 0% | No JWT algorithm confusion surface. Boundary uses DB-backed token validation. HashiCorp HackerOne program covers Boundary — valid target for future hunts with different detector focus. | Re-hunt Boundary with focus on gRPC auth interceptor coverage gaps and RBAC policy enforcement. |
| 2026-05-29 | https://github.com/grafana/grafana | Sprint 185 targeted grep: `apps/provisioning/pkg/connection/github/token.go:65` uses `jwt.WithValidMethods([]string{jwt.SigningMethodRS256.Alg()})` before keyfunc — safe. `pkg/services/rendering/auth.go:151` uses `jwt.WithValidMethods([]string{jwt.SigningMethodHS512.Alg()})` — safe. Both `func(_ *jwt.Token)` keyfuncs protected by `WithValidMethods` parser option. | 0% | Both JWT paths correctly protected by `WithValidMethods`. Grafana HackerOne program — valid target for other vulnerability classes (plugins, data sources, SSRF, stored XSS). | Re-hunt Grafana for data source SSRF (operator-configured URLs + server-side fetch to internal services), plugin sandbox escape, or stored XSS in dashboard comments. |
| 2026-05-29 | https://github.com/minio/minio | Sprint 185 hunt: `internal/config/identity/openid/jwt.go:137` creates `jwtgo.Parser` with `ValidMethods` including all RS/ES/HS variants. Fallback `jwtgo.ParseWithClaims` at line 171 has no `WithValidMethods`, BUT keyfunc returns `*rsa.PublicKey` for JWKS keys — golang-jwt HMAC verifier requires `[]byte`, so type assertion fails. The `[]byte(pCfg.ClientSecret)` in pubKeys map is keyed by `pCfg.ClientID` — an HS256 token forged with `kid = client_id` and signed with `client_secret` would pass, but requires knowing `client_secret` (Gate 2 fails — admin-configured secret). | 0% | Algorithm confusion requires knowing `client_secret` (admin privilege). Primary parser path has `ValidMethods` restriction. Gate 2 fails for unprivileged attackers. MinIO has a confirmed HackerOne bug bounty program — high-value target. | Re-hunt MinIO for: (1) pre-signed URL HMAC bypass; (2) STS token forgery via OIDC provider misconfiguration; (3) object path traversal in multi-tenant deployments. |
| 2026-05-29 | https://github.com/goharbor/harbor | Sprint 185 grep: `src/pkg/token/token.go:68` — `jwt.NewParser(jwt.WithLeeway(...), jwt.WithValidMethods([]string{opt.SignMethod.Alg()}))` — algorithm IS constrained by registered `SignMethod`. `func(_ *jwt.Token)` keyfunc protected by `WithValidMethods`. | 0% | JWT properly validated with `WithValidMethods`. Harbor has a VMware/Broadcom HackerOne program — valid target for registry-specific vulnerabilities. | Re-hunt Harbor for: OCI manifest injection, registry token scope bypass, or project admin privilege escalation. |
| 2026-05-29 | https://github.com/dexidp/dex | Sprint 185 grep: `storage/conformance/conformance.go:122` — `jose.ParseSigned(compact, []jose.SignatureAlgorithm{alg})` (explicit alg list). `server/oauth2.go:773` — `jose.ParseSigned(jwt, []jose.SignatureAlgorithm{RS256, RS384, RS512, ES256, ES384, ES512})` (explicit allowlist). go-jose v4 API with algorithm list. Both safe. | 0% | Dex uses go-jose v4 with explicit algorithm lists — safest possible API pattern. No paid bug bounty program confirmed for Dex (CNCF OSS project). | Dex has no commercial bounty. Check CNCF security policy for responsible disclosure path only. |
| 2026-05-29 | https://github.com/pomerium/pomerium | Sprint 185 grep: all JWT parsing uses `jwt.ParseSigned` (go-jose). `internal/encoding/jws/jws.go:38` — `tok.Claims(c.key, s)` verifies signature with typed key. `pkg/grpcutil/options.go:108` — `tok.Claims(key, &claims)` where key is `[]byte` (HMAC). `pkg/identity/oauth/apple/apple.go:161` — `idToken.UnsafeClaimsWithoutVerification(v)` for user info extraction AFTER OAuth2 token endpoint exchange (not from user input). | 0% | go-jose signature verification validates key-algorithm type match. Apple `UnsafeClaimsWithoutVerification` is post-token-endpoint (not user-input path). Pomerium has a security disclosure policy but no confirmed paid bounty. | Pomerium has security@pomerium.com. No confirmed paid program. |
| 2026-05-29 | https://github.com/hashicorp/consul | Sprint 185 hunt: `security:tls_verification_bypass` at `connect/tls.go:80` (`InvariantViolationProof`) — `InsecureSkipVerify: true` IS present BUT compensated by `VerifyPeerCertificate` hook for Consul Connect mTLS + AuthZ (comment at line 78 explicitly documents this). `security:protobuf_any_unguarded_decode` × 3 at `agent/xds/clusters.go:2090`, `agent/xds/listeners.go:1082,2661` — all `lattice_gap_proposal`; `EnvoyClusterJSON`/`EnvoyListenerJSON` fields come from service registration config (operator writes, requires `service:write` ACL). `security:clock_skew_auth_split_brain` at `internal/go-sso/oidcauth/config.go:149` — configurable leeway (150s default), not an exploitable clock skew attack. | 0% | TLS bypass is intentional design (compensated by VerifyPeerCertificate); xDS config requires operator ACL; clock skew leeway is a configuration parameter. | `tls_verification_bypass` detector needs to check for `VerifyPeerCertificate` callback in ±10-line window as a compensation guard before emitting `InvariantViolationProof`. If present, classify as `InvariantViolationProof` (intentional bypass with custom verification). |
| 2026-05-29 | https://github.com/argoproj/argo-cd | Sprint 185 hunt: `security:tls_verification_bypass` × 4 — all `if insecure { ... InsecureSkipVerify: true }` gates (gitea pull_request:32, scm_provider:32, logout:134, dex:35) — ALL operator-controlled `insecure` flag. `security:unpinned_asset` at `ui/src/app/index.html:19` fires on an anchor `href` pointing to the argoproj GitHub Pages docs domain in a `noscript` fallback block — NOT a loaded script/stylesheet, just a documentation hyperlink. `security:unpinned_git_dependency` at `ui/package.json:19` — `argo-ui: "git+https://github.com/argoproj/argo-ui.git"` — same-org repo under argoproj control, same maintainers. | 0% | All TLS bypass sites are explicitly guarded by operator `insecure` flag; unpinned_asset FP on documentation link; git dep is same-org low-risk. | `unpinned_asset` detector needs to skip anchor href links in noscript blocks — only flag external script src and stylesheet href without integrity= attribute. `tls_verification_bypass` needs `if insecure {` conditional guard as FP suppressor. |
| 2026-05-29 | https://github.com/gravitational/teleport | `security:protobuf_any_unguarded_decode` × 28 — `api/types/role.go`, `api/types/wrappers/wrappers.go` | 0% | Sprint 185 verification: `ptypes.UnmarshalAny` fully removed from `api/types/` at HEAD. Teleport migrated to typed `anypb.UnmarshalTo` or removed Any decode entirely. Finding was valid at Sprint 152 but is now fixed. | Teleport HackerOne program remains a valid target. Re-hunt for new protobuf/gRPC patterns in newer Teleport versions. |
| 2026-05-29 | https://github.com/hashicorp/vault | `security:non_constant_time_comparison` — `builtin/credential/approle/path_login.go` `bytes.Equal` on RoleID/SecretID | 0% | Sprint 185 verification: NO `bytes.Equal` in `path_login.go` at HEAD. AppRole uses HMAC storage-key lookup (`createHMAC` at lines 162,167) — hash-based comparison, not `bytes.Equal` on raw credentials. Pattern detector matched HMAC context incorrectly. | Vault AppRole timing side-channel class remains theoretically interesting. Future re-hunt should grep specifically for `bytes.Equal` on credential fields rather than HMAC context. |
| 2026-05-29 | https://github.com/trustwallet/wallet-core | `security:lcm_double_free` — `trezor-crypto/crypto/scrypt.c:334,336` | 0% | Sprint 185 control-flow trace: `free(XY0)` in success path (line 327) and `err2: free(XY0)` (line 334) are mutually exclusive — `return 0` before err labels; MAP_ANON munmap failure jumps to err2 only when XY0 not yet freed. Standard C error-unwind pattern. `classify_lcm_double_free_proof` attaches ReachabilityProof from SECP256K1_API context but does not trace goto label control flow. | `classify_lcm_double_free_proof` needs goto-label control-flow analysis to distinguish mutually-exclusive error unwind from true double-free. Add cross-label flow analysis to `proof_obligation.rs`. |
| 2026-05-29 | https://github.com/trustwallet/wallet-core | `security:lcm_off_by_one_loop` — `pbkdf2.c:90,166` (1-indexed PBKDF2 block counter `<= blocks_count`), `EOS/Name.cpp:47` (loop i=1..12 writing str[11..0], str[12] pre-set) | 0% | Sprint 185 control-flow trace: `pbkdf2.c` loop is standard 1-indexed PBKDF2 per RFC 2898 spec — `blocks_count++` when remainder exists, `last_block_size` guards final memcpy. `EOS/Name.cpp` writes all 13 chars correctly (str[12] set before loop, str[11..0] in loop). Not off-by-one; pattern detector fires on `<=` in loop condition without semantic check. | `classify_lcm_off_by_one_loop_proof` needs to detect RFC 2898 PBKDF2 block-counter idiom as a false-positive class. Add `pbkdf2_hmac_sha256\|pbkdf2_hmac_sha512` + `blocknr.*blocks_count` as `InvariantViolationProof` guard. |
| 2026-05-29 | https://github.com/mattermost/mattermost | `security:unpinned_asset` — `api/v4/html/ssr_template.hbs:9` (Stoplight Elements CDN script tag without SRI, loaded from unpkg.com) | 0% | Sprint 185 verification: `ssr_template.hbs` is a build artifact only. `api/Makefile:75` copies to `index.html` for docs generation. No server route in `server/` serves `api/v4/html/`. Regular users never receive this file. Gate 3 fails: non-production execution path. | Mattermost `api/v4/html/` is a Stoplight Elements documentation build. Future unpinned_asset detection should check whether the file is actually served by the server binary before emitting — cross-reference against HTTP route registration. |
| 2026-05-29 | https://github.com/Kong/kong | `security:swarm_context_exfiltration` — `kong/llm/drivers/llama2.lua:27` (`ai_plugin_ctx` import) | 0% | Sprint 185 verification: `kong/llm/plugin/ctx.lua` confirmed — all context stored in `ngx.ctx` (per-request in OpenResty). `get_namespaced_ctx("_global", ...)` reads `ngx.ctx["_global"]` — per-request namespace KEY, not a Lua global. No `get_global_ctx` function exists. FP: detector fired on `require("kong.llm.plugin.ctx")` import without analyzing storage backend (ngx.ctx isolation). | `swarm_context_exfiltration` detector needs OpenResty/Kong-specific guard: if context storage uses `ngx.ctx`, classify as request-isolated. Add `ngx.ctx` as a safe storage sentinel. |
| 2026-05-29 | https://github.com/okta/okta-auth-js | `security:prototype_pollution_merge_sink` — `lib/oidc/getWithPopup.ts:34` `Object.assign(params, {hardcoded})` | 0% | FP: `Object.assign(target, hardcoded_source)` is not prototype pollution. Source argument `{display: 'popup', responseMode: 'okta_post_message', popupWindow}` is fully hardcoded, not user-controlled. Detector fires on presence of `Object.assign` without checking source argument taint origin. | Add direction-aware check to `prototype_pollution_merge_sink`: require source argument (arg[1]) to be user-controlled (HTTP request, URL params) before emitting. |
| 2026-05-29 | https://github.com/okta/okta-auth-js | `security:oauth_missing_state_validation` — `handleOAuthResponse.ts:39` | 0% | FP: `handleOAuthResponse.ts:39` contains `if (res.state !== oauthParams.state) { throw new AuthSdkError(...) }` — explicit string equality check before code exchange. Engine fired on code-extraction patterns in 4 files without tracing to centralized state check. | `classify_oauth_state_validation_proof` needs to search for `res.state !== oauthParams.state` or equivalent string comparison pattern as a `InvariantViolationProof` guard before emitting in TypeScript files. |
| 2026-05-29 | https://github.com/hashicorp/vault | `security:ssrf_dynamic_url` — `builtin/credential/cert/backend.go:188` `http.Get(crl.CDP.Url)` | 0% | Sprint 185 source trace: `crl.CDP.Url` set by admin `vault write auth/cert/crls` API only. No X.509 CRL Distribution Point extension parsing path. URL is operator-configured storage entry, not derived from client certificate. Gate 2 fails. | SSRF detector needs to distinguish admin-write-path URL sinks (`pathCRLWrite`) from certificate-derived URL sinks. Add `vault write auth/cert` admin path context as operator-controlled suppressor. |
| 2026-05-29 | https://github.com/electroneum/electroneum-sc | `security:unauthenticated_debug_endpoint` × 9 — Go pprof debug endpoint | 0% | Sprint 185 verification: `internal/debug/flags.go:71` `pprofAddrFlag.Value = "127.0.0.1"` — loopback by default. `--pprof` must be explicitly enabled; `--pprof.addr` must be explicitly changed to `0.0.0.0` for network exposure. Gate 2 fails: requires operator privilege to expose. | pprof binding address detector needs to check `pprofAddrFlag.Value` default — if default is `127.0.0.1`, route to LOW_YIELD unless `0.0.0.0` binding is confirmed. |
| 2026-05-29 | https://github.com/elastic/elasticsearch | Sprint 184 hunt: `security:java_deser_allowlist_bypass` — no `ObjectInputStream`, `ObjectSerializationDecoder`, or `setAllowClasses` patterns found in `server/src/main/java` production paths (grep confirmed). `ConfigurationUtils.readObject` is a config-map accessor, not Java native object deserialization. No Netty `ObjectDecoder` pipeline wiring found. No `protobuf_any_unguarded_decode` — Elasticsearch uses custom wire format, not protobuf Any. | 0% | Threat Model Pre-Filter gate 1: no Java native deserialization sink (`ObjectInputStream.readObject()`) reachable from unprivileged network input. All inbound request deserialization uses ES's own custom binary transport protocol with strongly-typed decoders, not Java native serialization. Elastic HackerOne program is in scope but findings require a network-reachable Java deser sink. | No bounty action. Elasticsearch does not use Java native object deserialization in production transport paths. Future hunts should target `x-pack` plugin layers for Watcher/Script engine eval paths (potential RCE via Mustache/Painless script injection class). |
| 2026-05-29 | https://github.com/kubernetes/kubernetes | Sprint 184 hunt: `security:ssrf_dynamic_url` in `staging/.../admission/plugin/webhook/` — webhook destination URL comes from `ValidatingWebhookConfiguration.ClientConfig` (cluster-admin API resource); `security:path_traversal` in `pkg/volume/util/atomic_writer.go` — `filepath.Join(w.targetDir, ...)` where path components are ConfigMap/Secret data keys restricted to `[a-zA-Z0-9._-]` by Kubernetes API validation; no `filepath.Join` with user HTTP query input confirmed. | 2% | Threat Model Pre-Filter gate 2 fails for webhook SSRF: `ValidatingWebhookConfiguration.ClientConfig.URL` is set by cluster-admin (privileged). Gate 1 fails for path traversal: ConfigMap/Secret key names are validated at admission time (`pkg/api/validation/validation.go` DNS label restrictions); no raw user HTTP path components reach `filepath.Join`. CNCF Security Advisories scope confirmed. | No bounty action. Kubernetes webhook SSRF requires cluster-admin privilege to set URL (not an unprivileged attacker vector). Volume path traversal is mitigated by API validation of key name character set. Future hunts: target `pkg/apiserver/` OIDC token validation or `pkg/auth/` for unauthenticated paths. |
| 2026-05-29 | https://github.com/open-telemetry/opentelemetry-collector | Sprint 184 hunt: `security:ssrf_dynamic_url` in `exporter/otlphttpexporter/otlp.go:178` — `http.NewRequestWithContext` sends to `oCfg.ClientConfig.Endpoint`; `security:config_taint_*` in `receiver/otlpreceiver/` — all `endpoint:` fields (default `localhost:4317`/`localhost:4318`). | 2% | Threat Model Pre-Filter gate 1 fails for SSRF: `ClientConfig.Endpoint` is operator YAML config (`config.yaml`), not derived from incoming telemetry data payloads. Receiver endpoint URLs are operator-configured listen addresses, not attacker-supplied fetch targets. OTel telemetry body (spans/metrics/logs) is deserialized via strongly-typed protobuf — no dynamic URL derivation from telemetry attributes. OTel security reports: https://github.com/open-telemetry/opentelemetry-collector/security. | No bounty action. All URL-construction sites in the collector derive from operator pipeline config, not from attacker-controlled telemetry. Future hunts: target `confmap` provider plugins (env/file/http) where provider URLs could be user-controlled if the config reloader accepts external input. |
| 2026-05-28 | https://github.com/hashicorp/terraform | Sprint 183 hunt: `security:protobuf_any_unguarded_decode` High ×3 (`internal/stacks/stackplan/from_proto.go:59`, `internal/stacks/stackstate/from_proto.go:81`, `internal/plans/planfile/tfplan.go:52`); `security:path_traversal` High ×4 (`checkpoint.go:50,60`, `internal/backend/local/backend.go:251,425`); `security:embedding_trust_transposition` Critical lattice_gap_proposal (`internal/backend/remote-state/s3/backend.go`) | 4% | Threat Model Pre-Filter gate 2 fails on all paths: Terraform is a CLI tool that reads operator-controlled state/plan files from local disk or trusted backends. Protobuf_any_unguarded_decode requires attacker backend write access (admin-equivalent) to inject malicious state. Path_traversal sites are all local config/state file paths — operator-controlled by design. S3 backend trust transposition is FP class (no LLM vector store). | No bounty action. Terraform's threat model is operator-controlled config; no unauthenticated network boundary exposed. Protobuf_any findings would require Terraform Enterprise API analysis — defer to TFE-specific hunt if needed. |
| 2026-05-28 | https://github.com/grafana/grafana | Sprint 183 hunt: `security:ssrf_dynamic_url` KevCritical ×6 (`packages/grafana-runtime/src/services/pluginMeta/plugins.ts:21,45,61`; `public/app/core/services/context_srv.ts:250`; `public/app/plugins/datasource/loki/tracking.ts:161`; `public/swagger/K8sNameLookup.tsx:37`); one Go SSRF in `pkg/services/publicdashboards/internal/commands/generate_datasources/generate.go:60` | 4% | All TypeScript findings are client-side `fetch()` calls (FP class per Bounty Extraction Law: client-side fetch ≠ SSRF). Go SSRF has `source_label: baseUrl + allPluginsEndpoint` — `baseUrl` is admin-configured datasource URL, not a user-controlled HTTP request parameter in production. Code generation command tool, not a production API handler. | Client-side SSRF classifier needs SSR-execution-path guard for `.ts`/`.tsx` files in `public/` directories. Go SSRF detector needs to suppress config-sourced `baseUrl` variables in internal command tools. |
| 2026-05-28 | https://github.com/cilium/cilium | Sprint 183 hunt: `security:ssrf_dynamic_url` KevCritical at `cilium-cli/features/summary.go:184` (source: generic 'String' field in CLI tool); `security:protobuf_any_type_field` Critical ×4 (api/v1/ proto annotations); `security:vector_filter_polymorphism` High at `pkg/datapath/linux/devices_controller.go:437` (FP on eBPF networking code); `security:financial_pii_to_external_llm` in `Documentation/` .rst file (non-executable) | 3% | SSRF finding fires on generic Go struct field named 'String' in CLI tool — not a network service endpoint, fails gate 1. Protobuf_any_type_field findings are proto schema annotations only (not unguarded decodes). vector_filter_polymorphism is FP on eBPF Linux datapath code. financial_pii on documentation .rst fails gate 3 (non-executable). Cilium security reports: security@cilium.io. | SSRF detector needs stronger field-name heuristic: 'String' field name alone insufficient; require 'url', 'URL', 'endpoint', 'host' semantic in field name or context. Vector filter detector needs to skip Go files in `pkg/datapath/linux/` (kernel networking, not ML vector stores). |
| 2026-05-28 | https://github.com/indeedeng/proctor | `security:dom_xss_innerHTML` (foundation.js lines 4, 19) — `reachability_proof`, Critical | 4% | Sprint 180 hunt: DOM XSS in vendored Zurb Foundation.js library. Source and sink both within foundation.js — library-internal DOM manipulation. Route `/function` is engine-templated, not a confirmed Indeed live endpoint. Proctor is an OSS A/B testing framework; vendored library not under Indeed's direct patch cycle. | Vendored library false-positive: improve detector to suppress findings where source and sink file are identical AND the file is under a `lib/` or `vendor/` path with a known library name in it. |
| 2026-05-28 | `aiven-client` (PyPI) | `security:config_taint_*` batch (cliarg.py, cli.py) — `lattice_gap_proposal`, High | 2% | Sprint 180 hunt: All `config_taint` findings on CLI `arg()` definitions (`--json`, `--timeout`, `--verbose`). CLI tool, arguments are user-controlled by design. No network-reachable attack surface. Already suppressed class per prior Superset note. | CLI argument parser gate: add detection that `arg(` calls in argparse/click context are not HTTP taint sources. |
| 2026-05-28 | `asana` (npm) | `security:config_taint_opts['sync']` (codegen/templates/README.mustache) — `lattice_gap_proposal`, High | 1% | Sprint 180 hunt: Pattern fired on mustache template in README codegen template. No executable code path. Documentation artifact, not production logic. | Add `.mustache` / `.md` extension suppressor to config_taint detector — documentation templates cannot carry taint. |
| 2026-05-22 | https://github.com/apache/superset | `security:jwt_validation_bypass` (`superset/utils/oauth2.py`) — `reachability_proof`, KevCritical | 5% | Sprint 170 hunt: `jwt.decode` at line 262-265 uses `algorithms=[app.config["DATABASE_OAUTH2_JWT_ALGORITHM"]]` — algorithm list is admin-configurable, not user-controlled. Per Triage Empathy Law: requires admin `DATABASE_OAUTH2_JWT_ALGORITHM=none` config change to exploit. Effectively operator-privilege-gated. | Requires `jwt_keyfunc_oracle` to detect admin-config-bound algorithm arrays as a sanitizer. Add `app.config["...ALGORITHM"]` array pattern to `JwtKeyfuncVerdict::Guarded` check. |
| 2026-05-22 | https://github.com/apache/superset | `security:oauth_missing_state_validation` (`superset/views/users/api.py`) — `lattice_gap_proposal`, High | 5% | Sprint 170 hunt: `users/api.py` is a user profile API (avatar, roles), not an OAuth callback handler. No `code` or `state` parameter extraction. Java HTTP-handler gate fix from this sprint would have suppressed this. Python equivalent gate needed. | Add Python handler context gate to `classify_oauth_state_validation_proof`: require file path to contain `oauth`, `callback`, `auth_callback`, or source to contain `request.args.get("code")` before emitting for Python files that are named `api.py` in a `users/` path. |
| 2026-05-22 | https://github.com/apache/superset | `security:config_taint_*` batch (superset/views/core.py, superset/utils/core.py et al.) — `lattice_gap_proposal`, High | 3% | Sprint 170 hunt: All `config_taint` findings are Python dict/list internal routing patterns (`form_data["key"]`, `slc.params`, session query objects) — not server-side HTTP input reaching dangerous sinks. All `lattice_gap_proposal`. No autonomous payload. | `config_taint` detector needs Python dict-access pattern to distinguish internal model state from HTTP-sourced user input. Add `request.args`, `request.json`, `request.form` provenance check before routing to CANDIDATE. |
| 2026-05-22 | https://github.com/supabase/gotrue | `security:oauth_missing_state_validation` batch (`internal/api/jwks.go`, `internal/api/oauthserver/handlers.go`, `internal/api/oauthserver/service.go`) — `lattice_gap_proposal`, High | 4% | Sprint 170 hunt: GoTrue uses DB-backed UUID flow state (see `loadFlowState` → `loadExternalState` → `uuid.FromString(state)` → `loadExternalStateFromUUID`). State IS validated. `jwks.go` is public-key endpoint; `oauthserver/handlers.go` is token endpoint (server, not client callback); `service.go` is service logic. All correctly suppressed as `lattice_gap_proposal`. | GoTrue `external.go` OAuth callback correctly validates state via DB UUID lookup. Classifier should detect `uuid.FromString(state)` + DB lookup as a state-validation guard in Go. |
| 2026-05-22 | https://github.com/supabase/gotrue | `security:jwt_validation_bypass` (`internal/api/auth.go:82`) — `reachability_proof`, Informational; `security:oauth_csrf_missing_state` (`internal/api/oauthserver/authorize.go:83`) — Informational | 2% | Sprint 170 hunt: Both findings carry `[deprecated_target: README.md="deprecated"] informational_only_per_scope_exclusion` in remediation. GoTrue is deprecated per its own README (superseded by Supabase Auth). Informational-only scope exclusion applies. | No bounty action. Preserve as deprecated-target negative sample. |
| 2026-05-22 | https://github.com/mattermost/mattermost-plugin-boards | `security:ssrf_dynamic_url` batch (`webapp/src/octoClient.ts` ×N) — `reachability_proof`, KevCritical | 5% | Sprint 170 hunt: `octoClient.ts` is a pure React browser client. All `fetch(this.getBaseURL() + path, ...)` calls are client-side TypeScript. Per Triage Empathy Law: client-side fetch ≠ server-side SSRF without SSR proof. SOP/CORS blocks cross-origin SSRF from browser. No Next.js API route or SSR path proved. | No bounty action. Client-side SSRF classifier needs SSR-execution-path guard for TypeScript files in `webapp/src/` directories. |
| 2026-05-22 | https://github.com/mattermost/mattermost-plugin-boards | `security:unauthenticated_debug_endpoint` (`server/assets/templates-boardarchive/brs9cdimfw7fodyi7erqt747rhc/board.jsonl:4`) — `lattice_gap_proposal`, KevCritical | 2% | Sprint 170 hunt: Template JSON fixture file in archive asset directory, not a production route registration. Engine matched JSON structure as route metadata. | Debug endpoint detector needs to skip template archive paths (`/templates-boardarchive/`, `/fixtures/`, `/testdata/`). |
| 2026-05-22 | https://github.com/hashicorp/vault | security:embedding_trust_transposition (`vault/logical_system.go:1325`) — fingerprint `b8c48c16e5b49ebd`, `lattice_gap_proposal`, Informational | 2% | Sprint 169 re-hunt: LLM-sink guard shipped this sprint correctly demoted this finding from `reachability_proof` to `lattice_gap_proposal`. `vault/logical_system.go` is Vault's core logical system routing layer — no LLM API call present (no openai/anthropic/llm.invoke/langchain). Engine behavior confirmed correct. | Proof: LLM-sink guard working as intended. No additional fix required. |
| 2026-05-22 | https://github.com/hashicorp/vault | security:iac_agentic_recon_target (`enos/ci/service-user-iam/main.tf:240`) — Informational | 2% | IaC CI terraform file for Vault's Enos test infrastructure IAM roles. Informational reconnaissance signal only; not a bounty target. | No bounty action. Preserve as IaC-CI negative sample. |
| 2026-05-22 | https://github.com/keycloak/keycloak | security:oauth_missing_state_validation batch — `core/src/main/java/org/keycloak/OAuth2Constants.java` (constants file), `server-spi-private/src/main/java/org/keycloak/authentication/Authenticator.java` (SPI interface), `server-spi-private/src/main/java/org/keycloak/scripting/Script.java` (scripting interface), `services/src/main/java/org/keycloak/protocol/oidc/grants/device/endpoints/DeviceEndpoint.java` (device code flow) — all `lattice_gap_proposal`, High | 5% | Keycloak is a mature, enterprise-grade IdP with extensive security review. OAuth2Constants.java is a constants definition, not a callback handler. Authenticator.java and Script.java are SPI interfaces, not implementations. DeviceEndpoint.java handles device code flow which does not use state parameter by RFC 8628 design. All `lattice_gap_proposal` — engine cannot prove HTTP callback reachability. | Add HTTP-handler context gate to `classify_oauth_state_validation_proof`: require file path to contain `controller`, `handler`, `callback`, `endpoint` AND import `javax.ws.rs` or Spring `@RequestMapping` annotation before emitting `reachability_proof` on Java files. |
| 2026-05-22 | https://github.com/keycloak/keycloak | security:llm_model_unverified_load (`js/apps/keycloak-server/scripts/start-server.js:143`) — `lattice_gap_proposal`, Informational | 2% | JavaScript startup script for Keycloak development server. Not a bounty target. Informational only. | No bounty action. Preserve as development-tooling negative sample. |
| 2026-05-22 | https://github.com/okta/okta-auth-js | security:oauth_missing_state_validation (`lib/oidc/util/oauthMeta.ts`) — `lattice_gap_proposal`, High | 8% | New file in Sprint 169 re-hunt (not in prior Sprint 151 CANDIDATE entry). `oauthMeta.ts` is a metadata utility that prepares OAuth parameters, not an inbound callback handler. No HTTP redirect consumption path proved. Triage Empathy: utility/preparation layer, not callback boundary. | Existing Sprint 151 CANDIDATE entry covers `handleOAuthResponse.ts` (the actual callback handler). This utility file is a classifier FP. Add path-segment guard to skip `util/` subdirectory files unless callback consumption pattern (`code = `, `state = `, `getQueryParam`) is present. |
| 2026-05-22 | https://github.com/gravitational/teleport | security:embedding_trust_transposition (`lib/srv/db/common/auth.go:1272`, `lib/utils/aws/signing.go:98`) — fingerprint `12959c8f40a29fb6`, Informational, `reachability_proof` | 2% | Both files are Teleport database access control and AWS credential signing utilities — neither is a RAG/vector store pipeline. The detector matched similarity-retrieval patterns in production Go code that uses pattern-matching or hash-lookup idioms, not LLM retrieval. No RAG context sink is present; the `embedding_trust_transposition` classifier passed `reachability_proof` on the IFDS RAG-shape heuristic but the threat model is inapplicable. **NOTE: Sprint 169 LLM-sink guard fix applies; re-hunt will produce `lattice_gap_proposal` for these files.** | LLM-sink guard shipped Sprint 169. These Sprint 168 rows are now historical artifacts — the fix prevents future false positives of this class. |
| 2026-05-22 | https://github.com/casdoor/casdoor | security:oauth_missing_state_validation (client-side: `web/src/ApplicationListPage.js`, `web/src/backend/InvitationBackend.js`) — lattice_gap_proposal, High | 3% | Client-side JavaScript files. Triage Empathy: OAuth state validation in React SPA code is handled by the OAuth redirect library (PKCE/implicit flow), not by individual component files. No server-side execution path proved; SOP/CORS blocks client-side CSRF from a third-party origin. | Add extension filter to `oauth_missing_state_validation` detector to skip `.js`/`.ts`/`.tsx` files unless an explicit `fetch()` → server-side redirect is proven (SSR only). |
| 2026-05-22 | https://github.com/casdoor/casdoor | security:oauth_missing_state_validation (Go IdP connector batch: `idp/weibo.go`, `idp/wecom_third_party.go`, `notification/wecom.go`, `object/oauth_dcr.go`, `object/syncer_dingtalk.go`, `object/syncer_lark.go`, `object/syncer_wecom.go`, `object/wellknown_oidc_discovery.go`) — lattice_gap_proposal, High | 8% | All findings are `lattice_gap_proposal` — the engine detected `code` extraction from OAuth callback parameters but could not prove these connector files handle the HTTP redirect callback without a central state-validation middleware. Casdoor is an IdP; state validation for third-party social logins is typically enforced in the central router/middleware layer (`controllers/auth.go`), not in each IdP connector. Requires manual confirmation that the central callback handler delegates without state check. | Wire `classify_oauth_state_validation_proof` to look for a centralized state-check function in `controllers/auth.go` or equivalent router file before emitting `reachability_proof` for IdP connector files; IdP connectors are library-internal, not direct HTTP handlers. |
| 2026-05-22 | https://github.com/Uniswap/v3-periphery | Sprint 164 hunt batch: no findings | 0 | Uniswap `v3-periphery` is in Immunefi scope, but the cloned target emitted zero findings. | No bounty action. Preserve as clean in-scope smart-contract negative sample for current detector coverage. |
| 2026-05-22 | https://github.com/aave/aave-v3-periphery | Sprint 164 hunt batch: `security:flash_loan_callback_unvalidated_sender`, `security:reentrancy`, `security:unprotected_authority_transition`, `security:unsafe_delegatecall` | 5 | Aave `aave-v3-periphery` is in Immunefi scope, but all rows emitted `Informational` and were downgraded by engine scope metadata (`deprecated_target: README.md="deprecated"` and focus-area mismatch where applicable). No autonomous fund-impact payload or accepted on-chain state transition was proven. | Re-evaluate only if a supported Aave periphery path proves unauthorized flash-loan callback control, reentrancy, or delegatecall impact with an executable smart-contract PoC. |
| 2026-05-22 | https://github.com/mattermost/mattermost-plugin-ai | Sprint 164 hunt batch: `security:financial_pii_to_external_llm` ×3, `security:config_taint_fileInput.files`, `security:oauth_missing_state_validation` in `mcp/dcrp.go` | 5 | Mattermost Copilot plugin is in scope, but the batch does not prove a bounty-grade exploit. `bifrost/*` and system-console rows are service configuration/model-listing paths, not demonstrated financial PII exfiltration; `rhs_file_drop_zone.tsx` is client-side drag/drop forwarding; `mcp/dcrp.go` is RFC 7591 dynamic client registration and metadata discovery, not an inbound browser OAuth callback requiring a `state` check. All relevant rows carried `lattice_gap_proposal` or client-side taint. | Add classifier guard for OAuth-state findings in Dynamic Client Registration Protocol clients: token/registration/metadata client code is not a browser redirect callback unless the handler consumes inbound `code` and `state` from a user-agent redirect. |
| 2026-05-22 | https://github.com/Uniswap/v3-info | Sprint 162 hunt batch: no findings | 0 | Immunefi scope explicitly lists `Uniswap/v3-info` as out of scope; the cloned target emitted zero findings. | No new P-tier item. Preserve as out-of-scope UI/info negative sample. |
| 2026-05-22 | https://github.com/aave/aave-ui | Sprint 162 hunt batch: `security:missing_ownership_check` ×9, `security:parser_exhaustion_anomaly` ×3, `security:react_xss_dangerous_html` ×2, `security:ssrf_dynamic_url` ×2 | 0 | Immunefi scope explicitly lists `aave/aave-ui` as out of scope. All rows emitted `Informational`, with deprecated-target/focus-area demotions; UI/Cypress selectors and client-side fetch surfaces do not prove smart-contract fund impact. | No new P-tier item. Re-evaluate only if a supported Aave in-scope repository exposes the same class through protocol fund movement or oracle/collateral state. |
| 2026-05-22 | https://github.com/smartcontractkit/chainlink | Sprint 162 hunt batch: `security:protobuf_any_unguarded_decode` ×45, `security:model_weight_backdoor` ×7, `security:unauthenticated_debug_endpoint` ×3, `security:tls_verification_bypass` ×2, plus 5 informational singletons (`jwt_validation_bypass`, `financial_pii_to_external_llm`, `embedding_trust_transposition`, `command_injection_shell_exec`) | 0 | Chainlink is in Immunefi scope, but every row emitted `Informational`. JWT is algorithm-allowlisted, protobuf rows are concrete typed unmarshals or lattice gaps, and the remediation stream cites deprecated-target/focus-area mismatch; no autonomous oracle-feed, on-chain, or off-chain data-integrity payload survived triage. | No new P-tier item. Re-evaluate only if a non-deprecated Chainlink production path reaches oracle feed manipulation, callback integrity loss, or accepted on-chain state change with an autonomous payload. |
| 2026-05-21 | https://github.com/cashapp/cash-app-pay-android-sdk | Sprint 160 hunt batch: no findings | 0 | Block/Cash App scope lists `cash-app-pay-android-sdk`, but the cloned target emitted zero findings. | No new P-tier item. Preserve as clean Android payments SDK negative sample. |
| 2026-05-21 | https://github.com/cashapp/hermit | Sprint 160 hunt batch: `security:unpinned_asset` ×2 in installer templates (`cmd/geninstaller/install.sh.tmpl`, `files/install.sh.tmpl`) | 5 | Block/Cash App scope lists Hermit, but both findings are local installer template symlink/download UX surfaces, not server-side or cross-user product impact. Taint source is local operator install control; no autonomous payload. | No new P-tier item. Re-evaluate only if a remote update channel or release manifest can be modified by an attacker without maintainer compromise. |
| 2026-05-21 | https://github.com/bullish-exchange/api-docs | Sprint 160 hunt batch: no findings | 0 | Bullish scope lists the API docs repository, but the cloned target emitted zero findings. | No new P-tier item. Preserve as clean API-documentation negative sample. |
| 2026-05-21 | https://github.com/immutable/wallet-contracts | Sprint 159 hunt batch: no findings | 0 | Immutable scope lists the wallet contracts repository, but `janitor hunt` emitted zero findings from the cloned target. | No new P-tier item. Preserve as clean Web3 contract-repository negative sample for the current detector set. |
| 2026-05-21 | https://github.com/ClickHouse/ClickHouse | Sprint 159 residual hunt batch excluding existing and newly logged CANDIDATE rows: `security:config_taint_*` mass batch, `security:optimizer_phantom_authority` ×141, `security:ci_persistence_vector` ×2, `security:dom_xss_innerHTML` ×1, `security:subprocess_shell_injection` ×10, `security:unauthenticated_debug_endpoint` ×18, `security:unpinned_asset` ×8, `security:llm_model_unverified_load` ×7, `security:lotl_api_c2_exfiltration` ×8, `security:model_weight_backdoor` ×3, `security:os_command_injection` ×1 | 5 | ClickHouse scope requires a supported-release `clickhouse-server` impact. Residual rows terminate in CI/praktika tooling, `.claude` helpers, CMake/debug-symbol scripts, benchmark/demo HTML utilities, browser-only utilities, model/config heuristics, or local utility scripts without a public server entrypoint or autonomous payload. | No new P-tier item. Re-evaluate only if a residual class proves a release-build server path reachable from SQL/client input or ClickHouse Cloud tenant control. |
| 2026-05-21 | https://github.com/afterpay/sdk-android | Sprint 159 hunt batch: no findings | 0 | Block/Afterpay scope includes `sdk-android`, but the S159 clone emitted zero findings. | No new P-tier item. Preserve as clean Android SDK negative sample. |
| 2026-05-21 | https://github.com/gleanbugbounty/mcp-server-bugbounty | Sprint 158 hunt batch: no findings | 0 | Glean target scope confirms the MCP server repository is in scope for code-change style triggers, but `janitor hunt` emitted zero findings. | No new P-tier item. Preserve as clean MCP-server negative sample. |
| 2026-05-21 | https://github.com/electroneum/electroneum | Sprint 158 hunt batch: `security:lcm_*` ×52, `security:optimizer_phantom_authority` ×12, `security:config_taint_*` ×9, `security:ssrf_dynamic_url` ×3, `security:parser_exhaustion_anomaly` ×3, `security:unsafe_string_function` ×7 | 5 | Legacy Blockchain scope includes the repository, but the emitted paths are vendored/external libraries (`external/db_drivers/liblmdb`, `external/easylogging++`, `external/secp256k1/src/tests.c`), GUI migration tooling, Python RPC utilities, or generic memory witnesses that require a deterministic fixture rather than proving unauthorized mint, bridge exploit, accepted ledger rewrite, wallet-key theft, or block-explorer integrity impact. | No new P-tier item. Re-evaluate only if a witness reaches consensus state, wallet key material, accepted chain history, or block-explorer display integrity from a supported public entrypoint. |
| 2026-05-21 | https://github.com/trustwallet/wallet-core | Sprint 158 residual hunt batch excluding existing CANDIDATE rows: `security:java_deser_allowlist_bypass` ×86, `security:ffi_memory_corruption` ×20, `security:protobuf_any_type_field` ×4, `security:ld_preload_injection` ×4, `security:unsafe_string_function` ×3, plus bounded-overflow/debug informational singletons | 5 | Binance scope lists Trust Wallet `wallet-core`, but the residual rows are informational, tool/generated-path, protobuf type-field presence, yarn lock/debug noise, or generic Rust FFI lifetime advisories without a working wallet-impact PoC. Existing candidate rows for `lcm_double_free`, `lcm_off_by_one_loop`, and `ffi_unsafe_deref_unguarded` remain routed in CANDIDATE_LEDGER and are not duplicated here. | No new P-tier item. Promote only if mobile caller-path proof reaches a wallet signing, seed, key-derivation, or transaction-construction boundary with an autonomous crash/corruption payload. |
| 2026-05-21 | https://github.com/smartcontractkit/chainlink | Sprint 157 hunt batch: `security:protobuf_any_unguarded_decode` ×45, `security:model_weight_backdoor` ×7, `security:unauthenticated_debug_endpoint` ×3, `security:tls_verification_bypass` ×2, plus 5 singletons (`jwt_validation_bypass`, `financial_pii_to_external_llm`, `embedding_trust_transposition`, `command_injection_shell_exec`) | 0 | All findings emitted `Informational` after scope/deprecation triage. Remediation stream repeatedly cites `[deprecated_target: README.md="deprecated"]`; protobuf rows mostly carry concrete-typed-unmarshal demotion; JWT carries `jwt_keyfunc_oracle` allowlist proof. Immunefi scope includes Chainlink, but no autonomous exploit payload survives target deprecation and focus-area mismatch. | No new P-tier item. Existing oracles already demote the observed classes; re-hunt only if the program publishes a non-deprecated Chainlink target path. |
| 2026-05-21 | https://github.com/immutable/ts-immutable-sdk | Sprint 157 hunt batch: `security:oauth_account_fusion_pretakeover` ×60+, `security:ssrf_dynamic_url` ×6, `security:config_taint_*` ×10, `security:dom_xss_innerHTML` ×1, `security:non_constant_time_comparison` ×1 | 0 | All findings emitted `Informational` with `[deprecated_target: CONTRIBUTING.md="deprecated"]`; OAuth rows are SDK/client/server helper surfaces with `lattice_gap_proposal`, SSRF rows are client-side SDK URL construction without server-side execution proof, and config-taint rows are local configuration/control-plane telemetry. Immutable SDK is in program scope, but no finding has an autonomous payload or cross-user exploit path. | No new P-tier item. Re-evaluate only if a production Passport/Auth callback path proves server-side execution and user-controlled taint. |
| 2026-05-20 | https://github.com/gravitational/teleport | security:jwt_validation_bypass (`lib/cloud/azure/kubernetes.go:570` — `ParseUnverified` on Azure SDK token) | 8 | Intentional design: comment at line 568 documents "We do not want to validate the token since we generated it from Azure SDK." Token origin is Azure credential pipeline (trusted). Attacker control requires prior Azure credential compromise (admin-level). Threat Model: requires admin privilege. | Add a comment-aware AST guard in the jwt_validation_bypass detector that suppresses findings where a parent comment block within ±5 lines contains "do not want to validate" or "we generated it from" — eliminates this class of intentional-bypass false positives. |
| 2026-05-20 | https://github.com/gravitational/teleport | security:dom_xss_innerHTML × 2 (`web/packages/teleport/src/lib/term/terminal.ts`, `web/packages/teleterm/src/ui/DocumentTerminal/Terminal/ctrl.ts`) | 5 | Terminal WebSocket output rendered via innerHTML in the terminal emulator component. Self-XSS unless an attacker controls the server-side terminal output stream — requires SSH session hijack or Teleport proxy compromise. Threat Model: not cross-user without prior server compromise. | Map terminal-output innerHTML sinks to a separate `terminal_xss_through_server_compromise` class at 0% to prevent confusion with stored XSS candidates. |
| 2026-05-20 | https://github.com/gravitational/teleport | security:credential_leak × 13 (`api/fixtures/`, `fixtures/certs/`) | 5 | Fixture/test credential files — `invariant_violation_proof` correctly applied. Not billable. | No follow-up required. |
| 2026-05-20 | https://github.com/gravitational/teleport | security:model_weight_backdoor × 8 (database Go files: `lib/services/database.go`, etc.) | 0 | FP: Go database service configuration structs misidentified as ML model weight payloads. Pattern fires on serialized database config structs containing arbitrary byte fields. | Add a file-path guard to `model_weight_backdoor` suppressing `.go` files under `lib/services/` and `lib/client/db/` unless the file imports a known ML library (tensorflow, pytorch, onnx). |
| 2026-05-20 | https://github.com/gravitational/teleport | security:embedding_trust_transposition × 3 (`lib/integrations/awsoidc/`, `lib/srv/db/common/auth.go`, `lib/utils/aws/signing.go`) | 8 | AWS OIDC integration and DB auth code — `reachability_proof` but no exploit chain proven. `embedding_trust_transposition` requires proving a similarity-search vector store is present and attacker-controlled content can be injected. These files do not use a vector store. FP class. | Add a vector-store import guard to `embedding_trust_transposition`: suppress unless the file imports a known embedding/vector-DB library. |
| 2026-05-20 | https://github.com/gravitational/teleport | security:config_taint_* × 8 TypeScript UI state fields (`web/packages/teleport/src/`) | 3 | Client-side TypeScript state object field reads — internal component state, no server-side taint source proven. Threat Model: client-side state manipulation without proven SSR path → <10%. | No follow-up required. |
| 2026-05-20 | https://github.com/hashicorp/vault | security:missing_ownership_check × 29 (`ui/mirage/handlers/`) | 0 | Mirage.js mock API handlers used only in UI development/test environment. Not shipped in production builds. Threat Model: test infrastructure, not production code. | Add a path-based suppressor for `ui/mirage/` prefix so mock-API handlers are never emitted from `missing_ownership_check`. |
| 2026-05-20 | https://github.com/hashicorp/vault | security:credential_leak × 2 (`builtin/credential/aws/path_login.go`, `vault/testing.go`) — `invariant_violation_proof` | 5 | Both suppressed correctly: test fixture credentials. Not billable. | No follow-up required. |
| 2026-05-20 | https://github.com/hashicorp/vault | security:tls_verification_bypass × 2 (`builtin/logical/pki/acme_challenges.go`) — `invariant_violation_proof` | 5 | Suppressed correctly: conditional InsecureSkipVerify inside ACME challenge verification context. Not billable. | No follow-up required. |
| 2026-05-20 | https://github.com/hashicorp/vault | security:config_taint_* × 4 (`ui/app/routes/vault/cluster/oidc-callback.js`, `ui/app/components/auth/form/base.ts`) | 3 | Client-side Vue/TS OIDC callback and form state. `lattice_gap_proposal` correctly assigned. No SSR path proven. | No follow-up required. |
| 2026-05-20 | https://github.com/stripe/stripe-node | security:oauth_account_fusion_pretakeover × 18 (`src/resources/AccountLinks.ts`, `src/resources/Accounts.ts`, etc.) | 0 | FP: stripe-node is a client SDK — `account_link` is an API method definition that calls the Stripe server API. There is no server-side OAuth account fusion logic in the Node.js SDK itself. The SDK does not handle OAuth callbacks. Threat Model: TypeScript client SDK without SSR path → 0%. | Add a library-SDK classifier that suppresses `oauth_account_fusion_pretakeover` in files under `src/resources/` where the file exports only API method wrappers without authentication flow logic. |
| 2026-05-20 | https://github.com/stripe/stripe-node | security:oauth_missing_state_validation × 9 (`src/resources/OAuth.ts`, etc.) | 0 | TypeScript SDK files — `classify_oauth_state_validation_proof` correctly returns `LatticeGapProposal` (.ts extension not in server-side list). stripe-node OAuth.ts handles the OAuth flow client-side for Node.js server-side usage, but the `state` parameter is documented as caller's responsibility. Without proof of a stripe-node-specific gap, this is LOW_YIELD. | If a server-side Node.js OAuth integration framework built on stripe-node is found that handles callbacks without state validation, re-evaluate at 25%. |
| 2026-05-20 | https://github.com/stripe/stripe-node | security:embedding_trust_transposition × 50+ (`src/resources/`) | 0 | TypeScript SDK resource definitions — no vector store present. FP class identical to teleport pattern. | Pending vector-store import guard (same fix as teleport path). |
| 2026-05-20 | https://github.com/stripe/stripe-node | security:parser_exhaustion_anomaly × 3 (`src/resources/Events.ts`, etc.) | 3 | Informational: large TypeScript resource definitions trigger parser size heuristic. Not exploitable without a specific payload path. | No follow-up required. |
| 2026-05-20 | https://github.com/stripe/stripe-node | security:model_weight_backdoor × 1 (`src/resources/Checkout/Sessions.ts:1`) | 0 | FP: TypeScript Checkout resource definition misidentified as ML model weight. Same false-positive class as teleport database.go. | Pending Go/TypeScript resource file path guard (same fix). |
| 2026-05-20 | https://github.com/pinterest/querybook | security:react_xss_dangerous_html × 6 sites (`StatementLog.tsx:106`, `SearchResultItem.tsx:58,171,287,388,494`) | 0 | Phase 5 Docker PoC: `DOMPurify.sanitize()` confirmed at all 6 `dangerouslySetInnerHTML` sinks — `StatementLog.tsx:107` and `SearchResultItem.tsx:59,172,288,389,495`. DOMPurify imported at file top in both files. Sanitization prevents XSS. Structural eradication guard added to `apply_phase2b_suppression` in `hunt.rs`: suppresses `react_xss_dangerous_html` when file-level `DOMPurify.sanitize(` present. | Engine FP class eradicated. No further manual testing required. Re-run `janitor hunt /tmp/querybook-xss` to confirm finding is absent. |
| 2026-05-20 | https://github.com/newrelic/newrelic-python-agent | security:llm_prompt_injection (`newrelic/hooks/mlmodel_openai.py:1`) | 5 | The `mlmodel_openai.py` file is an instrumentation hook for observability telemetry — it wraps OpenAI SDK calls to capture traces but does not control LLM inputs or outputs. Threat Model Awareness: hook monitors calls without taint source authority; no attacker-controlled prompt injection path proven. | Add instrumentation-hook path classifier so APM hook files (`hooks/`, `instrumentation/`, `middleware/telemetry`) are demoted from llm_prompt_injection unless the file directly constructs or modifies LLM API request bodies. |
| 2026-05-20 | https://github.com/openai/codex | security:ffi_unsafe_deref_unguarded (`codex-rs/windows-sandbox-rs/src/token.rs:212`) | 5 | Windows sandbox WinAPI raw pointer operation (`TOKEN_USER` struct read via `GetTokenInformation`); platform-mandated unsafe call in sandboxing infra. No externally attacker-controlled path to this deref was proven — precondition requires local Windows process injection or token manipulation privileges. Threat Model: requires prior local code execution. | Re-evaluate after P17-3A `security:ffi_unsafe_deref_unguarded` proof-obligation cure lands with interprocedural attacker-input tracking through WinAPI call chains. |
| 2026-05-06 | https://github.com/freedomofpress/securedrop | security:subprocess_shell_injection (`devops/scripts/verify-mo.py:112`) | 5 | The sink lives in a local verification script and no attacker-controlled remote input or production invocation path was proven, so Threat Model Awareness forces low-yield routing. | Extend the command-execution classifier so local verification and release-helper scripts are demoted unless a networked ingress or shipped runtime path is proven. |
| 2026-05-06 | https://github.com/freedomofpress/securedrop-client | security:subprocess_shell_injection (`client/scripts/verify-mo.py:112`) | 5 | The sink is confined to a local verification script and no remote attacker path or production invocation surface was proven. | Extend the command-execution classifier so local verification and release-helper scripts are demoted unless a networked ingress or shipped runtime path is proven. |
| 2026-05-06 | https://github.com/smartcontractkit/chainlink | security:command_injection_shell_exec (`core/scripts/chaincli/handler/verify.go:44`) | 5 | The sink sits in CLI/script handler code and no remote ingress or attacker-controlled runtime surface was proven; under Threat Model Awareness it is not remotely exploitable yet. | Add reachability classification that demotes script-only command-execution sinks unless a network/API entrypoint is proven. |
| 2026-05-06 | https://github.com/smartcontractkit/chainlink | security:embedding_trust_transposition (`core/cmd/shell.go:665`) | 5 | A vector-query shape was detected, but no trusted-policy corpus, no RAG answer sink, and no attacker-controlled retrieval path were proven, so this remains architectural telemetry rather than a bounty. | Tighten the detector to require visible answer synthesis or tool-use on retrieved chunks before reporting production RAG trust transposition. |
| 2026-05-06 | https://github.com/smartcontractkit/chainlink | security:tls_verification_bypass (`core/scripts/keystone/src/99_app.go`, `deployment/environment/nodeclient/chainlink.go`) | 5 | The configuration appears in deployment/test-adjacent tooling and no supported production threat boundary was proven; current evidence terminates at local or operator-controlled environments. | Add path and deployment-surface guards that distinguish production clients from bootstrap, test, or operator-only tooling. |
| 2026-05-06 | https://github.com/smartcontractkit/chainlink-docs | security:dom_xss_innerHTML (vendored `source/javascripts/lib/_jquery.js`) | 5 | Sink is inside vendored jQuery and the Acceptance Oracle explicitly reports missing `schema_taint:proven`; no repository-native reflection path or server-controlled data source was proven. | Add vendored-library suppression and require repository-native reflection evidence before emitting DOM XSS candidates from documentation sites. |
| 2026-05-06 | https://github.com/fireblocks/mpc-lib | security:lcm_double_free | 5 | Static LCM pattern emitted only manual pentester notes; no route, attacker-controlled input, or concrete repro_cmd was proven. | Add bounded C ownership-lifetime witness synthesis for paired free paths before bounty escalation. |
| 2026-05-06 | https://github.com/fireblocks/mpc-lib | security:lcm_use_after_free | 5 | Static LCM pattern emitted only manual pentester notes; no route, attacker-controlled input, or concrete repro_cmd was proven. | Add interprocedural C lifetime graph proof that demonstrates reachable post-free dereference from public parser/API input. |
| 2026-05-06 | https://github.com/fireblocks/mpc-lib | security:lcm_malloc_integer_truncation | 5 | Static LCM pattern emitted only manual pentester notes; allocation size controllability was not proven. | Add Z3-backed allocation-width model that extracts attacker-controlled length bounds from C arithmetic before reporting. |
| 2026-05-06 | https://github.com/fireblocks/mpc-lib | security:lcm_off_by_one_loop | 5 | Static LCM pattern emitted only manual pentester notes; no input-dependent loop bound or crash proof was generated. | Add C loop-bound witness generation with sanitizer-aware range proof and deterministic crash harness synthesis. |
| 2026-05-06 | https://github.com/electroneum/electroneum-sc | security:ssrf_dynamic_url (`cmd/etn-sc/version_check.go:118`) | 5 | The Smartchain program pays only for outcomes that threaten chain integrity, user funds, private keys, or exchange/block-explorer truthfulness; the current SSRF witness proves only a request primitive, not any eligible blockchain impact. | Extend the blockchain hunt lane so Smartchain findings are suppressed unless the witness reaches chain-state mutation, validator consensus control, wallet key material, or explorer/exchange display integrity. |
| 2026-05-06 | https://github.com/electroneum/electroneum | security:ssrf_dynamic_url (`gui/migration-tool/electron/main.js:339`) | 5 | The Legacy Blockchain program automatically rejects non-chain-integrity classes; this SSRF path terminates in tooling without a demonstrated bridge exploit, unauthorized mint, token theft, accepted historical rewrite, or comparable integrity break. | Add program-aware payout gating that demotes blockchain-adjacent SSRF unless the proof binds the request primitive to bridge abuse, fund theft, consensus integrity loss, or accepted ledger tampering. |
| 2026-05-06 | https://github.com/trustwallet/wallet-core | security:protobuf_any_type_field (`rust/tw_cosmos_sdk/Protobuf/authz_tx.proto:11`) | 5 | The detector proved only the presence of `google.protobuf.Any`; no reachable decode path, no type-url allowlist bypass, and no production wallet flow consuming attacker-controlled Any envelopes was established, so Binance's working-PoC requirement is not met. | Add protobuf Any reachability analysis that traces public decode entrypoints through type-url dispatch and emits only when an attacker-controlled envelope can reach an unpack or authorization sink without an allowlist. |
| 2026-05-06 | https://github.com/ClickHouse/ClickHouse | security:os_command_injection (`ci/praktika/execution/machine_init.py:103`) | 5 | The sink terminates in CI runner teardown logic rather than the in-scope `clickhouse-server` component, and the program explicitly rejects static-only or non-server proofs that do not reproduce on supported release builds. | Extend the command-execution classifier so CI bootstrap, runner lifecycle, and release-helper paths are demoted unless a public server input can reach the shell sink on an official ClickHouse release build. |
| 2026-05-06 | https://github.com/afterpay/sdk-ios | security:unpinned_asset (`Sources/Afterpay/Model/Environment.swift:23`) | 5 | The only concrete drift is the sandbox `afterpay.github.io/sdk-example-server/widget-bootstrap.js` bootstrap URL; no production exploit path, asset compromise proof, or customer-impact chain was established, so Block's proof-of-exploitability bar is not met. | Strengthen unpinned-asset triage so sandbox-only bootstrap hosts and demo flows are demoted unless the witness proves production asset substitution, integrity bypass, or customer-impacting script control. |
| 2026-05-07 | https://github.com/cashapp/hermit | security:unpinned_asset (`cmd/geninstaller/install.sh.tmpl:117`, `files/install.sh.tmpl:117`) | 5 | The hunt surfaced two installer-template asset fetches, but the generated witness still terminates at a placeholder `<remote-url>` and does not prove a concrete mutable production asset, digest drift, or customer-impacting substitution path. | Strengthen unpinned-asset extraction so shell-template URLs are concretized into immutable repro commands and emit only when production install paths lack digest pinning for a live remote artifact. |
| 2026-05-08 | https://github.com/Uniswap/docs | no_findings | 0 | Documentation-only repository (MDX/TypeScript docs site); no security-relevant source-to-sink paths, credential material, or unsafe execution patterns found. The immunefi Uniswap program targets smart contracts, not the docs site. | No R&D follow-up required — route to smart-contract targets in the immunefi/Uniswap scope instead. |
| 2026-05-08 | https://github.com/aave/aave-address-book | no_findings | 0 | Address-book data repository (TypeScript/JSON contract address registry); no source-to-sink taint paths, credential material, or injection surfaces found. Aave immunefi program covers smart contracts and protocol endpoints, not the address-book package itself. | No R&D follow-up required — route future aave hunts to core protocol or vault contract repos. |
| 2026-05-08 | https://github.com/immutable/ts-immutable-sdk | security:ssrf_dynamic_url (client-side: `packages/auth/src/Auth.ts:677`, `packages/checkout/sdk/src/availability/availability.ts:25`, `packages/checkout/widgets-lib/src/components/Transak/useTransakIframe.ts:106`, `packages/internal/bridge/sdk/src/lib/gmpRecovery.ts:35`, `packages/wallet/src/zkEvm/relayerClient.ts:136`) | 5 | Threat Model pre-filter: all five sinks are in client-side TypeScript (browser execution context); `fetch()`/`axios()` calls in browser bundles are blocked by SOP/CORS and cannot reach internal cloud metadata services without a server-side execution path. No Next.js API route or SSR path was proven for these files. | Add server-vs-client execution context classification to the SSRF demoting rules so browser-executed SDK modules are automatically routed to LOW_YIELD without analyst review. |
| 2026-05-08 | https://github.com/immutable/ts-immutable-sdk | security:dom_xss_innerHTML (`packages/auth/src/overlay/embeddedLoginPromptOverlay.ts:25`) | 5 | Acceptance Oracle flagged missing `schema_taint:proven` server-reflection proof; the overlay function returns a fixed HTML string with no user-controlled input path reaching the innerHTML sink; Self-XSS scenario only. | Tighten the embedded-overlay helper guard (already partially implemented in slop_hunter.rs) to suppress fixed-content overlay helpers with zero external parameters. |
| 2026-05-08 | https://github.com/mattermost/mattermost | security:prototype_pollution_merge_sink (`webapp/channels/src/packages/mattermost-redux/src/actions/search.ts:161`, `:236`) | 5 | The sink is inside a client-side Redux action (browser JS bundle); object-merge operations in Redux reducers are blocked by SOP and cannot be leveraged by an external attacker without pre-existing XSS. Self-prototype-pollution without a chained gadget is not independently exploitable at Mattermost's bounty threshold. | Add a Redux-action context guard to the prototype_pollution_merge_sink detector so client-side Redux action files are automatically demoted. |
| 2026-05-08 | https://github.com/transferwise/pipelinewise | security:model_weight_backdoor (`pipelinewise/fastsync/commons/target_snowflake.py:1`, `singer-connectors/target-snowflake/target_snowflake/__init__.py:1` ×5 more) | 5 | FALSE POSITIVE — `load_file()` in S3/Snowflake ETL data-pipeline code triggers the ML weight backdoor detector; no ML model weight loading, adapter, or safetensors path present in this ETL framework. | Add `load_file` context guard to `detect_model_weight_backdoor` that requires co-presence of ML library imports (torch, safetensors, transformers, peft) before emitting; suppress findings in files that import cloud storage clients (boto3, snowflake.connector) without ML imports. |
| 2026-05-08 | https://github.com/ProjectOpenSea/seaport | no_findings | 0 | Solidity NFT marketplace contracts; no security-relevant source-to-sink paths detected in current scanner scope. Solidity taint analysis not implemented. | Route future OpenSea hunts to any server-side API or backend infrastructure; add Solidity REENTRANCY detector to the hunt lane. |
| 2026-05-08 | https://github.com/IABTechLab/uid2-web-integrations | no_findings | 0 | UID2 web integration SDK (JavaScript/TypeScript); no security-relevant taint paths or injection sinks detected. | Route future IAB TechLab hunts to UID2 Operator server-side components. |
| 2026-05-08 | https://github.com/immutable/ts-immutable-sdk | security:ssrf_dynamic_url (`packages/auth-next-server/src/config.ts:38`) — P2-17 DEMOTION | 5 | P2-17 config-backed SSRF demotion: `config.ts` is a Next.js configuration module; URL at line 38 originates from operator-supplied environment configuration, not from attacker-controlled HTTP request input. Previously in CANDIDATE at 40%; P2-17 post-filter correctly reclassifies to Informational. | No follow-up required — P2-17 correctly demotes operator-config SSRF. If an attacker-controlled input path is ever proven (e.g., query-param injection into the config loading), re-evaluate as CANDIDATE. |
| 2026-05-08 | https://github.com/mattermost/mattermost-plugin-boards | security:ssrf_dynamic_url (`webapp/src/octoClient.ts` ×45) | 5 | Client-side TypeScript API client (WebSocket + REST); all 45 SSRF sinks are in browser-executed code blocked by SOP/CORS. P2-17 path guard does not fire (octoClient is not a config module), but client-side execution context makes these non-exploitable by a remote attacker. | Add execution-context guard to SSRF demoting rules so browser WebSocket/API client files (matching `*Client.ts`, `*client.ts`, `webapp/src/**`) are automatically demoted without config-module path matching. |
| 2026-05-08 | https://github.com/mattermost/mattermost | security:git_ref_dependency (`webapp/channels/package.json` — `marked` at commit SHA `e4a8785014b26ba9f637c1fdab23e340961c6a03`) | 2 | Pinned to immutable commit SHA — this IS correct supply-chain hygiene, not an unpinned dependency. Detector false-positive on `github:<org>/<repo>#<sha>` form. | Add a git-ref validator that verifies the ref length (40 hex chars = commit SHA, not a branch name) and suppresses findings where the ref is a full 40-character SHA. |
| 2026-05-08 | https://github.com/mattermost/mattermost | security:ics_default_credential (`api/v4/source/introduction.yaml:79`) | 2 | YAML file is OpenAPI documentation for the Mattermost API — not a deployed configuration with active credentials. Documentation-only files must be excluded from ICS credential scanning. | Add path guard: suppress `security:ics_default_credential` when file path matches `**/docs/**`, `**/source/**`, `**/spec/**`, or ends in `introduction.yaml`. |
| 2026-05-08 | https://github.com/mattermost/mattermost | security:model_weight_backdoor (`webapp/channels/src/components/common/svg_images_components/guest_access_svg.tsx`, `upgrade_svg.tsx`, `webapp/channels/src/utils/states.ts`, `server/public/plugin/checker/internal/version/version.go`) | 2 | False positive — static SVG image components and utility TypeScript files are not ML model weight files. The model_weight_backdoor detector incorrectly fires on TypeScript/TSX source files unrelated to ML inference. | Add file-type guard: suppress `security:model_weight_backdoor` on `.tsx`, `.ts`, `.js`, `.jsx`, `.svg` files and on `version.go` unless the file imports a known ML inference library. |
| 2026-05-08 | https://github.com/mattermost/mattermost | security:eval_injection (`server/scripts/ldap-check.sh:88`) | 2 | Shell script in `scripts/` directory — deployment/admin utility, not remotely reachable from the public network. `is_ci_or_local_script_path` should demote this. | Verify that `server/scripts/` is correctly caught by `is_ci_or_local_script_path` path guard. If not, add `server` segment prefix exclusion so `server/scripts/` does not match as a production server path. |
| 2026-05-08 | https://github.com/mattermost/mattermost | security:ssrf_dynamic_url (operator-scope sinks: `server/channels/app/slashcommands/command_loadtest.go:642`, `server/cmd/mmctl/commands/auth.go:175`, `server/platform/services/upgrader/upgrader_linux.go:254`, `server/platform/shared/mail/inbucket.go:59`, `server/channels/app/security_update_check.go:90`, `server/channels/app/cloud.go:53`, `server/channels/app/platform/license.go:355`, client-side: `webapp/channels/src/components/admin_console/workspace-optimization/dashboard_checks/data_privacy.ts:37`, `updates.ts:44`) | 5 | Loadtest slash command and mmctl CLI require System Admin privileges (threat model: admin privilege = operator-controlled); upgrader URL is operator-supplied config; inbucket is a test mail server (not production); security_update_check and cloud/license URLs are hardcoded internal calls (no attacker-controlled input); admin_console TypeScript files are client-side. | Extend the command-execution and SSRF classifiers to demote: (1) CLI tool paths (mmctl, scripts), (2) upgrader service URLs tied to admin config, (3) internal update-check calls without external taint source, (4) test mail adapters (inbucket). |
| 2026-05-09 | https://github.com/openai/codex | security:raw_pointer_deref (`codex-rs/config/src/loader/mod.rs:557`) | 5 | Config loader reads local filesystem state; exploitability requires control of the user's local config file (local write access). Threat Model pre-filter: local config modification → Approval % < 10%. | P2-7 AST dominance now emits `security:public_ffi_unsafe_deref` for pub-fn raw pointer dereferences. Re-hunt after P2-15 C ownership proof suite lands if a network-facing config parse path is identified. |
| 2026-05-09 | https://github.com/openai/codex | security:raw_pointer_deref (`codex-rs/windows-sandbox-rs/src/winutil.rs:116`) | 5 | Windows sandbox utility; WinAPI raw pointer operations in sandbox helpers are unavoidable platform calls. No externally attacker-controlled input path to the unsafe deref was proven — requires local sandbox escape precondition. | Re-evaluate after P2-15 C ownership proof suite lands with interprocedural attacker-input tracking through WinAPI call chains. |
| 2026-05-09 | https://github.com/cashapp/misk | security:protobuf_any_type_field (`misk-proto/src/main/proto/misk/status.proto:91`) | 35 | P2-16 AST dominance shipped (Go files now proved for unguarded decode). Misk is primarily Kotlin/JVM — no Go decode consumers found in shallow clone; Java/Kotlin UnmarshalAny path requires JVM analysis beyond current engine (P2-16 covers Go only). | Extend `find_protobuf_any_reachability` to cover Kotlin/Java `DynamicMessageMarshaller` and protobuf-java `Any.unpack()` patterns. Re-hunt misk after Java/Kotlin coverage lands. |
| 2026-05-09 | https://github.com/ClickHouse/ClickHouse | security:dom_xss_innerHTML (`utils/merge-selector-lab/renderMath.js:52`) | 5 | Utility script under `utils/` — not part of the in-scope `clickhouse-server` binary. ClickHouse HackerOne program requires server-side impact on supported release builds; JS utils in the repo do not qualify. | Add scope-based suppression for `utils/` directory paths in programs that restrict scope to the server binary. |
| 2026-05-09 | https://github.com/smartcontractkit/chainlink | security:unpinned_asset (`deployment/ccip/shared/bindings/usd_stablecoin/usd_stablecoin_metadata.go:7`) | 5 | Triggered on a doc URL inside a massive JSON-stringified Solidity StandardInput blob (>2KB string literal). The URL is not a runtime fetch target — it is ABI/metadata embedded in a Go generated binding file. | Extend asset detector to suppress URLs inside string literals > 2KB. Phase 0 guard implemented in `should_ignore_supply_chain_match` using Go tree-sitter AST node byte-length check. |
| 2026-05-09 | https://github.com/aave/aave-v3-core | no_findings | 0 | Net-new org hunt (Sprint 135). Solidity/TypeScript DeFi core protocol. No Python/Go RAG dataflow patterns, no C memory management code, no credential or supply-chain sinks found in shallow clone. TypeChain bindings are auto-generated. | Re-hunt after P5-1 ZK attestation support or if RAG/Python tooling is detected in adjacent Aave subgraph/governance repos. |
| 2026-05-12 | https://github.com/aave/aave-v3-core | no_findings | 0 | Re-hunt on local clone after Pages/security remediation and dependency refresh. The current engine again found no source-to-sink taint paths, credential material, or remotely actionable supply-chain sinks; the repo remains primarily Solidity/TypeScript contract code outside the strongest current hunt lanes. | Hold in LOW_YIELD until a Solidity-native exploit witness lane or protocol-backend target appears; do not spend analyst time on repeated shallow-contract scans. |
| 2026-05-12 | https://github.com/Uniswap/v4-core | no_findings | 0 | Re-hunt on local clone. Smart-contract core repository stayed clean under the current detector mix; no server-side ingress, credential, or artifact-fetch surfaces were present for autonomous exploitation proof generation. | Route future Uniswap effort toward backend, bridge, or docs/runtime repos, or re-hunt only after Solidity-specific invariant or exploit-witness detectors land. |
| 2026-05-13 | https://github.com/Uniswap/v3-core | no_findings | 0 | Smart-contract core repository produced no Janitor findings. Current strongest lanes are SaaS/API/AI-copilot surfaces; Solidity-native invariant and exploit-witness coverage is not yet implemented for v3-core. | Re-hunt only after a Solidity-native invariant witness lane lands, or pivot Uniswap coverage to backend, bridge, governance, or indexer services. |
| 2026-05-13 | https://github.com/mattermost/mattermost | security:vector_filter_polymorphism false-positive class | 0 | First Mattermost pass exposed over-broad `.query(` matching in non-vector Go request handlers. The detector was tightened to require vector-store-specific query markers before predicate-polymorphism emission. | No bounty action. Preserve as negative training data for P2-15 precision and add future language-specific vector client markers deliberately. |
| 2026-05-13 | https://github.com/mattermost/mattermost | security:prototype_pollution_merge_sink (`webapp/channels/src/packages/mattermost-redux/src/actions/search.ts`) | 5 | Client-side Redux action path remains browser-local without a proven remote gadget or independent privilege boundary. | Keep Redux-action context demotion; promote only if a stored cross-user object merge gadget is proven. |
| 2026-05-13 | https://github.com/mattermost/mattermost | security:unpinned_git_dependency (`webapp/channels/package.json`) | 2 | Findings resolve to full commit SHA git refs, which are immutable pinning rather than branch-based dependency drift. | Implement git-ref-length validation so 40-character hex commit refs are suppressed before ledger routing. |
| 2026-05-13 | https://github.com/mattermost/mattermost | security:ics_default_credential (`api/v4/source/introduction.yaml:79`) | 2 | OpenAPI/source documentation path, not deployed runtime configuration. | Suppress default-credential findings under source/spec/documentation paths unless a deployed config loader consumes the file. |
| 2026-05-13 | https://github.com/mattermost/mattermost | security:model_weight_backdoor (static TSX/version utility paths) | 2 | Static SVG, TSX, and version files are not ML model or adapter loading surfaces. | Require ML framework imports or model-file extensions before emitting model-weight backdoor findings. |
| 2026-05-13 | https://github.com/mattermost/mattermost | security:eval_injection (`server/scripts/ldap-check.sh:88`) | 2 | Script/admin utility path with no remotely reachable production ingress. | Extend local-script suppression to `server/scripts/` before report generation. |
| 2026-05-14 | https://github.com/skroutz/rspecq | no_findings | 0 | No exploitable issues found in RSpec distributed test runner source. | No follow-up required. |
| 2026-05-19 | https://github.com/mattermost/mattermost-plugin-ai | security:config_taint_fileInput.files (`webapp/src/components/rhs/rhs_file_drop_zone.tsx`) | 3 | Client-side React file-drop zone; `fileInput.files` is populated by the user's own drag-and-drop action, not by remote attacker-controlled input. Threat Model pre-filter: self-XSS only, no remote exploitation path. | Add file-drop component heuristic to config_taint classifier so client-side file-picker inputs are auto-demoted. |
| 2026-05-19 | https://github.com/mattermost/mattermost-plugin-ai | security:oauth_missing_state_validation (`mcp/dcrp.go`) — FALSE POSITIVE | 0 | `dcrp.go` is the Dynamic Client Registration Protocol handler (client registration, not OAuth callback). The actual OAuth callback at `api/api_oauth.go:77` validates empty state immediately, and `mcp/oauth_manager.go:284-286` enforces `session.State != state` comparison. State IS validated end-to-end. | Tighten `oauth_missing_state_validation` to require that the triggering file contains an actual HTTP callback handler (ServeHTTP, gin.Context, http.HandlerFunc) before emitting; client-registration protocol files should be excluded. |
| 2026-05-19 | https://github.com/freedomofpress/securedrop-client | security:llm_model_unverified_load (`app/src/main/crypto.ts:467`) — likely FALSE POSITIVE | 3 | SecureDrop client does not load LLM model weights; `crypto.ts` is a cryptographic utility module with no ML inference imports. The detector appears to have matched a false pattern in the cryptographic code. | Add import-presence guard to `llm_model_unverified_load`: require co-presence of an ML framework import (torch, transformers, gguf, safetensors) before emitting. |
| 2026-05-19 | https://github.com/freedomofpress/securedrop-client | security:config_taint_* (×6 config reads in `app/src/main/database/index.ts`, `client/securedrop_client/storage.py`, `client/securedrop_client/config.py`, `app/src/main/export.ts`) | 3 | All taint paths originate in local database reads and local config files within the QubesOS VM. No remote attacker-controlled input source proven. Threat Model: local config modification — Approval % < 10%. | No follow-up required; existing candidate entries for the qdb FFI path capture the highest-yield surface in this repo. |
| 2026-05-20 | https://github.com/ethereum/go-ethereum | security:non_constant_time_comparison (`crypto/ecies/ecies.go:254,319`) — **Sprint 150 structural eradication FP** | 0 | `classify_timing_comparison_proof` now suppresses via `InvariantViolationProof`: `subtle.ConstantTimeCompare` guard is present at line 322 (MAC authentication tag comparison). The detector fired on MAC derivation context (`d := messageTag(...)`) at :254/:319 rather than the comparison itself. Structural eradication fix landed in Sprint 150 — finding will no longer appear on re-hunt. | No bounty action. Structural guard in `classify_timing_comparison_proof` (±10-line window for `subtle.ConstantTimeCompare`/`hmac.Equal(`) correctly suppresses this FP class. |
| 2026-05-20 | https://github.com/ethereum/go-ethereum | security:jwt_validation_bypass (`node/jwt_handler.go:60`) — **Sprint 150 structural eradication FP** | 0 | `jwt_keyfunc_oracle` Pattern 4 now suppresses via `Guarded`: `jwt.WithValidMethods([]string{"HS256"})` at line 65 pins the algorithm set, blocking `alg:none` bypass at the library level. Finding will no longer appear on re-hunt after Pattern 4 landed in Sprint 150. | No bounty action. JWT keyfunc oracle Pattern 4 (`WithValidMethods(`) covers the go-ethereum/golang-jwt v5 pattern. |
| 2026-05-20 | https://github.com/ethereum/go-ethereum | security:lcm_double_free (`crypto/secp256k1/libsecp256k1/src/tests.c:350`), security:lcm_malloc_integer_truncation (`bench_ecmult.c:316–321`, `schnorrsig/bench_impl.h:54–57`, `precompute_ecmult.c:45–46` ×12 total), security:lcm_off_by_one_loop (`ecmult_impl.h:456`, `tests_exhaustive_impl.h:83`, `tests.c:4075,5670`), security:parser_exhaustion_anomaly (`precomputed_ecmult_gen.c:1`) | 3 | All 18 LCM-family + parser sinks land in benchmark drivers (`bench_ecmult.c`, `schnorrsig/bench_impl.h`), test harnesses (`tests.c`, `tests_exhaustive_impl.h`), and auto-generated precomputed-constants files (`precomputed_ecmult_gen.c`). `classify_lcm_double_free_proof` routes test-path findings to `LatticeGapProposal` (not `ReachabilityProof`). No production mobile caller path proven. Threat Model: no attacker-controlled input reaches secp256k1 bench/test entry points from a wallet API boundary. | Promotion to CANDIDATE requires proving a mobile caller path (iOS Swift/Android JNI → wallet API → production secp256k1 C boundary) reaching these bench/test paths — architecturally impossible given that bench files are compile-excluded from library builds. Preserve as evidence that LCM proof cure is needed for production paths (`scrypt.c:334,336` → TrustWallet CANDIDATE remains the live surface). |
| 2026-05-20 | https://github.com/ethereum/go-ethereum | security:non_constant_time_comparison (`internal/jsre/deps/web3.js:9286,10788,10939,11020,11220,11544,11628,11952` ×8; `p2p/discover/v5wire/crypto.go:116,117`, `encoding.go:415,571` ×4) | 3 | Eight sinks are in vendored web3.js (external library, not go-ethereum production Go code). Four sinks in `p2p/discover/v5wire/` are in the DevP2P discovery AEAD handshake implementation; timing oracle on HKDF-derived session material requires a network-position precondition (active MitM on peer discovery UDP). Program scope for go-ethereum bug bounty requires network-accessible, user-fund-threatening impact — timing oracle on UDP peer discovery does not meet the threshold without a demonstrated key-material recovery proof. | Add vendored-JS suppression to `non_constant_time_comparison` detector. Re-evaluate `p2p/discover/v5wire/` after P17-3A timing comparison proof suite can distinguish AEAD nonce comparison (constant time by AEAD spec) from HMAC tag comparison (genuinely vulnerable class). |
| 2026-05-20 | https://github.com/ethereum/go-ethereum | security:ssrf_dynamic_url (`internal/download/download.go:192`), security:ics_default_credential (`internal/jsre/deps/web3.js:7426,7427,7475,7476,7511,7512` ×6), security:protobuf_any_unguarded_decode (`accounts/usbwallet/trezor.go:361,372`), security:unpinned_asset (`params/config.go:696`), security:config_taint_* (`cmd/clef/pythonsigner.py` ×2) | 3 | SSRF at `download.go:192`: URL is the go-ethereum EIP-list fetch — operator-configured, not attacker-controlled. ICS credentials: vendored web3.js strings — not deployed configuration. Protobuf Any at Trezor USB wallet: requires physical hardware device; no remote attacker path. Unpinned asset at `params/config.go:696`: genesis config metadata field, not a runtime asset fetch. Config taint at `clef/pythonsigner.py`: Python signer plugin helper (Clef external signer protocol), no network ingress. All sinks fail Threat Model pre-filter: no remote attacker-controlled input proven. | Add Clef-plugin path suppression to `config_taint` detector. Extend protobuf_any classifier to require network-facing decode entrypoint (USB/HID hardware paths demoted). |
| 2026-05-20 | https://github.com/supabase/supabase | security:react_xss_dangerous_html (`apps/studio/components/interfaces/Realtime/Inspector/SelectedRealtimeMessagePanel.tsx:30`, `apps/studio/components/interfaces/Reports/renderers/ApiRenderers.tsx:385`, `apps/ui-library/registry/default/components/ui/chart.tsx:75`, `apps/www/app/blog/[slug]/page.tsx:184,188`, `apps/www/app/blog/page.tsx:47`, `apps/www/app/events/page.tsx:23`, `apps/www/app/pricing/page.tsx:34`, `apps/www/pages/auth.tsx:63,76`, `apps/www/pages/careers.tsx:118`, `apps/www/pages/company.tsx:48`, `apps/www/pages/customers.tsx:104`, `apps/www/pages/customers/[slug].tsx:125`, `apps/www/pages/database.tsx:81,94`, `apps/www/pages/edge-functions.tsx:55,68`, `apps/www/pages/events/[slug].tsx:230`, `apps/www/pages/features.tsx:127`, `apps/www/pages/index.tsx:30` ×19+ total) | 3 | **Structural sanitizer guard blocks exploitation across all sites.** `SelectedRealtimeMessagePanel.tsx:30` feeds through `jsonSyntaxHighlight` (`MessagesFormatters.tsx:57`) which HTML-escapes `&`, `<`, `>` via `.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;')` before setting `innerHTML`. Marketing and blog pages use CMS-rendered sanitized content. No unsanitized attacker-controlled string proven to reach `dangerouslySetInnerHTML`. Supabase bug bounty requires working PoC — no PoC possible against sanitized content. | Tighten `react_xss_dangerous_html` to detect `jsonSyntaxHighlight` and analogous HTML-escape wrappers (escape sequences `&amp;`, `&lt;`, `&gt;` in the feed pipeline) and emit `security:react_xss_sanitized_bypass` at Informational rather than a false exploitable finding. |
| 2026-05-20 | https://github.com/supabase/supabase | security:ssrf_dynamic_url (`apps/studio/components/interfaces/SQLEditor/SQLEditor.tsx:679`, `apps/studio/data/database-integrations/stripe/stripe-sync-install-mutation.ts:29,27`, `apps/studio/data/oauth-server-apps/oauth-openid-configuration-query.ts:40`, `apps/studio/data/platform/incident-banner-query.ts:21`, `apps/studio/data/platform/incident-status-query.ts:12`, `apps/studio/data/support/generate-attachment-urls-mutation.ts:27`, `apps/studio/lib/api/self-hosted/query.ts:37`, `apps/www/app/llms-full.txt/route.ts:48`, `apps/www/components/LaunchWeek/15/utils/github-api.ts:8`, `apps/www/lib/notion.ts:25,50,68`, `apps/www/supabase/functions/hubspot-notification/index.ts:89`, `packages/marketing/src/crm/hubspot.ts:77` ×15 total) | 3 | All 15 SSRF sinks fail Threat Model pre-filter: (1) Studio mutations use hardcoded Stripe/Supabase internal API base URLs — `dbId` or `orgId` come from authenticated session context, not from raw HTTP request input. (2) Notion URLs are `https://api.notion.com/v1/databases/${dbId}/query` with `dbId` from internal operator config, not attacker-controlled query params. (3) Hubspot edge function uses hardcoded `https://api.hsforms.com/…` with predefined `formId` allowlist. (4) `apps/www/` sinks are server-side Next.js routes but fetch from hardcoded internal service URLs. Client-side Studio components (mutations.ts) are additionally blocked by SOP/CORS. | Add strict attacker-input taint requirement to SSRF detector so operator-config URLs and hardcoded API host strings are suppressed. Extend the existing P2-17 `config-backed SSRF demotion` classifier to cover `notion.ts` and similar internal API integration patterns. |
| 2026-05-20 | https://github.com/supabase/supabase | security:dom_xss_innerHTML (`apps/www/components/LaunchWeek/15/LW15Heading.tsx:87`) | 3 | LaunchWeek marketing page heading component; the `innerHTML` content is derived from a static CMS string with no attacker-controlled interpolation path proven. Marketing pages are static-site-rendered with no user-input reflection. Supabase bug bounty requires demonstrated user-data exfiltration or privilege escalation — marketing heading DOM manipulation does not qualify. | Add static-content context guard to `dom_xss_innerHTML` suppressing sinks in marketing/landing-page components where no user-input taint path is proven (`LaunchWeek/`, `marketing/`, `www/pages/` paths with CMS-string sources). |
| 2026-05-20 | https://github.com/localstack/localstack | security:code_execution (`localstack-core/localstack/runtime/init.py:107`) | 3 | `exec(fd.read(), {})` is the intentional LocalStack init-hook system: it reads `.d`-directory hook scripts (`boot.d/`, `start.d/`, `ready.d/`, `shutdown.d/`) from the operator-mounted volume. This is documented operator functionality (`LOCALSTACK_VOLUME_DIR` mount). Exploiting this requires local filesystem write access to the mounted volume — equivalent to arbitrary code execution precondition already. Threat Model: no remote attacker-controlled input path proven. | No bounty action — LocalStack init hooks are documented extension points. Add a `is_operator_hook_system` guard to `code_execution` that suppresses `exec(fd.read())` calls in init/boot/startup runner modules with volume-dir path comments or imports. |
| 2026-05-20 | https://github.com/localstack/localstack | security:subprocess_shell_injection (`localstack-core/localstack/utils/run.py:37`) | 3 | `run()` utility function is called exclusively by internal LocalStack service modules (`java.py`, `docker_cmd_client.py`, `archives.py`). Commands are constructed by LocalStack internals from hardcoded templates — no HTTP request parameter or attacker-controlled input feeds the `cmd` argument. No public API endpoint calls `run()` with user-supplied shell arguments. Threat Model: no remote ingress proven. | Add caller-chain analysis to `subprocess_shell_injection` suppressing internal utility `run()` wrappers where no HTTP request or user-input taint source is proven to reach the `cmd` argument. |
| 2026-05-20 | https://github.com/localstack/localstack | security:ssrf_dynamic_url (`localstack-core/localstack/services/opensearch/cluster.py:70`), security:unpinned_asset (`dev/kubernetes/__main__.py:461`, `http/resources/swagger/templates/index.html:12`) | 3 | SSRF at `opensearch/cluster.py:70`: OpenSearch cluster URL built from operator-configured endpoint template (`LOCALSTACK_OPENSEARCH_ENDPOINT_STRATEGY`); attacker would need to control LocalStack config at startup — equivalent to local code execution precondition. Unpinned assets: `kubernetes/__main__.py` is dev-cluster bootstrap tooling (not shipped in the LocalStack container); Swagger UI template at `http/resources/swagger/templates/index.html:12` loads CDN-hosted Swagger UI JS — this is a developer documentation UI, not production API handler code. | Extend SSRF classifier to suppress operator-config endpoint templates when the URL origin is documented configuration variables. Add Swagger-template path guard to `unpinned_asset` suppressing CDN references in documentation-only HTML templates. |
| 2026-05-20 | https://github.com/localstack/localstack | security:config_taint_* (100+ entries across `services/`, `utils/`, `aws/handlers/`) | 0 | Entire `config_taint_*` class in LocalStack is a false-positive flood: LocalStack is an AWS API emulator — every service handler reads from its internal service state (`context.account_id`, `context.region`, AWS API response structs) and passes this to downstream service calls or response builders. This is normal data flow for an AWS emulator, not attacker-controlled taint propagation. No finding traces from an external HTTP request input through a dangerous sink in a way that differs from expected emulator behavior. | Add LocalStack-specific emulator-context guard: suppress `config_taint_*` in files matching `localstack/services/**`, `localstack/aws/handlers/**` unless the taint source is an unauthenticated HTTP parameter not flowing through `RequestContext` authorization. This guard would eliminate ~100 FPs per hunt. |
| 2026-05-19 | https://github.com/aiven/aiven-client | security:config_taint_* (×12 arg/config reads in `aiven/client/cliarg.py`, `aiven/client/cli.py`) | 3 | All findings are in a Python CLI tool processing local argparse arguments and local config files. No remote attacker-controlled HTTP input path proven. Threat Model: CLI tool — all inputs are local shell arguments requiring local access. | Add CLI-tool path classifier so argparse/click/typer argument parsers in standalone CLI packages are auto-demoted unless a server ingress path is proven. |
| 2026-05-14 | https://github.com/Aiven-Open/karapace | security:jwt_validation_bypass (`src/karapace/api/oidc/middleware.py:91`) | 3 | False positive — code uses `_jwks_client.get_signing_key_from_jwt(token)` to fetch JWKS-validated key and passes explicit `algorithms=self.algorithms` list. Not an alg:none bypass. | Add JWKS-client pattern as a suppressor in jwt_validation_bypass detector: if `get_signing_key_from_jwt` is in ±5 lines, suppress finding. |
| 2026-05-14 | https://github.com/Aiven-Open/karapace | security:workflow_no_provenance (benchmarks/) | 2 | Benchmark scripts are not CI/scientific workflow outputs — they are test harnesses. Provenance envelope is not a Bugcrowd-billable finding class. | Suppress workflow_no_provenance for files under `benchmark*/` or `tests/` paths. |
| 2026-05-14 | https://github.com/Uniswap/v3-core | security:workflow_no_provenance (`contracts/UniswapV3Pool.sol`, `contracts/libraries/Oracle.sol`) | 2 | Solidity smart contract files do not emit scientific workflow outputs — no ML reproducibility envelope is applicable. | Suppress workflow_no_provenance for `.sol` extension files entirely. |
| 2026-05-15 | https://github.com/aave/aave-v3-periphery | no_findings | 0 | Solidity/TypeScript DeFi periphery contracts: no credential, deserialization, command injection, or SSRF sinks found. All workflow files SHA-pinned. | No follow-up. Preserve as clean negative sample for Solidity/DeFi hunt calibration. |
| 2026-05-15 | https://github.com/smartcontractkit/chainlink | security:command_injection_shell_exec (`core/scripts/chaincli/handler/verify.go:44`) | 2 | `exec.Command("bash", "-c", command)` is a CLI dev-tool script (`chaincli`); `command` is constructed entirely from local config values (`NodeHttpURL`, `ExplorerAPIKey`, `NetworkName`) — Threat Model: local config control ≠ remote exploit. | Extend local-script suppression to `scripts/` and `chaincli/` directories to prevent FP noise on developer tooling. |
| 2026-05-15 | https://github.com/mattermost/mattermost-plugin-ai | security:workflow_no_provenance (`webapp/src/components/**/*.tsx`, `webpack.config.js`) | 2 | TSX React component files and webpack config are not scientific workflow outputs — no ML reproducibility envelope applies. | Suppress workflow_no_provenance for `.tsx`, `.ts`, `.js`, `.jsx` extension files in non-workflow directories. |
| 2026-05-15 | https://github.com/immutable/ts-immutable-sdk | security:ssrf_dynamic_url (`packages/auth-next-server/src/config.ts:38`) | 3 | `authDomain` is developer-provided config (`resolvedConfig.authenticationDomain || DEFAULT_AUTH_DOMAIN`) — not attacker-controlled input. Threat Model: local config control ≠ remote exploit. | Add config-sourced URL suppressor: if URL source is resolved from a config object or constant default, suppress SSRF finding. |
| 2026-05-15 | https://github.com/immutable/ts-immutable-sdk | security:dom_xss_innerHTML (`packages/auth/src/overlay/embeddedLoginPromptOverlay.ts:25`) | 3 | `overlay.innerHTML = getEmbeddedLoginPromptOverlay()` — value is from a pure static template function, not user-controlled input. IFDS false positive on static template return values. | Suppress dom_xss_innerHTML when right-hand side is a function call with no user-input parameters in scope. |
| 2026-05-15 | https://github.com/openai/codex | security:unauthenticated_debug_endpoint (`scripts/start-codex-exec.sh:107`) | 2 | `./target/debug/codex exec-server --listen ws://127.0.0.1:0` binds exclusively to loopback — not a remotely reachable endpoint. | Suppress debug endpoint findings for WebSocket/HTTP sinks bound to `127.0.0.1` or `localhost` addresses. |
| 2026-05-15 | https://github.com/openai/codex | security:raw_pointer_deref (`codex-rs/config/src/loader/mod.rs:666`, `codex-rs/windows-sandbox-rs/src/winutil.rs:116`) | 2 | Both `unsafe` blocks have documented `// SAFETY:` comments and wrap Windows API guarantees (`SHGetKnownFolderPath`, `ConvertSidToStringSidW`). Sound unsafe usage. | Suppress raw_pointer_deref when a `// SAFETY:` comment appears within ±3 lines of the unsafe block. |
| 2026-05-15 | https://github.com/openai/codex | security:financial_pii_to_external_llm (`codex-cli/scripts/init_firewall.sh`, `run_in_container.sh`) | 2 | Shell scripts are not Python/TypeScript LLM SDK invocations — the PII detector matched firewall config strings incorrectly. | Add `.sh` extension exclusion to financial_pii detector: shell scripts are not LLM API callers. |
| 2026-05-15 | https://github.com/freedomofpress/securedrop-client | security:subprocess_shell_injection (`client/scripts/verify-mo.py:112`) | 2 | `subprocess.run(shell=True)` in a developer CI/diff script; command built from internal `diffoscope_args()` — not user-supplied. Has explicit `# noqa: S602` + `# nosemgrep` reviewed suppressions. | Extend subprocess_shell_injection suppressor: if `# noqa` or `# nosemgrep` comments within ±2 lines, treat as reviewed and suppress. |
| 2026-05-15 | https://github.com/trustwallet/wallet-core | security:lcm_off_by_one_loop (`trezor-crypto/crypto/aes/aes_modes.c` ×9 sites, `trezor-crypto/crypto/chacha_drbg.c`, `trezor-crypto/crypto/pbkdf2.c`) | 7 | Vendored trezor-crypto off-by-one patterns in AES CBC/CTR modes and KDF implementations. These are standard loop-boundary patterns in a mature, extensively audited crypto library (used by Trezor hardware wallets). Pattern-level detection without runtime evidence. | Wire legacy_c_mining off-by-one findings through a vendored-lib suppressor that demotes confidence when the file path contains `trezor-crypto/` or other major audited crypto subdirectory prefixes. |
| 2026-05-15 | https://github.com/binance/binance-connector-typescript | security:workflow_no_provenance (`src/modules/websocket/websocketBase/websocketBase.ts`) | 3 | TypeScript source file matched the workflow_no_provenance detector — the detector incorrectly fired on a `.ts` file instead of a `.yml` GitHub Actions workflow. False positive from path guard gap. | Add `.ts`, `.js`, `.py`, `.go` extension exclusion to workflow_no_provenance: detector must be gated to `.yml`/`.yaml` files with a `jobs:` key. |
| 2026-05-15 | https://github.com/bullish-exchange/api-docs | no findings | 0 | Docs-only repository (Markdown + OpenAPI specs). No executable code surfaces for any detector. | Consider adding a metadata-only detector for OpenAPI specs that checks for missing security schemes, open CORS wildcards, or unauthenticated endpoints documented in the spec. |
| 2026-05-15 | https://github.com/Uniswap/v3-periphery | no findings | 0 | Solidity peripheral contracts (`NonfungiblePositionManager.sol`, `SwapRouter.sol`). Engine's `solidity_taint` module is unwired (zero callers in `crates/cli/src/`) — no Solidity reentrancy/delegatecall detector active. All active AhoCorasick detectors found no patterns in `.sol` files. | Wire `crates/forge/src/solidity_taint.rs` into `scan_buffer` in `hunt.rs` to activate reentrancy and unsafe delegatecall detection. [lattice-gap: solidity_taint unwired] |
| 2026-05-15 | https://github.com/aave/aave-v3-periphery | no findings | 0 | Solidity peripheral contracts (rewards, treasury, adapters). Same `solidity_taint` wiring gap as Uniswap. TypeScript helpers (`hardhat.config.ts`, test suite) — no active detector matched. | Wire `crates/forge/src/solidity_taint.rs` into `scan_buffer`. DeFi periphery contracts have high immunefi bounty potential ($10k–$5M) once Solidity detection is active. [lattice-gap: solidity_taint unwired] |
| 2026-05-15 | https://github.com/tempus-ex/hello-video-codec | security:workflow_no_provenance (`src/codec.rs`, `src/frame.rs`) | 3 | Rust source files matched the workflow_no_provenance detector — same path guard gap as binance/typescript Sprint 133 finding. FP: detector must be gated to `.yml`/`.yaml` files with `jobs:` key, not arbitrary source files. | Apply Sprint 133 R&D follow-up: add extension exclusion to workflow_no_provenance (`.rs`, `.ts`, `.js`, `.py`, `.go`) with `jobs:` key requirement to eliminate source-file FPs. |
| 2026-05-15 | https://github.com/Uniswap/v3-periphery | no findings | 0 | `solidity_taint` now wired (Sprint 134) and re-struck; still no findings. Peripheral contracts (`NonfungiblePositionManager.sol`, `SwapRouter.sol`) — examined patterns do not match `solidity_taint` AhoCorasick sinks. | Extend `solidity_taint` with additional reentrancy patterns: `delegatecall` + assembly-level `calldataload` patterns used by Uniswap periphery contracts. |
| 2026-05-15 | https://github.com/aave/aave-v3-periphery | no findings | 0 | `solidity_taint` now wired and re-struck; still no findings. Rewards/treasury adapters — contracts do not match current `solidity_taint` sink patterns. | Same extension as Uniswap: additional Solidity sink patterns for DeFi reward distribution paths (cross-contract reward draining via unchecked return values). |
| 2026-05-15 | https://github.com/trezor/trezor-firmware | security:lcm_double_free (`crypto/fuzzer/fuzzer.c` ×8, `core/embed/io/display/unix/display_driver.c:171`, `core/embed/projects/unix/main.c:293,630`) | 5 | 8 of 11 hits in fuzzer code (not production); 3 remaining in Unix simulator build targets not shipped on Trezor hardware. No production firmware path confirmed reachable. | Extend `lcm.rs` with a path suppressor: demote findings in files matching `fuzzer/`, `unix/`, `test/`, `sim/` path segments unless a corroborating production entrypoint is proven. |
| 2026-05-15 | https://github.com/trezor/trezor-firmware | security:ssrf_dynamic_url (`common/tools/cointool.py:394`, `core/tools/provision_device.py:144`, `tools/snippets/eth_defs_unpack.py:85`) | 2 | Developer tooling scripts run by engineers — not device firmware. Local config controlled. Threat Model Awareness: no remote attacker path proven. | Extend SSRF suppressor: demote findings in `tools/`, `scripts/`, `common/tools/` path segments to LOW_YIELD unless a production HTTP server entrypoint is proven. |
| 2026-05-15 | https://github.com/trezor/trezor-firmware | security:intent_divergence (Rust protobuf generated code, `logger.rs:56`) | 2 | Generated protobuf code and logger — not authored security logic. Auto-generated code suppression gap. | Add generated-file suppressor to `intent_divergence`: exclude files containing `// DO NOT EDIT` or `generated by protoc` headers from intent drift detection. |
| 2026-05-15 | https://github.com/smartcontractkit/chainlink | security:jwt_validation_bypass (`core/utils/jwt.go:230`) | 2 | `ParseWithClaims` callback explicitly checks `token.Method.Alg() != EthereumSigningMethod.Alg()` — algorithm enforcement IS present. Detector fired because `ParseWithClaims` is present without checking for the adjacent algorithm validation. FP: algorithm confusion guard was found within the same function body. | Extend `jwt_validation_bypass` detector to suppress when `token.Method.Alg()` comparison is visible within ±15 lines of the `ParseWithClaims` call. |
| 2026-05-15 | https://github.com/smartcontractkit/chainlink | security:tls_verification_bypass (`deployment/environment/nodeclient/chainlink.go:66`) | 2 | `InsecureSkipVerify: true` in `initRestyClient()` with `//nolint` and `// G402 - TODO: certificates` — known dev/deployment environment shortcut, not production oracle code. | Add path guard in `tls_verification_bypass` to demote findings in `deployment/`, `test/`, `scripts/` directories unless proven reachable from a production ingress. |
| 2026-05-15 | https://github.com/mattermost/mattermost-plugin-boards | security:ssrf_dynamic_url (`webapp/src/octoClient.ts:74`) | 2 | Client-side TypeScript `fetch()` to a user-configured base URL. Per Threat Model Awareness: client-side HTTP calls are CORS-blocked; this is NOT server-side SSRF. The engine scanned TypeScript frontend code, not a Node.js SSR backend. | Add server-side execution path requirement for TypeScript SSRF candidates — only emit when Next.js API route, Express handler, or Node.js backend pattern is proven. |
| 2026-05-15 | https://github.com/mattermost/mattermost-plugin-boards | security:dom_xss_innerHTML (`webapp/src/utils.ts:143`) | 2 | `htmlToElement()` helper has zero callers in the scanned source tree — dead code. No attacker-controlled reflection path to the sink was proven. Existing CANDIDATE entry at 55% (2026-05-01) is for a different reflection path. | Extend DOM XSS detector to require at least one external caller of the HTML sink function before emitting a finding. |
| 2026-05-15 | https://github.com/mattermost/mattermost-plugin-boards | security:workflow_no_provenance (`webapp/src/components/*.tsx`) | 0 | FP: detector fired on TypeScript source files. Sprint 135 path guard (`label.ends_with(".yml") || label.ends_with(".yaml")`) resolves this, but binary hasn't been rebuilt with the fix yet. | Rebuild release binary after PR #112 lands on main to activate Sprint 135 path guard. |
| 2026-05-15 | https://github.com/mattermost/mattermost-plugin-github | security:workflow_no_provenance (`webapp/src/*.jsx`, `webapp/webpack.config.js`) | 0 | Same FP pattern — detector fired on JavaScript source files. Binary not rebuilt with Sprint 135 path guard yet. | Rebuild release binary after PR #112 lands on main. |
| 2026-05-16 | https://github.com/cashapp/misk | security:protobuf_any_type_field (`misk-proto/src/main/proto/misk/proto/status.proto`) | 2 | Sprint 137 deep-path trace: `Status.details: repeated google.protobuf.Any` is used exclusively in gRPC ERROR RESPONSES (server-populated `WebActionException.details`), not in server-side deserialization of client input. Wire library is Square Wire `AnyMessage` — no `unpack()` call on attacker-controlled data found in production code. | Extend protobuf_any detector to distinguish response-type vs request-type Any fields. Demote when the Any appears only in error-detail response messages, not in inbound request parsing. |
| 2026-05-16 | https://github.com/vercel/next.js | security:ssrf_dynamic_url (`packages/next/src/server/api-utils/node/api-resolver.ts:302`) | 3 | `fetch(\`https://${req.headers.host}${urlPath}\`)` — Host header injection pattern, but only triggered when `trustHostHeader: true` is explicitly set by the operator. Not user-controlled URL injection from the HTTP request body or query params. | Add guard that requires attacker-controlled URL component from request body/query params before emitting SSRF; demote Host-header patterns unless `req.headers.host` flows unchecked into the full URL in a non-proxy context. |
| 2026-05-16 | https://github.com/vercel/next.js | security:react_xss_dangerous_html (`packages/next/src/pages/_document.tsx`, `_error.tsx`) | 2 | `dangerouslySetInnerHTML` sinks fed from framework-internal template strings (CSS snippets, `partytownSnippet()`, `assetPrefix` config) — not from user-controlled HTTP inputs. No attacker-controlled data source found upstream of any sink. | Add source-tracing guard that requires user-request-derived data to reach `__html` before emitting XSS candidates from framework internals. |
| 2026-05-16 | https://github.com/vercel/next.js | security:workflow_no_provenance (`actions/cache@v5` in `build_and_test.yml:175`) | 2 | Single unpinned `actions/cache@v5` action reference (no SHA comment). Informational only — `actions/cache` from GitHub is a verified first-party action with mandatory provenance; tag-based references are low-risk. Not submission-grade without proof of substitution path. | Distinguish first-party GitHub Actions org (`actions/*`) from third-party in the workflow_no_provenance detector — first-party tag-pinning is acceptable. |
| 2026-05-16 | https://github.com/mattermost/mattermost-plugin-boards | security:react_xss_dangerous_html — 9 `dangerouslySetInnerHTML` sinks across block editor components (originally rated 87%, weaponized via P2-5 dual-frame harness; demoted from BOUNTY_LEDGER) | 5 | Target deprecated to community-maintained status in late 2023. Falls under Mattermost scope file (`tools/campaign/targets/mattermost_targets.md` line 212-213) "Other publicly-released plugins" exclusion — accepted informational only, no bounty. The technical finding is real but the program no longer pays for it. | Add target-deprecation cross-check to `cvp_red_team.md` triage protocol: scan target's GitHub for "archived"/"deprecated"/"community-maintained"/"no longer officially supported" in README, SECURITY.md, and last 50 commits before promoting any finding. Add `is_excluded_hunt_entry` AST guard in `crates/cli/src/hunt.rs` for `mattermost-plugin-boards` (and any future deprecated plugin path). |
| 2026-05-16 | https://github.com/mattermost/mattermost-plugin-boards | security:dom_xss — `webapp/src/utils.ts:143` (demoted from CANDIDATE_LEDGER, originally 55%) | 5 | Same deprecation exclusion as the 87% sibling row above. | Same R&D follow-up as 87% sibling row. |
| 2026-05-16 | https://github.com/mattermost/mattermost-plugin-boards | security:react_xss_stored_markdown — `marked` v4 raw HTML through `dangerouslySetInnerHTML` (demoted from CANDIDATE_LEDGER, originally 55%) | 5 | Same deprecation exclusion as the 87% sibling row above. | Same R&D follow-up as 87% sibling row. |
| 2026-05-16 | https://github.com/freedomofpress/securedrop | Missing ownership checks on journalist/admin routes (`journalist_app/admin.py`, `journalist_app/col.py`, `journalist_app/main.py`) — demoted from CANDIDATE_LEDGER, originally 44% | 5 | **false_positive_threat_model_mismatch**: Sprint 140 pre-Vagrant validation. Tier 1 (static, 10 min): 3 admin.py routes (`/edit/<int:user_id>`, `/delete/<int:user_id>`, `/edit/<int:user_id>/new-password`) all carry `@admin_required` (lines 355, 414, 447) which checks `session.get_user().is_admin`. 2 non-admin routes (`/download_unread/<filesystem_id>`, `/<filesystem_id>/<fn>`) covered by blueprint-level `@app.before_request` login_required hook at `journalist_app/__init__.py:135`. Tier 2 (recent commits, 5 min): zero commits matching `IDOR\|authorization\|access control\|ownership\|cross.user` in 2 years across these 4 files; only a single `Automated ruff fixes` commit in 6 months. SecureDrop's threat model treats all authenticated journalists as peers (anonymous sources, collaborative response) — "shared journalist access" is design-intent, not a missing ownership check. Detector misclassified design-intent as vulnerability. | Sprint 141 Phase 0 shipped `forge::threat_model_oracle` (commit 21dfe88) — the structural cure. Future hunts will not re-emit this class against SecureDrop. |
| 2026-05-17 | https://github.com/smartcontractkit/chainlink | security:jwt_validation_bypass (`core/utils/jwt.go:230`) — demoted from CANDIDATE_LEDGER, originally 36% Critical (nominal $27K EV) | 5 | **false_positive_algorithm_allowlist_enforced**: Sprint 141 Phase 1 Tier 1 static validation. `core/utils/jwt.go:258-266` shows `jwt.ParseWithClaims` keyfunc with TWO algorithm validation gates. Line 259-261: `if token.Method.Alg() != EthereumSigningMethod.Alg() { return nil, fmt.Errorf("unsupported JWT 'alg': '%s'", ...) }`. Line 262-264: `if _, ok := token.Method.(*SigningMethodEth); !ok { return nil, jwt.ErrSignatureInvalid }`. The candidate's `alg:none` repro fails at line 260 before the keyfunc returns the public key. Additionally chainlink uses a CUSTOM `SigningMethodEth` (Ethereum ECDSA-based signatures), not standard HS256/RS256 — the entire JWT-bypass threat model is misapplied. Tier 2: latest commit on this file is 2025-10-24 ("ensure jwt token issuedAt is in the past"); 4 commits in past year, all routine maintenance; zero CVE/security fix commits — consistent with sound implementation. Tier 3 reaffirmed: focus_area_mismatch (off-chain Go vs oracle/reentrancy/on-chain focus areas per `tools/campaign/targets/immunefi_targets.md:63-67`). | New detector improvement needed: extend `forge::threat_model_oracle` (or a sibling `forge::jwt_keyfunc_oracle`) to AST-parse the keyfunc body of `jwt.Parse`/`jwt.ParseWithClaims` calls before emitting `security:jwt_validation_bypass`. Suppress when the keyfunc contains an algorithm-allowlist check or a type-assertion on `token.Method`. P-tier cost: 200-300 LOC. |
| 2026-05-17 | https://github.com/smartcontractkit/chainlink | security:sql_injection (`core/store/store.go:156`) — demoted from CANDIDATE_LEDGER, originally 54% High (nominal $16.2K EV) | 5 | **false_positive_test_only_function_with_quoted_identifiers**: Sprint 141 Phase 1 Tier 1 static validation. The cited line is inside `dropAndCreatePristineDB(db *sqlx.DB, template string)` — a TEST infrastructure function that drops and creates a pristine test database using `testdb.PristineDBName`. The actual SQL uses `pq.QuoteIdentifier()` to safely escape SQL identifiers. The Chainlink team has an explicit inline annotation: `//nolint:gosec // G701 false positive: identifiers from pq.QuoteIdentifier only` directly on the line. Tier 1 fails on three grounds: (a) `pq.QuoteIdentifier` is the canonical safe-identifier helper, (b) the function is test-only infrastructure not reachable from production HTTP/API ingress, (c) the maintainers have explicitly marked the linter result as a false positive. | New detector improvement needed: extend SQL-injection detector to (a) recognize `pq.QuoteIdentifier`/`sqlx.QuoteIdentifier`/`sql/driver.Quote` as identifier-sanitization helpers, (b) demote findings inside test-infrastructure functions (`testdb`/`*_test.go`/`testfixtures`/`pristine*`/`drop*Create*`), (c) parse `//nolint` annotations as maintainer-asserted suppression signal. P-tier cost: 150-200 LOC. |
| 2026-05-17 | https://github.com/smartcontractkit/chainlink | security:protobuf_any_unguarded_decode (`core/capabilities/confidentialrelay/handler.go:416` + 45 sibling sinks) — demoted from CANDIDATE_LEDGER, originally 25% (nominal $7.5K EV with focus_area_mismatch) | 5 | **false_positive_concrete_typed_unmarshal_not_anypb_any**: Sprint 141 Phase 1 Tier 1 static validation. Line 416 is `proto.Unmarshal(payloadBytes, &sdkReq)` where `sdkReq` is `sdkpb.CapabilityRequest` — a CONCRETE typed protobuf message, NOT `anypb.Any`. The protobuf-Any vulnerability class applies to code that unpacks into `Any` and dispatches on `type_url` to instantiate arbitrary types. Concrete-typed Unmarshal cannot suffer the type-confusion attack: malformed bytes produce a malformed `CapabilityRequest`, not a forged-type message. The candidate misidentified the threat model. The Sprint 138 P2-16 demotion logic in `hunt.rs::apply_protobuf_any_reachability_demotion` should have caught this — investigate why it didn't fire. | Detector improvement: extend P2-16 demotion logic in `hunt.rs::apply_protobuf_any_reachability_demotion` to also demote `security:protobuf_any_unguarded_decode` when the Unmarshal destination is a concrete typed message (`var x SomePb.X; proto.Unmarshal(bytes, &x)`) rather than `anypb.Any` or `interface{}`. Requires Go AST inspection of the destination type. P-tier cost: 100-150 LOC. |
| 2026-05-17 | https://github.com/mattermost/mattermost | security:react_xss_dangerous_html (`webapp/channels/src/components/admin_console/license_settings/modals/upload_license_modal.tsx:195` and 9 sibling sinks) — demoted from CANDIDATE_LEDGER (2026-05-13 row, 45% approval, $1.75K midpoint, EV $788) | 5 | **false_positive_admin_only_excluded_by_program_rules**: Sprint 142 Phase 1 Tier 1 static validation. Line 195 is `dangerouslySetInnerHTML={{__html: marked(serverError)}}` with vanilla `marked` import (line 4) — the pattern is real (no DOMPurify wrapper). BUT the file path is `admin_console/license_settings/modals/` — the modal is reachable ONLY by sysadmins uploading a license. Mattermost scope file (`tools/campaign/targets/mattermost_targets.md` line 222) explicitly states "System Admins are allowed to perform any actions on the system" — admin-to-admin XSS via license upload is not a privilege boundary. Even if attacker controls `serverError` content (which requires a non-trivial server-side echo path), the scenario is admin → admin which the program excludes. | Detector improvement: extend `forge::threat_model_oracle` or a new `forge::admin_only_surface_oracle` to inspect path components for `admin_console/`, `admin/`, `/admin/`, `sysadmin_*` patterns. When the cited file is on an admin-only UI/API surface AND the program's scope file lists admin-only attacks as excluded, demote the finding to Informational with reason `admin_only_excluded_by_program_rules`. P-tier cost: 100-150 LOC. |
| 2026-05-17 | https://github.com/mattermost/mattermost | security:ssrf_dynamic_url (`server/channels/app/admin.go:161`, function `TestSiteURL`) — demoted from CANDIDATE_LEDGER (both 2026-05-08 + 2026-05-13 rows, 35% approval, $1.75K midpoint, EV $613) | 5 | **false_positive_intentional_admin_feature_excluded_by_scope**: Sprint 142 Phase 1 Tier 1 static validation. Lines 159-171 show function `TestSiteURL(rctx request.CTX, siteURL string)` — a deliberate admin tool that fetches `<siteURL>/api/v4/system/ping` to verify a configured site URL is reachable. The "SSRF" is the function's intended behavior, not an unintended sink. The admin enters the URL; the server fetches it. Mattermost scope file line 192-193 explicitly excludes "Server-side-request-forgery (SSRF) that requires system admin privileges, except on a cloud instance." Reaching `TestSiteURL` requires sysadmin role on self-hosted; on cloud the same role gate applies via the admin API. | Detector improvement: extend `forge::threat_model_oracle` to suppress SSRF findings whose enclosing Go function name matches `Test*URL`/`Validate*URL`/`Ping*`/`Health*Check*` AND whose surrounding control flow has no taint propagation from a user-controlled request body. These are explicit admin-tool patterns by convention. P-tier cost: 80-120 LOC. |
| 2026-05-17 | https://github.com/mattermost/mattermost | security:tls_verification_bypass (`server/platform/shared/filestore/s3store.go:156` — `InsecureSkipVerify: true`) — demoted from CANDIDATE_LEDGER (both 2026-05-08 + 2026-05-13 rows, 35% approval, $1.25K midpoint, EV $438) | 5 | **false_positive_admin_set_config_field_not_default**: Sprint 142 Phase 1 Tier 1 static validation. Lines 155-157 show `if b.skipVerify { tr.TLSClientConfig = &tls.Config{InsecureSkipVerify: true} }` — the bypass is gated by an explicit admin-set config field (`b.skipVerify`), not a build-time default. The default is verify-on (the bypass branch is the exceptional case). Reaching this code path requires admin compromise to flip the config flag — which is excluded by scope rules around admin privileges. | Detector improvement: extend `forge::threat_model_oracle` to inspect `InsecureSkipVerify: true` and `tls.Config{InsecureSkipVerify: ...}` patterns. Suppress when the field is gated by a config-struct boolean (NOT a literal `true`) and the enclosing struct's config schema defaults the field to `false`. Promote only when (a) the literal is unconditional OR (b) the config default is `true`. Requires Go AST inspection of struct field initialization. P-tier cost: 100-150 LOC. |
| 2026-05-20 | https://github.com/grafana/grafana | security:jwt_validation_bypass (`apps/provisioning/pkg/connection/github/token.go:68` — `jwt.WithoutClaimsValidation()`) and (`pkg/services/rendering/auth.go:151` — `jwt.WithValidMethods(HS512)`) | 5 | `token.go:68`: `parseJWTToken` is a utility for READING back timestamps from a self-generated GitHub App JWT (same key, no authentication gate). `WithoutClaimsValidation()` is intentional — the function extracts IssuedAt/ExpiresAt, not validates auth. `auth.go:151`: `WithValidMethods([]string{HS512})` is algorithm-pinned; no bypass possible. Both are FPs. | Extend `forge::jwt_keyfunc_oracle` to detect when `WithoutClaimsValidation()` is used AND the function's return type is `*jwt.RegisteredClaims` (time-extraction pattern) rather than a user auth gate — suppress as informational. |
| 2026-05-20 | https://github.com/grafana/grafana | security:ssrf_dynamic_url (`packages/grafana-runtime/src/services/pluginMeta/plugins.ts:21,45,61`, `public/app/core/services/context_srv.ts:250`, `public/app/plugins/datasource/loki/tracking.ts:161`, `public/swagger/K8sNameLookup.tsx:37`) and Go: `pkg/services/publicdashboards/internal/commands/generate_datasources/generate.go:60` | 5 | TypeScript sinks: client-side browser execution; all fetch() calls are CORS-blocked — not server-side SSRF. Go sink: `baseUrl` is hardcoded to `"https://grafana.com"` in `main.go:24`; this is a CLI code-generation tool, not an HTTP handler. All are FPs. | Client-side TypeScript SSRF suppression already in LOW_YIELD_LEDGER (response-format Threat Model). Extend Go SSRF demoting filter to detect when `baseUrl` is a compile-time constant string literal rather than from `http.Request`. |
| 2026-05-20 | https://github.com/grafana/grafana | security:react_xss_dangerous_html (9 sites across `PanelChrome/PanelDescription.tsx`, `Table/TableNG/Cells/MarkdownCell.tsx`, `Typeahead/TypeaheadInfo.tsx`, `PluginHelp.tsx`, `HelpModal.tsx`, `Changelog.tsx`, `PluginDetailsBody.tsx`, `FolderReadmePanel.tsx`, `UsersListPage.tsx`) | 5 | All sinks receive content pre-sanitized via `renderMarkdown()` OR gated on `getConfig().disableSanitizeHtml` which is a server-side admin configuration (`grafana.ini security.disable_sanitize_html`) — not user-controlled. Admin-required config change → Approval % < 10%. | Extend `react_xss` detector to suppress `dangerouslySetInnerHTML` sinks where upstream dataflow passes through a `renderMarkdown()` call. Admin-config gate pattern detection (reading from `getConfig().disableSanitizeHtml`) should also emit suppression signal. |
| 2026-05-20 | https://github.com/vercel/next.js | security:jwt_validation_bypass (`packages/next/src/server/api-utils/node/try-get-preview-data.ts:90` — `jsonwebtoken.verify` without explicit `algorithms`) | 3 | `jsonwebtoken.verify(token, options.previewModeSigningKey)` — `previewModeSigningKey` is a string (HMAC symmetric key). Modern jsonwebtoken (v9.x) infers allowed algorithms from key type: string → HMAC only (HS256/HS384/HS512). `alg: none` is unconditionally rejected in v9.x. RSA/HMAC confusion fails because key type mismatch causes verify to throw. FP on modern jsonwebtoken. | Extend `forge::jwt_keyfunc_oracle` to check jsonwebtoken version in package.json; suppress `jwt_validation_bypass` when the installed version is ≥9.0.0 AND the key parameter is a string (symmetric key path). |
| 2026-05-20 | https://github.com/vercel/next.js | security:intent_divergence (`turbopack/crates/turbo-tasks/src/backend.rs:528,531,533,536` — `#[allow(unused_variables)]` on trait default method bodies) | 3 | FP: The detector matched `#[allow(unused_variables)]` as a "vacuous_allow" intent-divergence signal on Rust trait `Backend` default method implementations (empty body). Empty default methods in Rust traits routinely suppress unused-variable warnings on parameters. This is standard Rust API design, NOT an authorization bypass. | Add suppressor in `classify_intent_divergence_proof`: if file extension is `.rs` AND source around finding line contains `#[allow(unused_variables)]` + `{}` (empty body) within a `trait` definition context, classify as LatticeGapProposal and demote. |
| 2026-05-20 | https://github.com/vercel/next.js | security:dom_xss_innerHTML (`packages/next/src/client/head-manager.ts:11`, `packages/next/src/client/script.tsx:122`, and 28 sites in `packages/next/src/compiled/react-dom-experimental/cjs/`) | 3 | Framework internals and compiled React DOM bundles. `head-manager.ts`/`script.tsx`: `innerHTML` sinks receive content from React's own `dangerouslySetInnerHTML` prop — developer-controlled, not attacker-controlled HTTP input. Compiled React DOM files are vendored third-party bundles — not Next.js-owned code. No user-request taint path found. | Extend DOM XSS detector to: (a) skip files under `packages/*/compiled/` (vendored bundles), (b) require upstream user-request taint before emitting innerHTML as XSS — developer-passed props are not an attack surface for the framework vendor. |
| 2026-05-20 | https://github.com/coinbase/coinbase-advanced-py | no_findings — 0 security findings across all detectors | N/A | Repository is a lightweight Python SDK wrapping Coinbase's Advanced Trade API. No server-side execution, no template rendering, no auth middleware, no SQL, no FFI. Pure HTTPS client library. | N/A — no structural gaps identified. |
| 2026-05-20 | https://github.com/OctopusDeploy/go-octopusdeploy | no_findings — 0 security findings across all detectors | N/A | Pure Go REST API client library. No server-side execution paths, no subprocess invocations, no FFI, no template rendering. All functionality is HTTP client calls to the Octopus Deploy API. | N/A — no structural gaps identified. |
| 2026-05-20 | https://github.com/pinterest/querybook | security:non_constant_time_comparison @ `querybook/server/models/user.py:55,60` | 0 | FP: `check_password_hash` (Werkzeug) is a constant-time bcrypt comparison. Oracle guard extended in Sprint 151 (`classify_timing_comparison_proof`) to detect `check_password_hash(` and `hmac.compare_digest(` within ±10-line window and return `InvariantViolationProof`. This FP class is now structurally eradicated. | Extended `classify_timing_comparison_proof` with Python constant-time guard patterns: `check_password_hash(` and `hmac.compare_digest(`. Re-hunt will suppress this class. |
| 2026-05-20 | https://github.com/pinterest/querybook | security:config_taint_* × 13 (various Python files: `action.ts`, `query_execution.py`, `sqlalchemy.py`, `serializers.py` ×2, `metastore.py` ×2, `glue_client.py` ×3, `presto.py`, `table_schema.py` ×2, `tag.py`, `gspread_exporter.py`) | 0 | All are Python internal attribute accesses and dict key lookups on internal service state objects — not taint sources from HTTP input. `datadoc.meta`, `new_column_stat.id`, `result["DatabaseList"]` etc. are ORM model attributes, not attacker-controlled data. Threat Model: internal service state, not external request taint. | Add Python ORM attribute access patterns to `config_taint` suppressor: when the taint source is an SQLAlchemy model attribute (`.id`, `.meta`, `.columns`, `.tags`) or AWS API response dict key, classify as internal state and demote. |
| 2026-05-20 | https://github.com/pinterest/querybook | security:ics_hardcoded_override @ `querybook/webapp/lib/sql-helper/sql-setting.ts:22` | 0 | Client-side TypeScript SQL editor settings file. Hardcoded override values are UI defaults (e.g., tab size, line numbers) — not credentials, auth tokens, or connection strings. No remote attack surface. | ICS detector should distinguish between: (a) hardcoded auth/credential overrides (retain) vs (b) hardcoded UI configuration defaults (suppress). Add finding class `ics_ui_default` for (b). |
| 2026-05-20 | https://github.com/pinterest/querybook | security:llm_model_unverified_load @ `querybook/server/clients/redis_client.py:48` | 0 | FP: Redis client initialization code at line 48 is a `ConnectionPool` setup — not an ML model load. The `llm_model_unverified_load` detector matched on `redis_client.py` keyword proximity. No ML weight file loading or `torch.load`/`pickle.load` pattern confirmed at line 48. | Tighten `llm_model_unverified_load` detector to require explicit ML framework import (`torch`, `tensorflow`, `transformers`) in the same file before emitting. |
| 2026-05-20 | https://github.com/pinterest/querybook | security:model_weight_backdoor @ `querybook/webapp/components/DataDocStatementExecution/StatementResult.tsx:1` | 0 | FP: Match at line 1 of a React TSX component file — no ML model weights, no `pickle`, no `torch.load`. Detector triggered on file-level metadata in a UI component that displays SQL statement results. | `model_weight_backdoor` detector must require presence of ML framework imports (`torch`, `tensorflow`, `safetensors`, `pickle`) in the same file before emitting. Line-1 matches without import context are definitional FPs. |
| 2026-05-20 | https://github.com/pinterest/querybook | security:embedding_trust_transposition @ `querybook/server/lib/vector_store/base_vector_store.py:88` | 8 | Abstract base class for vector store integrations. Line 88 is a method stub/abstract interface. Whether embeddings are attacker-controlled depends on whether user-submitted query text reaches the vector embedding call without sanitization. Approval < 10%: requires proving user query → embedding → vector search → sensitive data return path with injection. | Elevation to CANDIDATE requires: (1) tracing user HTTP query text through `create_embedding()` call to the vector DB search, (2) proving that query reconstruction from nearest-neighbor results leaks data not authorized to the requesting user. |
| 2026-05-20 | https://github.com/auth0/node-auth0 | security:oauth_missing_state_validation × 2 (`src/auth/oauth.ts`, `src/management/api/requests/requests.ts`) + security:config_taint_* × 3 | 0 | TypeScript SDK files — client-side SDK method wrappers, not server-side OAuth callbacks. `classify_oauth_state_validation_proof` correctly emits `lattice_gap_proposal`. Config taint in Headers.ts/makePassthroughRequest.ts: client-side TypeScript fetch() without SSR path, CORS-blocked. | No engine change needed — TypeScript SDK wrappers are already correctly classified by `classify_oauth_state_validation_proof`. |
| 2026-05-20 | https://github.com/Kong/kong | security:credential_leak × 26 (`spec/fixtures/mtls_certs/*.key`, `t/certs/*.key`, `spec/03-plugins/16-jwt/fixtures.lua`, etc.) | 15 | Test fixture private keys committed for Kong's mTLS and JWT integration test suite. Real key entropy but no production use — all self-signed test CAs. Kong's bug bounty program typically rejects test fixture credential reports without proof of production use. | Add suppress-path guard for `spec/fixtures/` and `t/certs/` directories in credential_leak detector. |
| 2026-05-20 | https://github.com/Kong/kong | security:credential_leak × 2 (`kong/pdk/vault.lua:1640,1655`) | 0 | Documentation code examples in Lua doc comments — `"-----BEGIN RSA PRIVATE KEY-----..."` with `...` truncation. No actual key material. InvariantViolationProof correctly attached. | Credential leak detector should check for `...` suffix on multi-line PEM examples in comment blocks. |
| 2026-05-20 | https://github.com/Kong/kong | security:os_command_injection × 4 (`scripts/explain_manifest/main.py`) + security:command_injection × 3 (`spec/` test files) | 3 | `scripts/explain_manifest/main.py` is a developer utility run locally by Kong contributors — admin execution, not network-reachable. `spec/` command_injection: InvariantViolationProof correctly attached (test path). | — |
| 2026-05-20 | https://github.com/Kong/kong | security:unpinned_asset (`kong/templates/nginx_kong_gui_include.lua:87` — `https://buttons.github.io/buttons.js` without SRI) + security:unpinned_asset (`scripts/release-lib.sh:187`) + security:curl_pipe_execution (`scripts/build-wasm-test-filters.sh:48`) + security:protobuf_any_type_field (`spec/fixtures/grpc/`) | 5 | `nginx_kong_gui_include.lua`: Admin GUI external JS without SRI — admin UI typically private-network only, requires admin credential access. `release-lib.sh`: developer release tooling. `curl_pipe_execution` and `protobuf_any_type_field`: dev tooling and test fixtures. | Unpinned admin UI external JS: if confirming Kong Admin GUI is public-facing in standard deployment, promote to CANDIDATE at 25%. |
| 2026-05-20 | https://github.com/keycloak/keycloak | security:oauth_account_fusion_pretakeover × 29 (`js/apps/admin-ui/*.tsx`, `maven-resources-community/theme/**/*.properties`, `themes/**/*.ftl`) | 0 | All TypeScript/TSX admin UI components and i18n message property files referencing `linkAccount` string. `classify_oauth_account_fusion_proof` correctly emits `lattice_gap_proposal` for non-Java files. Java backend account-linking logic not reached by detector. | Engine gap: Java server-side account fusion path at `services/src/main/java/org/keycloak/authentication/authenticators/broker/IdpAutoLinkAuthenticator.java` not detected. Add to future Java hunt scope. |
| 2026-05-20 | https://github.com/keycloak/keycloak | security:ssrf_dynamic_url × 7 (`js/apps/admin-ui/src/clients/ssf/*.tsx`) + security:sqli_concatenation × 2 (Liquibase custom tasks) | 3 | SSF tabs: client-side TypeScript fetch() — CORS-blocked, no SSR path. SQL concatenation in Liquibase migration tasks: runs under Keycloak admin upgrade, not attacker-controlled input. | — |
| 2026-05-20 | https://github.com/keycloak/keycloak | security:non_constant_time_comparison × 2 (`BCEcdhEsAlgorithmProvider.java:80,112`) — `Algorithm.ECDH_ES.equals(header.getAlgorithm())` | 0 | Algorithm identifier name comparison (string constant `"ECDH_ES"`), not secret material. FP: `classify_timing_comparison_proof` Java gap does not distinguish algorithm-name `.equals()` from secret-material `.equals()`. | Add Java-specific guard: suppress when `.equals()` operand is a class constant (`Algorithm.*`, `MediaType.*`) rather than a variable derived from secret material. |
| 2026-05-20 | https://github.com/keycloak/keycloak | security:model_weight_backdoor × 4 + security:prototype_pollution_merge_sink × 1 + security:unpinned_git_dependency × 1 | 0 | `model_weight_backdoor` on Keycloak JS UI source files — no ML weight loading, FP. `prototype_pollution_merge_sink`: React admin UI object spread, no attacker-controlled key injection without admin credentials. `unpinned_git_dependency`: no git-ref deps in pom.xml at HEAD. | — |
| 2026-05-20 | https://github.com/hashicorp/vault | security:non_constant_time_comparison × 3 (`builtin/credential/approle/path_login.go`) | 8 | Sprint 153 timing PoC at N=200 Docker loopback: valid mean=1.796ms stdev=3.437ms; invalid mean=1.198ms stdev=0.727ms; delta=0.598ms (< 5ms threshold); sigma=3.4. Delta not statistically significant — Docker network variance dominates. Retain CANDIDATE at 35%. | Re-run via Unix socket: `vault server -dev` + `curl --unix-socket /var/vault/vault.sock`; N=1000; if delta >5ms promote CANDIDATE 35%→65%. |
| 2026-05-20 | https://github.com/openai/codex | security:intent_divergence — `codex-rs/model-provider/src/auth.rs:54-63` `UnauthenticatedAuthProvider` adds no auth headers | 0 | **Intentional design, not a bug.** Source comment at line 52: *"Some providers are meant to send no auth headers. Examples include local OSS providers and custom test providers with `requires_openai_auth = false`."* OpenAI ships `unauthenticated_auth_provider_adds_no_headers` as a passing test explicitly verifying this behaviour. Config loads from `~/.codex` only (`find_codex_home()` in `utils/home-dir/src/lib.rs:13`) — no project-level config attack vector exists. An attacker cannot override the provider base URL without write access to the user's home directory. Sprint 154 code audit of current HEAD confirmed. DO NOT re-surface — structural FP in `intent_divergence` detector for OSS-provider no-op `add_auth_headers` implementations. | Add a guard in `classify_intent_divergence_proof`: suppress when `add_auth_headers` body is `{}` AND the enclosing struct name contains `Unauthenticated` OR the file comment within ±5 lines contains `local OSS` / `requires_openai_auth`. |
| 2026-05-20 | https://github.com/hashicorp/vault | security:protobuf_any_http_metadata_dos — authenticated `identity-write` user sends `{"@type":"type.googleapis.com/vault.identity.Entity"}` as metadata key on `POST /v1/identity/entity` → HTTP 500 (controlled error, no panic/crash) | 5 | Phase 4 Docker PoC confirmed: Vault returns HTTP 500 at JSON API validation layer. Vault container logs show zero panic stack trace. `ptypes.UnmarshalAny` at `vault/identity_store.go:1172,1188,1194,1271,1289,1309` is in the storage deserialization path (Consul/etcd reads), NOT reachable from the HTTP entity write API. Authenticated DoS via malformed metadata key — not a deserialization gadget path. | Separate from CANDIDATE protobuf_any entry. If Vault adds richer structured metadata with Any-typed fields, re-evaluate whether `@type` can escape the metadata namespace into the storage-layer deserialization path. |
| 2026-05-20 | https://github.com/mattermost/mattermost | security:oauth_missing_state_validation FP × 3 (Sprint 154 channels hunt) — `app/saml.go`, `store/sqlstore/oauth_store.go`, `app/ratelimit.go` | 2 | FP batch: (1) `app/saml.go` uses `CreateSamlRelayToken` — a relay-state token is the correct SAML state mechanism; (2) `sqlstore/oauth_store.go` is a SQL persistence layer, not an HTTP callback handler; (3) `ratelimit.go` is rate-limiting middleware, not OAuth callback logic. Detector fires on "oauth" naming in non-handler contexts. | Add HTTP handler context gate to `classify_oauth_state_validation_proof`: demote when the triggering file is under `store/` (ORM layer) or `middleware/` (non-callback middleware), or when the enclosing function name contains `CreateSaml`, `RelayToken`, or `RateLimit`. |
| 2026-05-20 | https://github.com/grafana/grafana | security:protobuf_any_unguarded_decode FP × 7 (Sprint 154 grafana/pkg hunt) — `services/ngalert/notifier/redis_peer.go:493,574`, `services/ngalert/store/proto_instance_database.go:305`, `tsdb/tempo/search.go:95`, `tsdb/tempo/trace.go:88,114`, `tsdb/tempo/traceql_query.go:104` | 2 | All 7 are `proto.Unmarshal(buf, &concreteMsgType)` — standard typed deserialization, NOT `google.protobuf.Any` dynamic type dispatch. The vulnerability class requires `anypb.UnmarshalTo` / `ptypes.UnmarshalAny` with a type-URL dispatch table; `proto.Unmarshal` on a concrete type is safe. Structural FP: detector matches any `proto.Unmarshal` call in files that also import `anypb` (co-location heuristic, not call-chain match). | Tighten `find_protobuf_any_reachability`: require the deserialization call to be `UnmarshalAny`, `UnmarshalTo`, `proto.UnmarshalOptions.Unmarshal` on an `anypb.Any` variable, or an explicit `Any.UnmarshalTo()` method call. Suppress generic `proto.Unmarshal(&concreteType)`. |
| 2026-05-20 | https://github.com/grafana/grafana | security:jwt_validation_bypass FP — `services/rendering/auth.go:151` | 3 | `jwt.ParseWithClaims` key function uses `_ *jwt.Token` (blank identifier) because a single shared HMAC key is always returned — correct Go idiom for single-key providers. `jwt.WithValidMethods([]string{jwt.SigningMethodHS512.Alg()})` option blocks algorithm confusion. Signature is verified. FP: detector triggers on `_ *jwt.Token` without checking for accompanying `WithValidMethods` guard. | Add composite suppression: when `jwt.ParseWithClaims` call site includes a `jwt.WithValidMethods(` argument within the same call expression, downgrade to `InvariantViolationProof`. |
| 2026-05-20 | https://github.com/grafana/grafana | security:oauth_missing_state_validation FP × 6 (Sprint 154) — `services/authapi/authapi.go`, `cloudmigrationimpl/xorm_store.go`, `services/gcom/gcom.go`, `services/notifications/notifications.go`, `temp_user/tempuserimpl/store.go`, `tsdb/tempo/kinds/dataquery/types_dataquery_gen.go` | 2 | Access-policy API client, ORM stores, cloud-comms client, notification service, user store, auto-generated type binding — none are HTTP callback handlers receiving inbound OAuth redirects. Detector fires on presence of "oauth" or "code" field in any Go file without confirming HTTP router registration or inbound `r.URL.Query().Get("code")` extraction. | Require HTTP ingress confirmation in Go classifier: only emit when file imports `net/http` AND contains `r.URL.Query().Get("code")` / `r.FormValue("code")` within a route-handler function body. |
| 2026-05-20 | https://github.com/supabase/supabase | security:react_xss_dangerous_html × 5 + security:ssrf_dynamic_url × 1 — all in `packages/marketing/src/` | 0 | All findings are in the marketing website source (`marketing/src/go/sections/CodeBlockSection.tsx:65,73,77`, `CodeBlockTabs.tsx:34,38`, `marketing/src/crm/hubspot.ts:77`). Supabase bug bounty targets core backend services (GoTrue, Realtime, PostgREST, Storage, Edge Functions) — marketing site is explicitly out of scope. The monorepo `packages/` directory includes marketing alongside production services; pre-scope filter not applied before hunt. | Add Supabase-specific scope gate: when hunting `supabase/supabase`, skip `packages/marketing/` before running the detector suite. Generalise as: monorepo hunts should pre-filter subdirectories against the program's explicit asset scope list before analysis. |
| 2026-05-20 | https://github.com/oauth2-proxy/oauth2-proxy | security:unpinned_asset × 3 — `Dockerfile:68`, `contrib/local-environment/kubernetes/Chart.lock:6,9,12` | 5% | Infrastructure asset pinning in Dockerfile and Helm Chart.lock — maintainer-controlled build artifacts. Not bounty-eligible supply-chain risk (no attacker-controlled substitution path without repo write access). | Already documented as supply-chain best-practice gap; not a vulnerability in oauth2-proxy itself. |
| 2026-05-20 | https://github.com/oauth2-proxy/oauth2-proxy | security:oauth_missing_state_validation FP × 5 — `providers/azure.go`, `providers/google.go`, `providers/logingov.go`, `providers/ms_entra_id.go`, `providers/provider_default.go` | 2% | oauth2-proxy IS the OAuth state validator — state check happens at the proxy level (not per-provider). Detector fires on provider files that handle code exchange without seeing the centralized state check in `oauthproxy.go`. Same FP class as mattermost Sprint 154. | Classifier needs HTTP handler gate: only emit for files where `r.URL.Query().Get("code")` extraction precedes a missing state check. Provider files implement the token-exchange protocol layer, not the callback gate. |
| 2026-05-20 | https://github.com/casdoor/casdoor | security:non_constant_time_comparison × 12 — `cred/argon2id.go:17,19,21,22,26` (all `lattice_gap_proposal`) | 3% | Argon2id credential hashing. Engine emits `lattice_gap_proposal` — no direct comparison sink found at those lines; Argon2id library performs constant-time comparison internally. Proof class is correct: no user-visible timing leak exists here. | Do NOT promote. Argon2id's `argon2.CompareHashAndPassword` is constant-time by specification. The `lattice_gap_proposal` proof class correctly signals engine cannot prove the sub-library's behavior. |
| 2026-05-20 | https://github.com/casdoor/casdoor | security:ssrf_dynamic_url × 263 — mass KevCritical batch across casdoor Go backend | 5% | 263 SSRF findings in an IAM platform that intentionally contacts OAuth providers, SAML endpoints, LDAP, webhook callbacks, and user-configured identity sources. Casdoor's core architecture is an authenticated SSO/IdP: all outbound HTTP is operator-configured. Without individual source tracing, this entire batch is config-controlled outbound traffic. | Treat as LOW_YIELD until individual source tracing confirms a user-supplied URL without allowlist. Re-hunt casdoor SSRF batch with `--filter` for `upstream_validation_absent: true` AND file exclusion for `*/provider/*.go` (intentional OAuth client code). |
| 2026-05-20 | https://github.com/casdoor/casdoor | security:oauth_missing_state_validation FP × 35 — casdoor/casdoor sprint 155 hunt | 2% | Casdoor is itself an OAuth 2.0 identity provider — state validation is handled in the authorization code flow, not in individual provider callback files. Detector fires on provider files implementing RFC 6749 client code without seeing the state gate in the callback router. Same FP class as mattermost/grafana/oauth2-proxy. | Add classifier gate: suppress when `go.mod` of the repo declares `github.com/casdoor/casdoor` as the module (i.e., within Casdoor itself), since the entire platform is an IdP and state validation is in the core router. |
| 2026-05-21 | https://github.com/casdoor/casdoor | security:jwt_validation_bypass × 4 — `object/token_cleanup.go:37`, `object/token_dpop.go:103`, `object/token_jwt.go:669`, `object/token_standard_jwt.go:91` | 5% | Static keyfunc inspection blocks the alg-none hypothesis. `token_jwt.go` and `token_standard_jwt.go` reject non-RSA/ECDSA signing methods before returning a key. `token_cleanup.go` uses `ParseUnverified` only inside `CleanupTokens` to read `exp` for expired-token garbage collection, not an auth gate. `token_dpop.go` verifies DPoP proof signatures with the embedded JWK public key; `golang-jwt/jwt/v5` rejects `alg:none` unless `UnsafeAllowNoneSignatureType` is used, and that marker is absent. | Structural guard shipped in `crates/forge/src/slop_hunter.rs`: suppress cleanup-only `ParseUnverified` expiry scans and DPoP JWK signature-proof contexts. Existing `jwt_keyfunc_oracle` suppresses RSA/ECDSA keyfunc guards. |
| 2026-05-20 | https://github.com/zitadel/zitadel | security:non_constant_time_comparison × 23 — `internal/crypto/passwap.go` (×22 deduplicated lines) + `backend/v3/storage/database/repository/user_human.go:145` — all emitted `reachability_proof` pre-fix | 2% | **Go block FP batch** — neither file contains `bytes.Equal(`. `passwap.go` contains hash algorithm name constants (`HashNameArgon2 = "argon2"`, etc.) triggering the broad `== + hash` keyword check before the Sprint 155 fix narrowed `has_go_timing_sink` to `bytes.Equal(` only. `user_human.go:145` is a `password_hash` column ORM assignment, not a secret comparison sink. After the Sprint 155 fix these do not fire. | Sprint 155 fix: `has_go_timing_sink` narrowed to `source.contains("bytes.Equal(")` only — eradicates this FP class for algorithm-name constant files. Do NOT re-promote without `bytes.Equal(` call confirmed in source. |
| 2026-05-20 | https://github.com/zitadel/zitadel | security:oauth_missing_state_validation × 9 — zitadel sprint 155 hunt | 2% | Zitadel is an OIDC/OAuth 2.0 identity server — same FP class as casdoor/oauth2-proxy. State validation is centralized in the authorization endpoint, not in individual provider handler files. | Same fix class: HTTP handler gate required to confirm inbound `r.URL.Query().Get("code")` precedes missing state check. |
| 2026-05-20 | https://github.com/zitadel/zitadel | security:ssrf_dynamic_url × 2 — `apps/login/acceptance/idp/saml/main.go:156` + `internal/api/http/domain_check.go:35` | 3% | (1) `saml/main.go:156`: acceptance test directory — test SAML IdP HTTP fetch, not production code. (2) `domain_check.go:35`: `ValidateDomainHTTP(domain, ...)` — domain value originates from org admin's stored domain record, called from `VerifyOrgDomain` command wired only to `admin/import.go` grpc handler. Admin-level SSRF is not remotely exploitable without org-admin credentials. | If Zitadel adds a self-service domain verification endpoint reachable by non-admin org members, re-assess. Admin-SSRF is typically OOS at most programs. |
| 2026-05-20 | https://github.com/zitadel/zitadel | security:config_taint_* × 11 — `projectId`, `accessToken`, `appId`, `client.client_id`, `currentSessions:`, `dataSource`, `events`, `id`, `idToken`, `sessions:`, `setting`, `defaultLoginNameSuffix.innerText` | 1% | All 11 are local configuration taint signals in Angular console TypeScript or internal config structs. Config values originate from operator-controlled settings, not attacker-supplied HTTP parameters. Threat model: local config control = not remotely exploitable. | No R&D action. Config-taint detector needs a privilege-level filter to suppress when source is operator config (env var / admin settings API), not inbound HTTP request body. |
| 2026-05-20 | https://github.com/zitadel/zitadel | security:protobuf_any_unguarded_decode × 4 — `internal/api/grpc/admin/import.go:235,246`, `internal/protoc/protoc-gen-authoption/main.go:41`, `internal/protoc/protoc-gen-zitadel/main.go:52` | 3% | All Informational / `lattice_gap_proposal`. `import.go` sites handle admin bulk import; `protoc-gen-*` are build-time code generators invoked by `protoc`, not production runtime. No user-supplied protobuf Any payload reaches these sites without admin access. | Same FP class as grafana Sprint 154: tighten `find_protobuf_any_reachability` to require `UnmarshalAny`/`UnmarshalTo` on an `anypb.Any` variable, not generic `proto.Unmarshal`. |
| 2026-05-20 | https://github.com/zitadel/zitadel | security:credential_leak × 1 — `apps/login/acceptance/idp/saml/main.go:33` RSA private key (`MIIEpAIBAAKCAQEA0OhbM...`) | 5% | Acceptance test SAML IdP RSA private key — self-signed key used solely to sign test SAML assertions in CI acceptance tests. No production credential. `invariant_violation_proof` attached by engine (test-path context). Responsible disclosure to security@zitadel.com required if promoting, but test fixtures do not constitute leaked credentials in bug bounty scope. | Per governance: credential_leak in any directory is always recorded. Zitadel security contact: security@zitadel.com. Submit only if program scope explicitly covers test credential leakage. |
| 2026-05-20 | https://github.com/zitadel/zitadel | security:model_weight_backdoor × 2 — `apps/login/src/helpers/colors.ts:1`, `console/src/app/modules/policies/private-labeling-policy/private-labeling-policy.component.ts:1` | 0% | FP — neither file loads ML model weights. `colors.ts` is a color palette utility; `private-labeling-policy.component.ts` is an Angular UI component. `model_weight_backdoor` detector fires on TypeScript files that happen to match a structural heuristic. No ML weight loading present. | Detector needs file extension + content gate: only fire in `.py`/`.ipynb`/`.safetensors`/`.onnx` contexts where model loading libraries are imported. |
| 2026-05-20 | https://github.com/zitadel/zitadel | security:unauthenticated_debug_endpoint × 2 — `apps/login/scripts/server.mjs:69,94` | 1% | Informational — `apps/login/scripts/server.mjs` is a Node.js dev-server script for local development / CI test orchestration, not a production Zitadel deployment artifact. Dev servers expose debug endpoints by design. | No production attack surface. Detector should skip `scripts/` directory and `*.mjs` dev server files. |
| 2026-05-20 | https://github.com/zitadel/zitadel | security:dom_xss_innerHTML × 1 — `internal/api/ui/login/static/resources/scripts/avatar.js:182` | 1% | `initials` is `split[0].charAt(0) + (split[1] ? split[1].charAt(0) : "")` — at most 2 ASCII characters of username initials. `charAt(0)` returns a single character; cannot carry an HTML/JS injection payload. Informational severity correct. | Narrow `dom_xss_innerHTML` to suppress when the assigned value is the result of `charAt()` (provably single-character output). |
| 2026-05-20 | https://github.com/zitadel/zitadel | security:tls_verification_bypass × 1 — `apps/login/acceptance/oidcrp/main.go:61` | 1% | Acceptance test OIDC relying-party sets `InsecureSkipVerify: true` — expected test behavior for local self-signed cert acceptance testing. `invariant_violation_proof` correctly attached. | No action. Test-path suppression working correctly for this finding class. |
| 2026-05-20 | https://github.com/zitadel/zitadel | security:clock_skew_auth_split_brain × 1 — `console/src/app/pages/projects/apps/app-detail/app-detail.component.ts:453` + security:oauth_account_fusion_pretakeover ×2 (SQL migration files `013_user_table/down.sql`, `013_user_table/up.sql`) `lattice_gap_proposal` | 2% | `clock_skew` in Angular console component — client-side UI only, no server-side auth enforcement path. SQL migration `lattice_gap_proposal` findings: schema migration files define table structure, not runtime code; engine correctly emits `lattice_gap_proposal` (no reachable execution path from migration DDL). | No production exploit path. `clock_skew` detector requires server-side auth enforcement path; SQL migration files should be excluded from `oauth_account_fusion` scan scope. |
| 2026-05-21 | https://github.com/ory/hydra | Sprint 156 hunt batch: `jwt_validation_bypass` ×4, `oauth_missing_state_validation` ×20, `oauth_account_fusion_pretakeover` batch, `non_constant_time_comparison` ×28, `ssrf_dynamic_url` ×2 | 5% | No autonomous bounty target confirmed. Ory's public policy documents security SLAs for Ory Network and enterprise customers; Apache 2.0 users get no formal SLA. JWT sites are Fosite/OIDC protocol surfaces: request-object `alg=none` is explicitly permitted by OIDC when client policy allows it, `private_key_jwt` checks the registered client signing algorithm before key return, and the internal go-jose wrapper requires an `UnsafeAllowNoneSignatureType` magic key before skipping signature verification. OAuth state findings are mostly OAuth-server/provider/storage files, not client redirect callbacks. | Add a protocol-aware guard: suppress `oauth_missing_state_validation` unless inbound redirect handler context is proven; suppress Fosite request-object `alg=none` when the surrounding code checks registered client signing policy or returns the unsafe-none magic key only from a standards-permitted request-object path. |
| 2026-05-21 | https://github.com/supertokens/supertokens-core | Sprint 156 hunt batch: `oauth_missing_state_validation` at `OAuthTokenAPI.java`, `oauth_account_fusion_pretakeover` batch, `non_constant_time_comparison` ×67, config taint ×5 | 5% | GitHub reports no security policy for `supertokens-core`; no Bugcrowd/HackerOne bounty target found. `OAuthTokenAPI.java` is the OAuth token endpoint/proxy path after authorization, not a browser callback that should validate `state`; it parses `grant_type`, client auth, and refresh/authorization-code token exchange fields. OAuth state is a client redirect callback responsibility, not token endpoint semantics. | Add Java token-endpoint guard: `oauth_missing_state_validation` must not fire on `/oauth/token` handlers unless the same handler consumes a browser redirect `state` parameter; route token-exchange endpoints to LOW_YIELD unless a CSRF browser callback is proven. |
| 2026-05-21 | https://github.com/goauthentik/authentik | Sprint 156 hunt batch: `oauth_missing_state_validation` mass batch, `jwt_validation_bypass` ×2, `oauth_account_fusion_pretakeover` batch, config taint mass batch, `embedding_trust_transposition` ×40 | 5% | Authentik's policy offers responsible disclosure and explicitly states no monetary bounties. The mass OAuth-state batch is dominated by migrations, generated TypeScript client files, models, tests, and source-provider configuration files. `internal/outpost/flow/session.go` uses `ParseUnverified` only to read a session-cookie shape for flow state and falls back to a pseudo-JWT during tests; it is not a verifier accepting attacker-forged auth. `internal/web/static.go` is already demoted by `jwt_keyfunc_oracle`. | Add migration/generated-client/test path suppression to OAuth state classification and a `ParseUnverified` session-shape read guard for non-auth verifier contexts. Authentik remains disclosure-only unless a concrete account takeover path is reproduced. |
| 2026-05-22 | https://github.com/Uniswap/docs | no_findings | 0% | Docs repository is explicitly out of scope for the Uniswap Immunefi program and the hunt returned zero findings. | No bounty action. Preserve as clean negative sample for docs-only scope filtering. |
| 2026-05-22 | https://github.com/aave/aave-address-book | security:config_taint_[search, setSearch] (`ui/src/components/Search.tsx`) | 3% | Aave address-book is in scope, but this finding is client-side search-state taint with `lattice_gap_proposal`; no server-side auth, funds, or governance impact is proven. Triage Empathy: client-side state control is not a bounty-grade exploit without a privileged sink. | Add UI-state config-taint guard: demote React `useState` search/filter flows unless the value reaches a server-side privileged sink or transaction construction path. |
| 2026-05-22 | https://github.com/smartcontractkit/chainlink-docs | security:config_taint_* ×22 + security:dom_xss_innerHTML ×3 (`source/javascripts/lib/_jquery.js`, docs JS) | 0% | `chainlink-docs` is explicitly out of scope for the Chainlink Immunefi program. Findings are vendored jQuery/docs-site JavaScript with focus-area mismatch; no oracle, access-control, or price-aggregation impact. The requested `chainlink-contracts` URL returned GitHub `Repository not found`, so this was the next accessible SmartContractKit ledger target. | Do not promote. Add scope prefilter for Immunefi docs repositories and vendored `jquery.js` docs assets before hunt output. |
| 2026-05-24 | https://github.com/keycloak/keycloak | security:non_constant_time_comparison — `Argon2PasswordHashProvider.java:102` uses `encoded.equals(secretData.getValue())` without `MessageDigest.isEqual()` or `Arrays.constantTimeAreEqual()` (CWE-208) | 5% | **Sprint 171 timing analysis**: Argon2 KDF computation (~100ms) dominates String.equals() comparison timing (~60-100ns) by a factor of 10⁶. N=5000 requests are insufficient to isolate the sub-millisecond equals() delta from Argon2 KDF variance and JVM GC noise. The timing signal is irrecoverable without millions of requests — practically unexploitable. The vulnerability class is technically correct but the KDF completely masks the side-channel. `ReachabilityProof` still assigned by proof_obligation (PasswordHash marker present), but bounty payoff is functionally zero. | Demoted 40%→LOW_YIELD. Engine should add an Argon2/bcrypt/scrypt KDF-dominance suppressor: when password hash uses a KDF with configurable iteration count, the timing comparison class should be automatically downgraded to LatticeGapProposal unless a non-KDF comparison path is separately detected. Keycloak HackerOne remains active but this specific finding is not promotable. |
| 2026-05-25 | https://github.com/electroneum/electroneum-sc | Sprint 174 P17-3B hunt batch: `java_deser_allowlist_bypass` ×3 (false positive — Go/C++ codebase), `lcm_malloc_integer_truncation` ×8 (secp256k1 `tests.c` — test code only), `lcm_use_after_free` ×1 (secp256k1 library — extensively formally verified, no exploitable path), `non_constant_time_comparison` ×13 (blockchain network jitter ~10ms masks <100ns timing delta — impractical), `ssrf_dynamic_url` ×4 (pending verification whether URL originates from attacker-controlled RPC param or operator config), `ics_default_credential` ×6 (genesis/bootstrap credentials — intentional blockchain bootstrapping), `protobuf_any_unguarded_decode` ×2 (Informational — no proof class required), `config_taint_*` ×2 (Python helper scripts, not production RPC handlers), `unpinned_asset` ×5 (CI/CD supply chain — requires secondary attacker-controlled Action compromise) | 5% | All electroneum-sc findings below CANDIDATE threshold. `ssrf_dynamic_url` and `unauthenticated_debug_endpoint` routed to CANDIDATE for follow-up. Secp256k1 formally verified library — use-after-free and integer truncation in test code are not exploitable in production. Timing side-channel is masked by blockchain network variance. Promote `unauthenticated_debug_endpoint` if debug port confirmed bound to non-loopback. |
| 2026-05-25 | https://github.com/trustwallet/wallet-core | Sprint 174 P17-3B hunt batch: all findings Informational — `ffi_memory_corruption` ×8, `ffi_unsafe_deref_unguarded` ×20, `java_deser_allowlist_bypass` ×86 (false positive in C++/Rust codebase with Java JNI bindings), `lcm_double_free` ×2, `lcm_off_by_one_loop` ×8, `ld_preload_injection` ×4 (build/test scripts, local attacker required), `protobuf_any_type_field` ×4, `unauthenticated_debug_endpoint` ×2 (Informational), `unsafe_string_function` ×3, `bounded_overflow_witness` ×1 | 3% | All findings rated Informational by engine. TrustWallet Binance bug bounty program is in scope but no finding exceeded Informational severity. FFI findings now carry LatticeGapProposal proof class (P17-3B landed this session) but are still rated Informational due to lack of public-ingress proof path in wallet-core's static library build model. Re-hunt after P17-3C adds ingress-aware FFI taint tracing. |
| 2026-05-25 | https://github.com/ClickHouse/ClickHouse | Sprint 174 P17-3B hunt batch (src/Access + src/Server/HTTP sparse): `config_taint_*` ×5 (internal array subscript operations `mapping[index]` — index is compile-time enum, not user-controlled), `java_deser_allowlist_bypass` ×4 (C++ DBMS codebase — false positive, likely in Java JDBC test fixtures or contrib connector) | 5% | ClickHouse Bugcrowd in-scope. `optimizer_phantom_authority` ×3 routed to CANDIDATE. Config-taint and java-deser findings are structural false positives — suppress via `is_excluded_hunt_entry` path guard for C++ files in the `src/Access/` namespace when finding ID is `java_deser_allowlist_bypass`. |
| 2026-05-26 | https://github.com/ClickHouse/ClickHouse | Sprint 175 P17-3C: `security:optimizer_phantom_authority` × 3 — DEMOTED from CANDIDATE. Full clone investigation confirmed: `checkAccess` enforced at interpreter level in `InterpreterSelectQuery.cpp:1065`; row-level policy filter correctly applied in `QueryAnalyzer.cpp:5180-5196` BEFORE any optimizer rewrite step. No bypass path found. | 2% | Access enforcement confirmed upstream of optimizer. `checkAccess` at interpreter layer → row policy injected in Analyzer resolver → optimizer operates on already-filtered plan nodes. `optimizer_phantom_authority` detector fires on access-state-machine transitions that are correctly ordered. Structural FP for this codebase. | Add ClickHouse-specific suppressor to `optimizer_phantom_authority` detector: if `checkAccess` call is found in `src/Interpreters/` before query planning, demote to InvariantViolationProof. |
| 2026-05-26 | https://github.com/yearn/yearn-vaults | Sprint 175 hunt: `security:unauthenticated_debug_endpoint` at `yarn.lock:531` — engine matched debug/pprof string pattern inside npm lockfile metadata entry | 2% | `yarn.lock` is a package manager lockfile, not a production route registration. Pattern match on package name/URL fragment inside lockfile. Triage Empathy: lockfile contains string data from npm registry, not executable code. | Add lockfile path suppressor to `debug_endpoint` detector: skip findings in `yarn.lock`, `package-lock.json`, `Cargo.lock`, `Gemfile.lock`, `poetry.lock` path matches. |
| 2026-05-26 | https://github.com/apache/kafka | Sprint 175 hunt: `security:unpinned_asset` at `gradle/resources/dependencycheck-suppressions.xml:18` — CVE reference URL in OWASP Dependency Check config file; `security:java_deser_allowlist_bypass` ×multiple — Kafka `Deserializer<T>` interface (`CompletedFetch.java`, `ShareCompletedFetch.java`, `ClusterResourceListener.java`, `EndTransactionMarker.java`) — NOT Java ObjectInputStream deserialization | 3% | `unpinned_asset`: OWASP Dependency Check suppressions XML contains CVE URLs, not shell download scripts. FP by design. `java_deser_allowlist_bypass`: Kafka uses its own binary protocol Deserializer interface, not Java Object Serialization. Engine fix shipped this sprint: `OBJECT_DESER_CONTEXT_MARKERS` guard added to `java_deser_guard.rs` — bare `deserialize(` now requires ObjectInputStream/Serializable/readObject context in the same file to emit. Future hunts against Kafka will suppress these FPs automatically. | Engine fix applied (context guard in `java_deser_guard.rs`). Remaining gap: `unpinned_asset` detector needs Dependency Check XML format recognition to skip CVE URL references in suppression files. |
| 2026-05-26 | https://github.com/hashicorp/vault | Sprint 175 hunt: `security:ssrf_dynamic_url` at `http/testing.go:75` — test file path; `security:unpinned_git_dependency` at `ui/package.json:227` (`@hashicorp/vault-client-typescript: github:hashicorp/vault-client-typescript`) — HashiCorp-owned package in HashiCorp-owned repo | 3% | Test file SSRF: `http/testing.go` path flag suppresses via non-production context. `unpinned_git_dependency`: package is HashiCorp's own first-party TypeScript client, hosted in their own GitHub org — repojacking risk is effectively zero. HackerOne program does not accept first-party package pinning findings. | Test-file suppressor for SSRF already exists in path classifier. Add `unpinned_git_dependency` suppressor for cases where org name in package URL matches the repo org (first-party dependency). |
| 2026-05-28 | https://github.com/openai/codex | Sprint 181 hunt: `security:financial_pii_to_external_llm` Informational — `codex-cli/scripts/run_in_container.sh` | 2% | Threat Model Pre-Filter: shell script, no PII ingestion or processing execution path in a container bootstrap script. Informational severity with no proof class elevation. LLM sink is the containerized Codex model itself, not an external data-exfiltration endpoint. Not bounty-eligible under OpenAI Bugcrowd scope. | No action. Shell script container bootstrap files are not PII processing surfaces. |
| 2026-05-28 | https://github.com/chime/terraform-aws-alternat | Sprint 181 hunt: `security:ci_persistence_vector` Informational ×3 — `scripts/alternat.sh` | 2% | Threat Model Pre-Filter: AWS NAT gateway automation script. `ci_persistence_vector` fires on shell script AWS CLI invocations that could be chained to persist infrastructure state. All three sites are in the NAT route failover logic — legitimate infrastructure automation, not attacker-controlled persistence. Informational severity, no proof class. Not bounty-eligible. | No action. Add AWS NAT automation pattern to `ci_persistence_vector` suppressor for infrastructure failover scripts. |
| 2026-05-28 | https://github.com/smartcontractkit/chainlink-contracts | Sprint 182 hunt: no findings — 0 detector hits across Solidity/TypeScript smart contract library | 0% | No detector fires. Solidity-specific vulnerability classes (reentrancy, integer overflow) are not in current grammar set. Target is LOW_YIELD by coverage gap. | Add Solidity grammar coverage to forge detector suite for reentrancy and integer overflow patterns (P-tier candidate). |
| 2026-05-28 | https://github.com/immutable/ts-immutable-sdk | Sprint 182 hunt: `security:dom_xss_innerHTML` Informational ×1, `security:ssrf_dynamic_url` Informational ×6, `security:config_taint_*` Informational ×8, `security:non_constant_time_comparison` Informational ×1, `security:oauth_account_fusion_pretakeover` Informational ×41 | 2% | All findings Informational. Tri-Ledger routing: Informational severity → LOW_YIELD. `oauth_account_fusion_pretakeover` fires on Passport OAuth callback/hooks but no state parameter injection proof path established. SSRF findings are SDK-configured base URLs, not user-controlled. Immunefi in-scope but no finding reaches KevCritical + ReachabilityProof threshold. | `oauth_account_fusion_pretakeover`: requires ExploitWitness synthesizing attacker-controlled `state` parameter linkage to confirm pre-account-takeover path. |
| 2026-05-28 | https://github.com/mattermost/mattermost-plugin-confluence | Sprint 182 hunt: `security:path_traversal` High ×4 — `server/atlassian_connect.go:37`, `server/controller.go:92`, `server/plugin.go:65`, `server/plugin.go:169` | 3% | Threat Model Pre-Filter: all 4 sites use `filepath.Join(bundlePath, "assets", ...)` where `bundlePath` comes from `p.API.GetBundlePath()` / `config.Mattermost.GetBundlePath()` — Mattermost server API, NOT user-controlled. Taint source fails gate 1 (not user-controlled at network boundary). Structural FP class: `filepath.Join` with server-API-provided base path. | Add `GetBundlePath()` return value to path_traversal sanitizer registry as a trusted-path-base suppressor. |
| 2026-05-28 | https://github.com/pinterest/querybook | Sprint 181 hunt: `security:rag_trust:unprioritized_retrieval` Informational (`base_vector_store.py:88` → `llm.invoke`); `security:oauth_missing_state_validation` Informational ×2 (`oauth_auth.py`, `okta_auth.py`) downgraded by `blueprint_auth_hook_covers_route` oracle | 3% | RAG retrieval: `unprioritized_retrieval` is Informational — no proof that unranked chunks reach a privileged decision path. OAuth state findings downgraded by Sprint 140 oracle (Blueprint auth hook covers the route-level CSRF gate). No finding exceeds Informational severity. Pinterest HackerOne in-scope but confidence too low for submission. | Oracle correctly demoted OAuth state findings. RAG trust chain requires proof that retrieval output reaches an LLM response exposed to untrusted consumers without sanitization. |
