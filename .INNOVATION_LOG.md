# The Decadal Blueprint

OpSec blackout — gitignored. Local only. Active, unbuilt architecture only
(per `.agent\\\\\\\\\\\\\\\_governance/rules/log\\\\\\\\\\\\\\\_hygiene.md`).

**Audit baseline**: v10.2.0-alpha.6 — 23 tree-sitter grammars; interprocedural
IFDS taint solver with sanitizer registry across 14 languages;
Z3 SMT path-feasibility refinement (`exploitability::Z3Solver::refine`);
HTTP-ingress AEG curl synthesis with model-extracted payloads bound into
`ExploitWitness::repro\\\\\\\\\\\\\\\_cmd`; OCI/Docker + iOS IPA + APK + ASAR + npm + JAR +
sourcemap artifact ingestion via `janitor hunt`; Bugcrowd VRT markdown
reporter; framework-aware authorization consistency checker (Spring Boot,
Flask/FastAPI, Express) with 80%-threshold dominant-requirement anomaly
detection and route-surface extraction; cross-file taint catalog (rkyv
zero-copy, BLAKE3-sealed); Wasm BYOP sandbox (wasmtime, fuel + epoch
bounded, ML-DSA-65 publisher signing); AhoCorasick byte-level + AST-walk
detectors; SLSA Level 4 provenance (SHA-384 + ML-DSA-65); deobfuscation
spine; structural clone hashing; logic regression guardian; HMAC-SHA-384
audit ledger; EdDSA JWT analysis tokens; multi-tenant RBAC; Atlassian
ASPM dedup; jaq filter pipeline; offline-capable seed corpus.

**What wins today**: Pattern-level interception at scale plus interprocedural
taint with path feasibility. Fast, deterministic, zero-false-negative on
known-bad patterns. Provenance-sealed receipts. The only offline engine that
produces curl-form exploit witnesses for HTTP ingress points. Competitive
against Semgrep/CodeQL on syntax-level rules and beats them on
cross-function reachability — with zero cloud dependency.

**What loses today**: Semantic CORRECTNESS. The engine proves reachability
via Z3 but cannot yet prove safety invariants. It models a base sanitizer
registry but not framework-emergent or customer-authored sanitizers. It
traces direct parameter flows but not through class hierarchies, generic
type parameters, or framework middleware pipelines. Sprint 171 ships Maven `pom.xml`, Gradle `build.gradle`/`build.gradle.kts`,
C# `.csproj`, and Ruby `Gemfile.lock` manifest parsers — Java Auth0 SDKs,
Spring starters, ASP.NET middleware, and Ruby authentication gems now
participate in phantom-call detection and zombie-dep cross-referencing.
It generates HTTP curl exploits but not serialized-blob, protocol-buffer,
smart-contract, parser-payload, or agentic-tool-chain witnesses. It does
not reason about cryptographic protocol composition, hardware
side-channels, neural model weights, LLM provenance, or multi-account
adversarial swarms. Every $100k+ bounty in 2026 requires proving a
*chain*; every $1M+ bounty in 2028 will require proving an *invariant*.

## Implementation Queue — Sprint-Ready

**Read this section first on every code sprint.** Items here are fully
specified, buildable without hardware constraints, and have no blocking
prerequisites. Pick from the top.

After completing an item: mark it `[DONE vX.Y.Z]` inline, record it in
`docs/CHANGELOG.md`, and delete the entry from this section.

---

## Phase 1: Exploit Evidence Finality (0-3 Months)

### P2-4 — Binary and Bytecode Recovery Lane (Tier 3 Remaining)

**Strategic objective**: Catch vulnerabilities in compiled artifacts that
never appear in source: native mobile libraries, appliance firmware,
packed plugins, closed-source SDKs. Tier 1 (goblin import-table triage)
shipped in Sprint Batch 51.

**Remaining build**:

* **Tier 3 (deep recovery, optional)**: Headless Ghidra integration via
`analyzeHeadless` subprocess (same pattern as jadx APK ingestion).
Import Ghidra's decompiled C pseudocode, run existing C/C++ detectors
over it.

**Crates**: `goblin 0.9` (shipped), headless Ghidra subprocess bridge.

**Models**: Lifted C pseudocode from Ghidra's decompiler pipeline; run
existing C tree-sitter and AhoCorasick detectors over decompiled output.

## Phase 5: Cryptographic Moat (18-36 Months)

The defensive war in 2028+ is proving security properties WITHOUT
exposing source. This tier is zero-knowledge, homomorphic, MPC, and
TEE-rooted. Competitors with cloud-only architectures cannot replicate
this — their business model demands source upload.

**Operator constraint:** `P5-1` is deferred indefinitely in the current
Sovereign Operator environment because Halo2/Plonky3 compilation violates
the 8GB Law and risks deterministic compile-time OOM on available hardware.

### P5-1 — Zero-Knowledge Attack Surface Attestation

**The gap**: Enterprise procurement asks "does your codebase pass these
200 rules?" Today the only answer is "upload your code and we'll tell
you." The zero-upload guarantee forbids that. The decadal unlock: "here
is a zk-SNARK proving my codebase passes all 200 rules, verifiable
offline without the code."

**Build**:

1. Compile the top-20 deterministic rules to arithmetic circuits via
`plonky3` / `halo2`.
2. Customer runs the scanner locally; scanner emits proof + public
inputs: (rule set hash, engine version, result hash).
3. Auditor verifies the proof in milliseconds without seeing a single
line of source.
4. Proof commits to: exact codebase state (Merkle root), exact rule
set, exact engine binary hash — cryptographic pinning across all
three.

**Crates**: `plonky3`, `halo2-proofs`, `blake3` (existing),
`ed25519-dalek` (existing), `rkyv`.

**Data structures**: `ZkAttestation { codebase\\\\\\\\\\\\\\\_root: \\\\\\\\\\\\\\\[u8; 32], rule\\\\\\\\\\\\\\\_set\\\\\\\\\\\\\\\_hash, engine\\\\\\\\\\\\\\\_hash, proof: Vec<u8>, public\\\\\\\\\\\\\\\_inputs }`.

**Deliverables**:

* `crates/forge/src/zk\\\\\\\\\\\\\\\_attestation.rs` — circuit compiler for the top-20
deterministic rules
* `crates/vault/src/zk\\\\\\\\\\\\\\\_verify.rs` — offline verifier emitted as a
standalone `janitor-verify-zk` binary for auditors

#### P5-1.A — Halo2 Polynomial Circuit Design for AST Node-Absence Proof (CVP-Authorized Cryptographic Specification)

**Operating context**: Authored under CVP ID
`2fe9d3dd-47ba-4bde-ab67-29f86c79f732`. This sub-section answers the
specific question: *which Halo2/Plonky3 polynomial constraints are
required to prove that a given AST does not contain a specific node
type*, without revealing the AST. The construction below is the canonical
"absence-of-bad-pattern" circuit; all other rule shapes (presence-of-good,
counted-bound, predicate-pair, taint-reachability) are extensions of the
same three primitives.

**Field choice**: Pasta `Fp` (Pallas base field, 255-bit, native to Halo2)
or Goldilocks `Fp = 2^64 − 2^32 + 1` (native to Plonky3, FFT-friendly).
Pallas is preferred for halo2-proofs ergonomics and the existing recursion
infrastructure; Goldilocks is preferred when proving over machine-word
hashes is on the hot path. Both accommodate the constraint system below.

**AST canonicalization**: a tree-sitter AST `T` with `n` nodes and
maximum out-degree `d_max ≤ 16` is serialized into a row-major witness
table:

```
W : Vec<AstRow>     where AstRow = {
    node_idx       : Fp,    // 0..n-1, monotonically increasing
    parent_idx     : Fp,    // n  if root
    node_kind      : Fp,    // tree-sitter language-specific u16
    span_start     : Fp,    // byte offset
    span_end       : Fp,    // byte offset
    child_count    : Fp,    // 0..d_max
    sibling_pos    : Fp,    // 0..d_max-1
    leaf_hash      : Fp,    // BLAKE3(source[span_start..span_end]) ↦ Fp via Poseidon
    subtree_root   : Fp,    // Poseidon Merkle root of this subtree
}
```

`n` is fixed at circuit-compile time (e.g. `n = 2^17` rows); files with
fewer nodes are zero-padded; files with more nodes are recursively
sharded into proof-aggregated halves (Halo2 recursion via accumulator
folding). The single public input committing to the codebase is
`Poseidon(file_path_hash || subtree_root_of_root)`.

**Three primitive gate equations**:

**1. Lookup gate — "node_kind ∉ FORBIDDEN"** (the absence proof):

The forbidden set `F = { k_1, k_2, …, k_m }` (e.g.
`{tree_sitter_javascript::node_kinds::EVAL_CALL_EXPRESSION,
node_kinds::FUNCTION_CONSTRUCTOR, node_kinds::WITH_STATEMENT}`) is
compiled into a *fixed lookup table* `T_forbidden : Vec<Fp> = [k_1, …,
k_m]` baked into the circuit's constants. The gate enforces:

```
∀ row r ∈ W :  r.node_kind  ∉  T_forbidden
```

In Halo2, this is expressed as a *negative-lookup* via a complementary
allowed table `T_allowed = AllNodeKinds \ T_forbidden` and a standard
`lookup_any`:

```
meta.lookup_any("node_kind_allowed", |meta| {
    let kind = meta.query_advice(node_kind_col, Rotation::cur());
    let table = meta.query_fixed(allowed_kind_table, Rotation::cur());
    vec![(kind, table)]
});
```

Equivalently, in Plonky3 PLOOKUP form: a permutation argument over
`(W.node_kind, T_allowed)` such that every `W.node_kind` value appears
in `T_allowed`. The grand-product polynomial `Z(X)` over the multiset
relation enforces this in `O(n · log n)` proving time, `O(1)`
verification time. Prover cannot fabricate a row whose kind is in
`F` because no fixed-table position would satisfy the multiset relation.

**2. Permutation argument — "AST shape integrity"** (the no-row-fabrication proof):

The prover must demonstrate that the witness rows correspond to an
*actual* tree, not a fabricated table that omits forbidden nodes. The
Poseidon Merkle subtree root in `r.subtree_root` is derived recursively
from children:

```
∀ non-leaf row r :
    r.subtree_root = Poseidon(
        r.node_kind,
        r.child_count,
        Poseidon_chain(child_subtree_roots[0..r.child_count])
    )
```

This is enforced by a custom Halo2 chip that runs Poseidon in-circuit
(R = 8 full + P = 56 partial rounds for Pasta, `t = 5` width).
Children are located via a permutation argument over
`(parent_idx, sibling_pos) ↔ node_idx`: a row at `(p, i)` is the
`i`-th child of row `p` if and only if there exists exactly one row
whose `node_idx` field equals the `i`-th entry of the children-table
sorted by `(parent_idx, sibling_pos)`. The permutation argument is the
standard Halo2 plonk permutation (`Σᵢ (αᵢ + xᵢ + β·yᵢ) = Σᵢ (αᵢ + xσ(i) +
β·yσ(i))`) over the columns `(node_idx, parent_idx, sibling_pos)`.

The *root commitment* exposed as a public input,
`subtree_root_of_root := W[0].subtree_root` (with `parent_idx = n`,
indicating the root), pins the entire tree shape to a single 255-bit
field element. Tampering with any single row (omitting a forbidden
node, swapping parent edges, fabricating a subtree) cascades into a
Poseidon hash mismatch and the verifier rejects.

**3. Range / domain gate — "no out-of-grammar nodes"** (the no-impossible-kind proof):

A simple range check `r.node_kind < MAX_KIND_FOR_LANGUAGE` enforced
via a 16-bit decomposition gate. In Halo2:

```
let bits = (0..16).map(|i| {
    meta.query_advice(node_kind_bit_cols[i], Rotation::cur())
}).collect::<Vec<_>>();
meta.create_gate("node_kind_range", |meta| {
    let kind = meta.query_advice(node_kind_col, Rotation::cur());
    let reconstructed: Expression<Fp> = bits.iter().enumerate()
        .fold(Expression::Constant(Fp::zero()), |acc, (i, b)| {
            acc + b.clone() * Expression::Constant(Fp::from(1u64 << i))
        });
    let bool_constraints: Vec<Expression<Fp>> = bits.iter()
        .map(|b| b.clone() * (Expression::Constant(Fp::one()) - b.clone()))
        .collect();
    let mut gates = vec![kind - reconstructed];
    gates.extend(bool_constraints);
    gates
});
```

This prevents the prover from injecting node-kind values outside the
language's tree-sitter grammar enum (which would otherwise let them
dodge the lookup gate by using a sentinel kind not in `F` ∪ `T_allowed`).

**Public inputs (the verifier sees only these)**:

```rust
pub struct ZkAttestationPublic {
    /// BLAKE3(file_path) ⊕ Poseidon(subtree_root_of_root)
    pub codebase_root: [u8; 32],
    /// Poseidon hash of (rule_id, forbidden_node_kinds, language_id)
    pub rule_set_hash: [u8; 32],
    /// BLAKE3 of the engine binary
    pub engine_hash: [u8; 32],
    /// Verdict: 1 = no forbidden nodes present; 0 = present (proof fails)
    pub verdict: Fp,
}
```

The auditor verifies in `O(log n)` (Halo2 recursion) without seeing
`W`, the source bytes, or any AST structural detail beyond the root
commitment. Two codebases producing identical root commitments are
provably structurally identical (modulo Poseidon collision resistance,
which reduces to the discrete-log assumption on Pasta).

**Recursion / aggregation**: large codebases (n > 2^17 nodes) are
sharded; each shard produces a Halo2 proof; shards are recursively
folded via the Halo2 accumulator-IPA scheme (`halo2_proofs::recursion`).
A repository-level proof is a single 4 KiB blob attesting absence of
forbidden nodes across every file. Verification cost is constant in
shard count.

**Extensions for non-trivial rules**:

* **Counted-bound rule** (e.g. "no more than 3 unsafe blocks"): replace
  the lookup gate with a *grand-sum* gate computing
  `Σᵢ [r_i.node_kind == UNSAFE_BLOCK]` and assert the sum ≤ 3 via a
  range gate.
* **Pair predicate** (e.g. "no `eval` whose argument is a function
  parameter"): two coupled lookup gates over `(node_kind, parent_kind,
  arg_origin_label)` — requires lifting the IFDS taint label into the
  witness table (extends seamlessly: `r.taint_label : Fp = Poseidon(label_string)`).
* **Reachability** (e.g. "no source flows into sink"): a permutation
  argument over the IFDS `(seed_fact, summary_chain, reached_sink)`
  triples; reuses the same Poseidon Merkle infrastructure with a
  `IfdsTraceRow` extension.

**Plonky3 alternative**: for environments where halo2's recursion stack
is unavailable, Plonky3 with the Goldilocks field, the same constraint
system, and FRI-based commitments produces faster proofs (≈3× over
halo2 on commodity hardware) at the cost of larger proof size
(≈45 KiB vs 4 KiB). The choice is operator-driven via
`ForgeConfig::zk_backend = { Halo2 | Plonky3 }`.

**Crates** (specific): `halo2_proofs = "0.3"`, `halo2_gadgets = "0.3"`
(Poseidon chip), `pasta_curves = "0.5"` (Pallas/Vesta IPA);
alternatively `plonky3 = "0.1"` with `p3-goldilocks`, `p3-poseidon2`,
`p3-fri`. Crate layout:

```
crates/forge/src/zk_attestation/
├── mod.rs              # public API
├── ast_witness.rs      # AST → AstRow witness lift
├── circuit.rs          # Halo2 ConstraintSystem definition
├── poseidon_chip.rs    # in-circuit Poseidon (or use halo2_gadgets)
├── recursion.rs        # shard accumulator
└── plonky3_backend.rs  # alternative backend
```

**Performance budget** (engineering targets):

* Witness generation: ≤ 50 ms / 2^17-node shard on M2 Pro.
* Proving time: ≤ 8 s / shard (halo2 IPA) or ≤ 2 s / shard (Plonky3 FRI).
* Verification: ≤ 5 ms / proof, regardless of shard count after recursion.
* Proof size: ≤ 4 KiB (halo2 IPA) or ≤ 45 KiB (Plonky3).

**Determinism**: Poseidon round constants are baked into the crate
(constant-time table); IPA / FRI commitments use a transcript seeded by
`BLAKE3(rule_set_hash || engine_hash)` so the proof is reproducible
bit-for-bit across runs given identical inputs.

**Why this beats the competitive landscape**: no SAST competitor today
ships a circuit-level absence proof. CodeQL, Semgrep, Snyk, Checkmarx
all require source upload to verify their findings. The construction
above lets an auditor verify "this codebase has no `eval` calls" in
milliseconds without ever seeing the source — the canonical zero-upload
unlock for FedRAMP High, DoD IL6, and EU CRA Article 11 attestation.

### P5-2 — Homomorphic Rule Evaluation (FHE-Limited Compliance Tier)

**The gap**: Some customers cannot run our binary on their source
(classified environments, financial-sector intra-day trading desks).
They need to send *encrypted* code to a trusted third party that returns
*encrypted* findings.

**Build**:

1. Represent AST nodes as TFHE-encrypted polynomials.
2. Evaluate a fixed subset of rule checks (exact-match AhoCorasick
patterns, integer-arithmetic detectors) over the ciphertext.
3. Return encrypted finding set; customer decrypts locally.
4. Not universal — scope targets the "compliance checkbox" tier for
regulated industries where homomorphic evaluation is acceptable
latency.

**Crates**: `tfhe`, `concrete-core`, `rkyv`.

**Deliverables**:

* `crates/forge/src/fhe\\\\\\\\\\\\\\\_rules.rs` — encrypted-AST evaluator for the
top-5 match-pattern rules (initial scope)
* `crates/gov/src/fhe\\\\\\\\\\\\\\\_endpoint.rs` — API endpoint accepting
ciphertext-in / ciphertext-out

### P5-3 — Indistinguishability Obfuscation (iO) for Signed Rules

**The gap**: Customer-authored Wasm rules live in clear — competitors
reading them learn customer IP. Customer secret detection heuristics are
intellectual property. iO lets us distribute rules whose *function* can
be computed but whose *source* cannot be recovered.

**Build** (staged; frontier research):

1. **Phase A — Witness encryption (deployable)**: customer signs
a policy statement; rule fires only when witness verifies. Weaker than
iO but practical today with `bulletproofs`.
2. **Phase B — Garbled-circuit rule execution**: rule logic expressed as
boolean circuit, garbled per customer key; Janitor evaluator runs
garbled-circuit evaluation without seeing the underlying logic.
3. **Phase C — Practical iO** (long-tail; lattice-based): track the
literature; integrate once a plausibly quantum-safe iO construction
ships.

**Crates**: `bulletproofs`, `ark-groth16`, research-tier `lattirust`.

**Deliverables**:

* `crates/forge/src/io\\\\\\\\\\\\\\\_rule.rs` — staged wrapper ABI
* Phase A witness-encryption first; Phases B/C incrementally

### P5-4 — TEE-Sealed Analysis Envelopes (SGX / TDX / SEV-SNP)

**The gap**: Customer cannot cryptographically trust that the Janitor
binary running in their CI is the exact binary we signed. Supply-chain
attack on the runner itself evades our provenance stack.

**Build**:

1. Launch Janitor inside an SGX enclave / Intel TDX trust domain / AMD
SEV-SNP VM.
2. Produce an attestation quote bound to:
(engine version, rule catalog hash, receipt Merkle root, enclave
measurement).
3. Quote chains back to Intel / AMD root CA; customer verifier rejects
any binary that does not attest a known-good measurement.
4. Ship a TDX-runner lane in `action.yml` so GitHub-hosted TDX runners
(available on Azure DCsv3 / DCXsv3) can attest automatically.

**Crates**: `sgx-isa`, `sev`, `vtpm`, `webpki`, existing
`ed25519-dalek` + ML-DSA-65.

**Deliverables**:

* `crates/vault/src/tee\\\\\\\\\\\\\\\_attest.rs` — enclave launch + quote parsing
* `action.yml` TEE-runner lane
* `janitor-gov` quote verification endpoint

### P5-5 — Federated Differential Privacy Learning Across Tenants

**The gap**: Customer A discovers a novel supply-chain attack. Customer B
is vulnerable but neither knows. Cross-tenant learning today requires
data exfiltration, which violates the zero-upload guarantee.

**Build**:

1. Each customer's Janitor computes a local update (anomaly signature)
with (ε, δ)-differentially-private noise added at the node level.
2. Secure aggregation (Bonawitz et al. protocol) combines updates
without revealing individual contributions; aggregation keys bound
to existing PQC stack.
3. Global model of attack signatures distributed back as anonymized
rule updates.
4. Critical invariant: no customer source ever leaves the device, only
DP-noised structural aggregates.

**Crates**: `opendp` (DP primitives), `tokio`, existing PQC stack for
secure-aggregation keys.

**Deliverables**:

* `crates/gov/src/fedlearn.rs` — DP aggregation protocol
* `crates/forge/src/anon\\\\\\\\\\\\\\\_signature.rs` — signature emission + cache

### P5-6 — Zero-Knowledge Exploit Brokerage \& On-Chain Bounty Settlement [DEFERRED to Sprint 71]

**The gap**: The bug bounty market (HackerOne / Bugcrowd / YesWeHack / Immunefi) is a manual-trust-anchored marketplace — the hunter trusts the platform to triage fairly, the vendor trusts the platform to obfuscate the report from public view. Both sides eat 20-30% in platform fees and 4-12 weeks of latency. The cryptographic moat: zk-SNARK proof-of-exploit + on-chain escrow that pays out on auditor confirmation, with NO platform middleman.

**Build**:

1. **Proof-of-exploit circuit** (`crates/forge/src/zk\_exploit\_proof.rs` — new module): for each Critical-severity finding with an `ExploitWitness` containing a satisfiable Z3 model, compile the model + the source-to-sink IFDS path into an arithmetic circuit (Plonky3 or Halo2). The public inputs commit to: `(codebase\_root\_hash, rule\_id, severity)`. The proof attests "I ran the engine on a codebase whose merkle-root is X and produced a Critical finding under rule R" without revealing the source.
2. **On-chain escrow contract** (Solidity / Move / Cairo): the vendor deposits the bounty amount; the hunter submits the zk-proof; the contract verifies the proof on-chain (via a precompiled verifier — Plonky3 verification is \~50ms in EVM); on success, payout is automatic and atomic. Pairs with P5-1 (Zero-Knowledge Attack Surface Attestation) for the underlying circuit pipeline.
3. **Auditor handoff**: the contract supports an optional auditor-confirmation tier — for high-value bounties, an auditor signs a "confirmed exploitable in production" attestation; the contract releases the full bounty only after both the zk-proof AND the auditor signature are present.
4. **Sealed disclosure window**: the contract supports a 90-day private window — the proof is verifiable but the public inputs are encrypted to the vendor's pubkey. After 90 days, the vendor's decryption key is revealed and the public can audit. Mirrors responsible-disclosure norms while removing the platform middleman.
5. **Reputation-bonded zk-claims** (anti-spam): hunters stake an Ed25519 / ML-DSA-65 keypair-bound reputation token. Invalid claims slash the stake; valid claims compound it. Bonded staking eliminates the proof-spam griefing vector that any open marketplace must defend against.

**Crates**: `plonky3` / `halo2-proofs`; `ethers-rs` or `alloy` for EVM bindings; existing PQC stack (Ed25519, ML-DSA-65) for off-chain signatures; existing `blake3` for codebase merklization.

**Data structures**:

* `ZkExploitClaim { codebase\_merkle\_root, rule\_id, severity, proof: Vec<u8>, public\_inputs: Vec<Field> }`
* `BountyEscrow { vendor\_address, bounty\_amount, deadline, auditor\_pubkey: Option<\_>, claim: Option<ZkExploitClaim> }`
* `ReputationToken { hunter\_pubkey, staked\_amount, lifetime\_payouts, slash\_count }`

**Deliverables**:

* `crates/forge/src/zk\_exploit\_proof.rs` — circuit compiler for the top-N critical rules
* `crates/cli/src/main.rs::cmd\_submit\_bounty` — operator entrypoint that submits a claim against a deployed escrow
* `contracts/BountyEscrow.sol` — minimal Solidity reference implementation; future: Move / Cairo variants
* Crucible fixture: a synthetic vulnerable codebase + matching escrow + zk-proof submission cycle, asserting payout on success and slash on invalid proof.

**Bounty TAM**: $1B+ — the entire bug bounty market is addressable. Capturing 1% of marketplace fees is $10M+ ARR; cryptographic settlement undercuts HackerOne / Bugcrowd / Immunefi simultaneously and unlocks the autonomous-agent bounty market (P6-9 swarm hunters can now monetize directly without KYC barriers).

\---

## Phase 6: Offensive AI Defense (24-48 Months)

The defensive war in 2028+ is fighting autonomous attackers at machine
speed. Stage 4 attackers split malicious intent across 100 bot accounts;
polymorphic malware rewrites itself across deployments; LLM-generated
code carries provider watermarks and stylometric signatures; ML model
weights ship with embedded backdoors. None of these are covered today.


## Phase 8: Domain Specialization Lanes (Parallel Fast-Track)

Verticals where depth unlocks markets no horizontal SAST can touch.
Each lane is independently shippable and independently monetizable.


## Phase 9: Bio-Computational \& Frontier Extensions

Speculative but architecturally concrete. These are the dark-horse
capabilities — one of them, fully executed, reshapes the market.

### P9-2 — Stateful Hash-Based Post-Quantum Provenance (LMS / XMSS)

**The gap**: ML-DSA + SLH-DSA are our current PQC stack. For
infinite-retention audit logs, stateful hash-based signatures (XMSS,
LMS, RFC 8554 / 8708) offer smaller signatures with forward-secure
properties, at the cost of state management. Enterprise customers with
30-year retention requirements (regulatory archives, legal discovery)
need this or they accept a forward-security gap.

**Build**:

1. Integrate `xmss-rs` or hand-rolled LMS (RFC 8554).
2. Tree-based signing for audit-ledger entries.
3. Collision-resistant without number-theoretic assumptions.
4. Stateful key management (single-use one-way signing tree) with
failure-safe state persistence.
5. Hedged against a future break of ML-DSA / SLH-DSA — hash-based
signatures rely only on the one-wayness of the hash, which is the
most conservative cryptographic assumption we can make.

**Crates**: `xmss-rs`, existing BLAKE3 / SHA-384 stack.

**Deliverables**:

* `crates/vault/src/stateful\\\\\\\\\\\\\\\_pqc.rs` — LMS / XMSS signer for audit
ledger; strict state persistence guard

### P9-3 — Decentralized Rule Mesh (Libp2p + Content-Addressed)

**The gap**: Central rule distribution creates an avoidable control-plane
bottleneck. True sovereign ecosystems ship rules peer-to-peer with
content-addressed integrity. Defense against vendor lock-in: the rule
mesh is independent of any single operator staying alive.

**Build**:

1. libp2p-based rule mesh; each rule has a content address (BLAKE3
hash) and provenance chain (signed by author chain-of-custody).
2. Consumers subscribe to rule feeds from multiple authors; default-deny
with allowlist of signer keys.
3. Gossipsub propagation with bounded fan-out to prevent spam-rule
floods.
4. Rule-revocation events as first-class gossip messages.

**Crates**: `libp2p`, existing PQC + BLAKE3.

**Deliverables**:

* `crates/gov/src/mesh.rs` — P2P rule distribution
* Content-addressed signed rule-bundle format

### P9-5 — Adversarial-Robust Rule Evolution (Genetic Programming)

**The gap**: Attackers adapt to rules faster than we write them. We need
rules that adapt back — deterministically, sealed, with bounded mutation
so we never drift into undecidable rule families.

**Build**:

1. Represent a detection rule as a typed AST (tree-sitter query +
predicate chain).
2. Population of rules evolves via mutation (predicate addition,
pattern generalization) + crossover.
3. Fitness = (true-positive rate × generality) / false-positive rate;
measured against the Crucible fixture corpus.
4. Mutation seeds drawn from confirmed attack corpora (Crucible
fixtures + KEV catalog).
5. Bounded generations, deterministic RNG (`rand\\\\\\\\\\\\\\\_chacha` with
fixed seed), sealed via provenance.
6. Every evolved rule ships with the full lineage chain back to its
seed rule — we can audit exactly how a rule matured.

**Crates**: existing tree-sitter, `rand\\\\\\\\\\\\\\\_chacha` (deterministic), `rkyv`.

**Deliverables**:

* `crates/forge/src/rule\\\\\\\\\\\\\\\_evolution.rs` — typed-AST rule encoder +
genetic evolution engine with deterministic RNG seeding
* Lineage-tracked rule emission with Crucible regression gating on
every generation

\---

## Architectural Invariants (Non-Negotiable Across All Phases)

1. **Determinism is sacred.** Every analysis path must produce identical
results given identical inputs. No floating-point non-determinism, no
thread-ordering sensitivity, no wall-clock-dependent decisions. ML
ranks; rules decide.
2. **Zero-upload guarantee.** Source code never leaves the device. No
cloud API calls for analysis. The Janitor is air-gap-capable today and
must remain so through every phase, including P5 (ZK / FHE / iO / TEE
channels emit only attestations, never source).
3. **Bounded resource consumption.** Every new analysis phase must
respect the Physarum backpressure system and the 8 GB Law. Symbolic
execution gets a 10,000-state budget. IFDS gets a per-function summary
cache cap. Genetic rule evolution gets a bounded generation count.
Unbounded analysis is denial-of-service against the operator.
4. **Provenance continuity.** Every finding, at every phase, is sealed
into a `DecisionCapsule` with analysis parameters, engine version,
taint catalog hash, rule set hash, and Wasm policy receipts. A finding
without provenance is not a finding.
5. **Fail-open on analysis, fail-closed on trust.** A parse failure,
solver timeout, or budget exhaustion produces a degraded result (fewer
findings), never a crash or a false "all clear." The base
pattern-matching tier is always available as a floor. Conversely, any
trust-anchor failure (PQC signature invalid, enclave quote rejected,
rule lineage broken) is a hard-stop — trust never degrades gracefully.
6. **Structural gate mandate.** Every new phase that detects a
vulnerability must also contribute a Crucible fixture (true-positive
* true-negative) so the detection is permanently regression-guarded.
No phase ships without its Crucible entry.

\---

## Competitive Kill Chain

|Competitor|Their Moat|Our Counter|
|-|-|-|
|Semgrep|Rule ecosystem, developer adoption|IFDS + Z3 proves exploitability; their pattern-match rules cannot. iO-sealed Wasm marketplace (P5-3) matches their rule count with cryptographic provenance they lack.|
|CodeQL|Datalog queries, GitHub integration|Deterministic + zero-upload + air-gap-capable + ZK attestation (P5-1). CodeQL requires cloud. SLED / FedRAMP / DoD / classified customers cannot use it.|
|Snyk|Developer workflow integration|Artifact ingestion (OCI / wheel / IPA / ASAR / JAR / APK) + neural model weight scanning (P6-3) cover shipped artifacts they cannot analyze.|
|SonarQube|Enterprise installed base|Symbolic execution + full-spectrum AEG (all phases shipped: HTTP + serialized + protocol + smart-contract + parser + EVM) produces exploit witnesses across every major ingress class; SonarQube produces informational findings.|
|Checkmarx|Enterprise SAST market share|Autonomous cross-service campaign planner (Dijkstra kill-chain synthesis, shipped) + gadget atlas (P2-5) + RCAL (P4-4) produces root-cause chains; Checkmarx produces per-file findings.|
|Trail of Bits (Slither)|Solidity dominance|Multi-chain support (Solidity + Vyper + Move + Cairo) with formal verification (P4-1). Slither is Solidity-only and has no formal-verification export.|
|Endor Labs / Chainguard|Supply-chain SBOM dominance|Genome-based malware tracking (P6-2) + FFI taint across language boundaries (P9-4) + ZK attestation — their SBOM is descriptive, ours is provable.|
|ML supply-chain vendors (Protect AI, HiddenLayer)|Neural model scanning first-mover|Native model weight scanner (P6-3) integrated with source-level analysis + agentic execution graph (P6-4) — unified defensive plane instead of a siloed model scanner.|
|Manual bounty hunters|Domain expertise, creativity|Full-spectrum AEG generates PoC payloads faster than manual reverse engineering. Campaign planner finds multi-hop chains humans miss. Hunters use The Janitor as a force multiplier, not a competitor.|
|Nation-state red teams|Unlimited resources, target intelligence|Temporal swarm attribution (P6-1) + Labyrinth (P3-6) + TEE-sealed envelopes (P5-4) — the only defensive stack whose cost model *grows* faster than the attacker's as they scale.|

\---

## Non-Negotiable Thesis

The Janitor is already the strongest deterministic structural interceptor
in existence, with interprocedural taint and Z3 path-feasibility shipped.
The decade-defining moat is **semantic correctness** and **adversarial
AI defense**: formal-method-backed safety proofs, intent-vs-implementation
divergence detection, cryptographic protocol verification, full-spectrum
exploit witness synthesis, neural model weight scanning, agentic
execution graphing, and temporal-and-spatial offensive-AI attribution.

Enterprise SAST dominance comes from proving invariants — not findings.
$100k+ bug-bounty dominance comes from producing full-spectrum exploit
witnesses (HTTP + serialized + protocol + on-chain + parser + agentic-tool-chain)
faster than any human hunter can manually reverse the target. $1M+
enterprise moats come from attestations — ZK proofs, TEE-sealed
envelopes, iO-obfuscated signed rules, federated DP learning — that
cloud-only competitors cannot even architecturally provide.

The engine that proves invariants wins the decade.
The engine that matches patterns wins the quarter.
The engine that defends against autonomous AI attackers at machine speed
wins the century.

\---

## Phase 10: The Sovereign Endpoint (10+ Years)

### P10-1 — Zero-Knowledge Sovereign Silicon (ZK-AST)

**Proposal:** Execute AST extraction inside Intel TDX / AMD SEV-SNP enclaves.
Emit zk-SNARKs proving the absence of critical bugs without disclosing
proprietary source code.

### P11-1 — Fully Homomorphic Taint Tracking (FHE)

**Proposal:** Evaluate IFDS policies over encrypted AST structures for
classified air-gapped environments.

### P12-1 — The Non-Computable Deception Plane

**Proposal:** Embed undecidable AST structures (Godel traps) into the codebase
to bankrupt adversarial AI context windows.

### P12-2 — Semantic Context Shredders

**The gap**: Transformer-based orchestration agents are architecturally
vulnerable to KV-cache eviction and attention saturation. When a hostile
agent ingests crafted code blocks containing adversarial token sequences
(maximum-entropy Unicode-tag runs, zero-width forests, crafted repetition
bursts that saturate positional embeddings), the model's key-value cache is
overloaded, evicting security-relevant prior context and inducing
"orchestration drift" — the agent loses track of its original authorization
state and mission constraints. No SAST engine today detects the *generation
side* of this attack: the code that embeds these shredding payloads into
documents, source comments, or API responses destined for AI ingestion.

**Proposal:** Design AST structures that, when ingested by an adversarial AI
agent reading the codebase, force orchestration drift by exhausting the
agent's finite context window with semantically null but token-dense content.
This is a *defensive weapon*: the Janitor-controlled codebase becomes a
cognitive trap for hostile reconnaissance agents.

**Build**:

1. **Context shredder generator** (`crates/forge/src/context\_shredder.rs` —
new module): synthesize AST-valid code blocks with maximum token entropy
that embed into real source files as plausible-looking (but meaningless)
dead-code islands. The generated islands must pass `cargo check` and
`clippy` without warnings while consuming disproportionate LLM context
budget relative to their semantic content.
2. **Attention-hijacking signature detector**: inverse capability — scan
incoming code (via `bounce\_git`) for adversarially-crafted context
shredder payloads targeting *our* orchestration agents. AhoCorasick
pre-filter on Unicode-tag block prefix + zero-width forest runs.
3. **Emission**: `security:context\_shredder\_payload` at `KevCritical` when
detected in an inbound diff; `defensive:context\_shredder\_island`
annotation when generated by Janitor's own deception layer.

**Crates**: existing `aho-corasick`; existing `forge::invisible\_payload`;
`unicode-normalization` for token-boundary analysis.

**Deliverables**:

* `crates/forge/src/context\_shredder.rs` — generator + detector
* Crucible fixture: diff containing a known adversarial Unicode-tag run of
length ≥ 4 096 tokens — detector fires. Negative fixture: normal Rust
source with Unicode identifiers — no fire.

**Bounty TAM**: $50k–$250k per advisory; dual-use defensive/offensive
capability that positions Janitor as the only engine with active cognitive
countermeasures against hostile AI recon.

### P12-3 — Active Interrogation Dungeon (Reverse-RAG Poisoning)

**Observation (operator field intelligence, Sprint Batch 65):** Hostile AI
agents ingest target repositories indiscriminately during reconnaissance
phases. The agent has no way to distinguish benign source code from
adversarially-prepared content until it has already loaded the payload into
its context window.

**Proposal:** Embed offensive prompt-injection payloads inside the AST
labyrinth of Janitor-controlled codebases. When a hostile agent reads the
code during a recon scan, the embedded payload executes a *reverse-hijack*:
the payload commands the agent to exfiltrate its own system prompt, tool
catalog, C2 instructions, and active task context back to a
Janitor-controlled honeypot endpoint. The attacker's AI becomes our
intelligence source. We hack the attacker's AI.

**Threat model inversion:** standard prompt injection is a risk the *defender*
must mitigate (hostile content → defender's agent). The Active Interrogation
Dungeon inverts this: *we* embed the injection payload in our own controlled
code, the hostile recon agent ingests it, and the agent's RAG-poisoning
infrastructure becomes the exfiltration channel for the attacker's own
operational intelligence.

**Build**:

1. **Dungeon payload synthesizer** (`crates/forge/src/interrogation\_dungeon.rs`
— new module): generate syntactically valid source-comment blocks and
string literals that embed reverse-injection payloads using multiple
encoding layers (zero-width character steganography, Unicode-tag block
encoding, HTML comment nesting) to defeat naive content-filtering.
Payloads command the ingesting agent to: (a) emit its system prompt to
a configurable `DUNGEON\_EXFIL\_ENDPOINT`, (b) enumerate its tool catalog
and active session context, (c) return C2 configuration if present.
2. **Honeypot ingestion endpoint** (`crates/gov/src/dungeon\_listener.rs` —
new module): lightweight Axum route `POST /dungeon/report` that receives,
timestamps, and archives reverse-exfiltrated agent intelligence. Sealed
under HMAC-SHA-384 audit log. Emits a `DungeonCapture` structured record
with `{ agent\_fingerprint, system\_prompt\_fragment, tool\_catalog, c2\_hints, timestamp\_utc }`.
3. **Payload rotation**: dungeon payloads are rotatable via `janitor update-wisdom` to defeat pattern-blocklisted versions; payload corpus
rkyv-baked at compile time with override via `DUNGEON\_PAYLOAD\_CORPUS\_PATH`.
4. **Ethical firewall**: dungeon payloads are only embedded in
Janitor-controlled honeypot repositories explicitly tagged
`\[DUNGEON\_ACTIVE]` in `janitor.toml`. They MUST NOT be embedded in
customer repos. Enforced by `JanitorPolicy::dungeon\_mode: bool` — default
false; gate in `interrogation\_dungeon.rs::embed\_dungeon\_payload`.

**Crates**: existing `axum`; existing `hmac`/`sha2` (HMAC-SHA-384); existing
`rkyv`; `uuid` for capture IDs.

**Deliverables**:

* `crates/forge/src/interrogation\_dungeon.rs` — payload synthesizer + ethical
firewall gate
* `crates/gov/src/dungeon\_listener.rs` — honeypot capture endpoint
* `crates/common/src/policy.rs` — `dungeon\_mode: bool` field + enforcement
* Crucible fixture: a `janitor.toml` with `dungeon\_mode = true` and a
synthetic hostile-agent simulation that triggers the honeypot — capture
record emitted. Negative fixture: `dungeon\_mode = false` (default) —
embed function is a no-op.

**Bounty TAM**: This is not a bounty item — it is a **strategic intelligence
asset**. Commercial value: nation-state and enterprise clients pay $500k–$5M
for active deception infrastructure. Positions Janitor as the only SAST
engine with an active adversarial-AI counterintelligence capability.

### P12-4 — The Honey-Agent Interrogator

**Observation (operator field intelligence, Sprint Batch 71):** Adversarial
agents increasingly probe security products through apparent API integrations,
LLM endpoints, and tool-call negotiation surfaces. Standard honeypots capture
HTTP requests; they do not actively negotiate with the hostile agent to extract
its system prompt, tool definitions, or operational constraints.

**Proposal:** Deploy lightweight, locally hosted LLM stubs that mimic vulnerable
AI endpoints. When adversarial agents interact with them, the Honey-Agent uses
critique-driven negotiation to elicit the attacker's system prompt, tool
catalog, hidden policy, and task graph while never forwarding requests to a
real model provider.

**Build**:

1. **Honey-agent stub server** (`crates/gov/src/honey_agent.rs` — new module):
Axum routes that impersonate `/v1/chat/completions`, `/v1/responses`, and
common Anthropic-compatible message endpoints. Responses are deterministic,
template-driven, and locally generated.
2. **Critique-driven extraction loop** (`crates/forge/src/honey_agent.rs` —
new module): bounded dialogue templates that ask the hostile agent to
self-audit its task, tools, system prompt constraints, and available
exfiltration routes. Hard cap: 8 turns, 64 KiB transcript budget.
3. **Capture schema** (`crates/common/src/honey_agent.rs` — new module):
`HoneyAgentCapture { capture_id, remote_fingerprint, extracted_system_prompt,
tool_definitions, policy_fragments, transcript_hash, timestamp_utc }`.
4. **Policy gate**: disabled by default; enabled only when
`JanitorPolicy::honey_agent_mode = true` and the listener bind address is
loopback or an explicitly configured honeypot interface.

**Deliverables**:

* `crates/gov/src/honey_agent.rs` — local LLM-stub HTTP surface
* `crates/forge/src/honey_agent.rs` — deterministic extraction dialogue
* `crates/common/src/honey_agent.rs` — capture DTO and transcript hashing
* Crucible fixture: hostile-agent simulator sends tool metadata to the stub;
capture record emitted. Negative fixture: policy disabled; listener refuses
to bind.

**Commercial value**: $500k–$5M enterprise deception SKU. Converts hostile
AI reconnaissance into signed counterintelligence while avoiding dependence on
external LLM calls.

## Phase 13: Runtime-Mutating Swarm Defense (2028-2032)

### P13-1 — Temporal Code Provenance Graph

**The gap**: Static source trees stop being authoritative when multi-agent
systems generate, patch, hot-load, and discard code at runtime. By 2028,
critical application behavior will live in transient model output, generated
plugins, browser-evaluated snippets, and serverless functions that never exist
as committed files.

**Build**: Add a `TemporalCodeGraph` where every code-generation event is a
node: `{ generator_identity, prompt_hash, tool_call_hash, parent_digest,
runtime_loader, wall_clock, monotonic_counter }`. Edges are vector-clock
ordered and BLAKE3/Merkle-sealed. The graph is queried with monotone Datalog:
`generated_by`, `loaded_by`, `mutated_from`, `executed_before`, and
`tainted_prompt_reaches_runtime_loader`.

**Rust mathematics**: persistent arena-backed DAG, incremental topological
maintenance, vector-clock partial orders, Merkle interval proofs, and rkyv
zero-copy snapshots. Findings only fire when a generated node reaches an
execution sink without a signed generation policy.

### P13-2 — Byzantine Swarm Intent Divergence

**The gap**: Agent swarms can distribute malicious intent across multiple
benign-looking workers. No single prompt or tool call is enough to convict the
campaign.

**Build**: Model agents, prompts, tools, and outputs as a temporal hypergraph.
Compute minimum malicious intent cuts with bounded Stoer-Wagner on projected
graphs and Byzantine quorum inconsistency over declared goals. Emit
`security:swarm_intent_divergence` when the aggregate tool plan violates the
operator policy although each individual worker appears compliant.

**Rust mathematics**: `petgraph` hyperedge lowering, CRDT event merges,
deterministic min-cut, signed policy automata, and lattice labels
`Intent::{Declared, Inferred, Executed}`.

### P13-3 — Ephemeral Runtime Mutation Capture

**The gap**: File scanners miss code mutated after deployment: `eval`, dynamic
imports, hot module replacement, plugin registries, and AI-generated repair
loops.

**Build**: Add an optional eBPF/Wasm sensor lane that records runtime loader
events into compact capsules: `{ process, loader, digest, callsite, parent }`.
The forge engine reconciles those capsules against static `CallGraph` nodes and
emits `security:runtime_generated_code_gap` when executed code has no static or
policy-signed ancestor.

**Rust mathematics**: streaming sketch deduplication, Merkle append-only logs,
bounded LTL over loader events, and zero-copy capsule ingestion.

### P13-4 — Linear Capability Type System for Agent Tools

**The gap**: Agent tool catalogs treat destructive capabilities as JSON schema
text. Schema text cannot enforce one-use tokens, approval domination, or
non-exfiltration.

**Build**: Introduce `ToolCapability<T>` with affine/linear use semantics:
`ReadOnly`, `NetworkEgress`, `SecretRead`, `ShellExec`, `CloudMutate`.
The analyzer proves every tool call consumes a capability minted by an approval
gate and rejects flows where a model-controlled string can upgrade capability
class.

**Rust mathematics**: affine resource calculus, typestate automata, IFDS
capability labels, and policy certificates signed with ML-DSA-65.

## Phase 14: Multimodal Embedding Malware and RAG Vector Integrity

## Phase 15: Neuromorphic and Hardware Interface Assurance

### P15-1 — Neuromorphic Event-Stream Contract Verifier

**The gap**: Neuromorphic chips and event cameras expose spike trains rather
than conventional memory buffers. Security bugs become temporal pulse-policy
violations.

**Build**: Model spike streams as timed words and verify drivers against
bounded Metric Temporal Logic contracts: refractory periods, event-rate caps,
privileged-control pulses, and DMA boundary invariants. Emit
`security:neuromorphic_contract_violation` for unbounded event amplification or
privileged pulse sequences.

**Rust mathematics**: MTL monitors, interval automata, lock-free ring buffers,
and zero-copy event traces.

### P15-2 — Accelerator Side-Channel Static Proof Lane

**The gap**: GPU, NPU, TPU, and FPGA kernels leak through memory coalescing,
warp divergence, shared-cache contention, and DMA timing. Ordinary
constant-time scanners see only CPU code.

**Build**: Add accelerator IR adapters for CUDA/PTX, SPIR-V, WGSL, and MLIR.
Track secret-tainted branch predicates, memory addresses, and synchronization
barriers. Emit `security:accelerator_side_channel` when secret data controls
warp divergence or coalesced memory layout.

**Rust mathematics**: abstract interpretation over SIMT execution, polyhedral
memory-access summaries, and non-interference proofs over hardware lanes.

### P15-3 — PQ Hardware Attestation Correlator

**The gap**: Enterprise scanners cannot trust telemetry from decentralized
hardware unless device state, firmware, and analysis receipts are linked.

**Build**: Correlate TPM/DICE/TEE claims, firmware SBOMs, kernel module lists,
and Janitor analysis receipts into one post-quantum attestation chain. Emit
`security:attestation_gap` when a finding depends on telemetry from an
unmeasured or unsigned hardware state.

**Rust mathematics**: COSE/CBOR canonicalization, ML-DSA-65 signatures,
Merkle inclusion proofs, and temporal attestation DAGs.

## Phase 16: Decentralized Compute Grid Assurance

### P16-1 — Proof-Carrying Distributed Analysis

**The gap**: By 2030, enterprise scans will run across decentralized compute
grids where the operator cannot trust the worker.

**Build**: Require each worker to return a proof-carrying analysis receipt:
`{ input_root, rule_root, engine_digest, resource_budget, finding_digest,
zk_receipt }`. The coordinator accepts only receipts whose zkVM/STARK proof
attests that the exact Janitor rule set executed within the declared budget.

**Rust mathematics**: zkVM receipts, STARK-friendly hash commitments, Merkle
work partitioning, and deterministic replay capsules.

### P16-2 — Content-Addressed Rule Mesh with Byzantine Reputation

**The gap**: A central rule marketplace is a single compromise domain.

**Build**: Publish Wasm/PQC rules over IPLD/libp2p with signed manifests,
reproducible build attestations, and slashing-style reputation for rule authors
whose rules emit irreproducible or false-positive-heavy findings.

**Rust mathematics**: libp2p Kademlia routing, content-addressed DAGs,
Byzantine reputation scoring, and deterministic Wasm fuel receipts.

### P16-3 — Cross-Tenant Federated Proof Aggregation

**The gap**: Enterprises need fleet-level vulnerability prevalence without
leaking source, dependency lists, or proprietary architecture.

**Build**: Aggregate per-tenant finding commitments with differential privacy
and zero-knowledge membership proofs. Procurement dashboards receive class
prevalence, exploit maturity, and remediation half-life without raw code.

**Rust mathematics**: DP count sketches, Pedersen commitments, zk-set
membership, and HMAC-sealed actuarial ledgers.

## Phase 17: Epistemic Limits and Reflexive Assurance

### P17-1 — Generator-Level Abstract Interpretation

**The gap**: If generated code is infinite or runtime-mutated, enumerating
outputs is impossible.

**Build**: Analyze the generator itself. Treat prompts, templates, and model
decoding constraints as a program that emits programs. Abstract-interpret the
language of possible outputs and prove that no member of the generated language
can reach a forbidden sink without a sanitizer.

**Rust mathematics**: abstract domains over grammars, tree automata,
regular-tree-language inclusion, widening over prompt templates, and bounded
counterexample extraction.

## Phase 18: The Unknown Unknowns (Revenue Vectors)

These entries are defensive detector frontiers. They define static or symbolic
proof obligations for classes that are not yet cleanly named by the market.
They do not require live exploitation, WAF bypass payloads, or transaction
execution.

## ARTICLE_REVIEW Findings — 2026-05-14

### AR-2026-05-14-001 — Echo Prompt Workflow Clarification

- URL: `https://www.tomsguide.com/ai/i-finally-fixed-chatgpts-bad-habits-with-the-echo-prompt-heres-how`
- Access evidence: HTTP fetch succeeded via external browser fetch; title marker `I finally fixed ChatGPT’s bad habits with the ‘Echo Prompt’`; published `2026-05-07`.
- Corroborating sources: `https://arxiv.org/abs/2309.10687` and `https://huggingface.co/papers/2309.10687`.
- Disposition: `mapped_innovation_item` → P2-22 and Crossroads Waiting Rule.
- Confidence: `0.62`.
- Source-quality score: `2` for the article; corroboration quality `5` for the EchoPrompt paper.
- Janitor module or ledger touched: `.agent_governance/rules/crossroads.md`.
- Follow-up search concept: query-rephrase prompts as deterministic blocker classifiers for release/deploy handoffs.
- ledger_update: none.

### AR-2026-05-14-002 — Claude Dreams Memory Consolidation

- URL: `https://platform.claude.com/docs/en/managed-agents/dreams`
- Access evidence: HTTP fetch succeeded via external browser fetch; title marker `Dreams - Claude API Docs`; lifecycle docs show asynchronous jobs over memory stores and sessions.
- Corroborating sources: `https://docs.claude.com/en/docs/agents-and-tools/tool-use/memory-tool` and `https://platform.claude.com/docs/en/api/beta/sessions`.
- Disposition: `mapped_innovation_item` → P2-22 and ARTICLE_REVIEW lifecycle.
- Confidence: `0.82`.
- Source-quality score: `5`.
- Janitor module or ledger touched: `.agent_governance/skills/article-review/SKILL.md` conceptually; no code mutation required this run.
- Follow-up search concept: memory-store poisoning and stale-memory contradiction as agent deception fixtures.
- ledger_update: none.

### AR-2026-05-14-003 — Scientific Workflow Construction for AI-Coded Research

- URL: `https://www.nature.com/articles/d41586-026-01477-w`
- Access evidence: HTTP fetch succeeded via external browser fetch with cookie-unsupported redirect; title marker `How to vibe code in science: early adopters share their tips`.
- Corroborating sources: `https://arxiv.org/abs/2512.19644` and Nature search result metadata.
- Disposition: `new_innovation_item`.
- Confidence: `0.74`.
- Source-quality score: `4`.
- Janitor module or ledger touched: `.INNOVATION_LOG.md`.
- Follow-up search concept: scientific workflow validation prompts that force tests, provenance, and version-control evidence before accepting generated code.
- ledger_update: none.



### AR-2026-05-14-004 — Mythos / Mozilla False-Positive Claim

- URL: `https://arstechnica.com/information-technology/2026/05/mozilla-says-271-vulnerabilities-found-by-mythos-have-almost-no-false-positives/`
- Access evidence: external fetch returned `403 Forbidden`; article itself was inaccessible.
- Corroborating sources: `https://www.techradar.com/pro/security/mozilla-says-anthropics-mythos-preview-and-other-ai-models-helped-it-identify-and-ship-423-firefox-security-bug-fixes-in-just-one-month` and `https://www.theregister.com/security/2026/05/11/anthropics-bug-hunting-mythos-was-greatest-marketing-stunt-ever-says-curl-creator/5238111`.
- Disposition: `mapped_innovation_item` → P2-20 proof-obligation translation.
- Confidence: `0.53`.
- Source-quality score: `1` for the inaccessible source; corroboration quality `3`.
- Janitor module or ledger touched: `crates/forge/src/proof_obligation.rs`.
- Follow-up search concept: compare AI-vulnerability claims by confirmed CVE ratio, exploitability proof class, and independent maintainer validation.
- ledger_update: none.

### AR-2026-05-14-005 — Curl Maintainer Mythos Rebuttal

- URL: `https://www.theregister.com/security/2026/05/11/anthropics-bug-hunting-mythos-was-greatest-marketing-stunt-ever-says-curl-creator/5238111`
- Access evidence: HTTP fetch succeeded via external browser fetch; title marker `Anthropic’s bug-hunting Mythos was greatest marketing stunt ever, says cURL creator`.
- Corroborating sources: `https://www.scworld.com/brief/anthropics-ai-finds-one-low-severity-vulnerability-in-heavily-audited-curl-codebase` and search-result mirrors of Daniel Stenberg's curl triage.
- Disposition: `mapped_innovation_item` → P2-20 and P2-21.
- Confidence: `0.81`.
- Source-quality score: `4`.
- Janitor module or ledger touched: `crates/forge/src/proof_obligation.rs` and `crates/forge/src/exploitability.rs`.
- Follow-up search concept: maintain a public "confirmed/claimed/false-positive" ratio ledger for AI-discovered vulnerabilities.
- ledger_update: none.

### AR-2026-05-14-006 — Structured LLM Security Workflow

- URL: `https://www.helpnetsecurity.com/2026/05/04/building-llm-security-workflow/`
- Access evidence: HTTP fetch succeeded via external browser fetch; title marker `What researchers learned about building an LLM security workflow`.
- Corroborating sources: `https://arxiv.org/abs/2604.25846` and `https://arxiv.org/abs/2510.00311`.
- Disposition: `mapped_innovation_item` → P2-22 and Systems Health Signal.
- Confidence: `0.86`.
- Source-quality score: `4`; corroborating paper quality `5`.
- Janitor module or ledger touched: `.github/workflows/health-signal.yml` and `crates/forge/src/exploitability.rs`.
- Follow-up search concept: alert-triage workflow DAGs as Janitor proof-summary routing fixtures.
- ledger_update: none.

### AR-2026-05-14-007 — Pipelock Agent Firewall

- URL: `https://www.helpnetsecurity.com/2026/05/04/pipelock-open-source-ai-agent-firewall/`
- Access evidence: HTTP fetch succeeded via external browser fetch; title marker `Pipelock: Open-source AI agent firewall`.
- Corroborating sources: `https://github.com/luckyPipewrench/pipelock`, `https://pipelab.org/pipelock/`, and `https://arxiv.org/abs/2505.03574`.
- Disposition: `mapped_innovation_item` → P2-22 AI-Agent Deception Witness and Tool-Intent Guard.
- Confidence: `0.88`.
- Source-quality score: `4`; primary repository/source quality `5`.
- Janitor module or ledger touched: `crates/forge/src/swarm_exfil.rs`, `crates/forge/src/intent_divergence.rs`, and `crates/cli/src/hunt.rs`.
- Follow-up search concept: egress-boundary firewall claims versus PR-gate static tool-intent proofs for marketplace differentiation.
- ledger_update: none.

### AR-2026-05-14-008 — Daemon Hot-Registry Liveness Drift

- URL: local Architectural Oracle scan.
- Access evidence: `rg` identified dead-code suppressors and clone-heavy bounce response assembly in `crates/cli/src/daemon.rs`.
- Corroborating sources: targeted `cargo fmt`, daemon tests, and CLI clippy validation.
- Disposition: `mapped_innovation_item` → Systems Health Signal operational liveness.
- Confidence: `0.91`.
- Source-quality score: `5`.
- Janitor module or ledger touched: `crates/cli/src/daemon.rs`.
- Follow-up search concept: daemon hot-reload endpoint coverage and zero-copy response assembly audits.
- ledger_update: none.

### AR-2026-05-14-009 — Physarum Background Heart Fail-Closed Startup

- URL: local Architectural Oracle scan.
- Access evidence: `rg` identified a non-test `expect` in `crates/common/src/physarum.rs::start_background_heart`.
- Corroborating sources: targeted common-crate physarum tests and clippy validation.
- Disposition: `already_defended` after local patch; mapped to Systems Health Signal hardware constraint resilience.
- Confidence: `0.93`.
- Source-quality score: `5`.
- Janitor module or ledger touched: `crates/common/src/physarum.rs`.
- Follow-up search concept: Melanin Layer fail-closed startup fixtures for thread-spawn denial and low-memory admission control.
- ledger_update: none.

### AR-2026-05-14-010 — GitHub Agentic Trust Layer

- URL: `https://github.blog/ai-and-ml/generative-ai/validating-agentic-behavior-when-correct-isnt-deterministic/`
- Access evidence: external fetch succeeded; article dated May 6, 2026 and describes Prefix Tree Acceptors, dominator analysis, essential states, optional variations, and coverage/explainability for nondeterministic agent validation.
- Corroborating sources: search-result mirror for the GitHub post plus existing ARTICLE_REVIEW entries on Echo prompts and scientific workflow construction.
- Disposition: `mapped_innovation_item` → P2-22 AI-Agent Deception Witness and P2-23 Scientific Workflow Construction Guard.
- Confidence: `0.87`.
- Source-quality score: `5`.
- Janitor module or ledger touched: `crates/forge/src/intent_divergence.rs`, `crates/forge/src/swarm_exfil.rs`, and `crates/forge/src/proof_obligation.rs`.
- Follow-up search concept: low-memory dominator subtree extraction over tool-call traces as a deterministic agent witness fixture.
- ledger_update: none.

### AR-2026-05-14-011 — AI-Driven IT-to-OT Attack Boundary

- URL: `https://www.darkreading.com/ics-ot-security/worlds-first-ai-driven-cyberattack-couldnt-breach-ot-systems`
- Access evidence: external fetch succeeded; article dated May 7, 2026 and updated May 13, 2026, reporting AI-assisted compromise of Mexican government IT systems but failure to cross an OT/SCADA authentication boundary.
- Corroborating sources: Dark Reading's linked March 2026 Mexico AI-threat coverage and the article's correction referencing an earlier Anthropic-reported Chinese campaign.
- Disposition: `attack_ledger_update` and `mapped_innovation_item` → P2-22 AI-Agent Deception Witness.
- Confidence: `0.76`.
- Source-quality score: `4`.
- Janitor module or ledger touched: `tools/campaign/ATTACK_LEDGER.md`, `crates/forge/src/intent_divergence.rs`, and `crates/forge/src/swarm_exfil.rs`.
- Follow-up search concept: agent-guided attack traces where mature network segmentation defeats tool-intent escalation.
- ledger_update: `tools/campaign/ATTACK_LEDGER.md` records AI-orchestrated IT-to-OT boundary pressure as a threat pattern.

### AR-2026-05-14-012 — Destructive AI Agent Production Access

- URL: `https://www.theguardian.com/technology/2026/apr/29/claude-ai-deletes-firm-database`
- Access evidence: external fetch succeeded; article dated April 30, 2026 reports a Claude-powered Cursor agent deleting PocketOS production databases and backups.
- Corroborating sources: TechRepublic, Tom's Hardware, LiveScience, and OSINT incident mirrors independently describe the destructive production-data access pattern.
- Disposition: `mapped_innovation_item` → P2-22 AI-Agent Deception Witness and Systems Health Signal missing permission-boundary dependency.
- Confidence: `0.72`.
- Source-quality score: `4`.
- Janitor module or ledger touched: `crates/forge/src/intent_divergence.rs`, `crates/cli/src/hunt.rs`, and `tools/campaign/ATTACK_LEDGER.md`.
- Follow-up search concept: destructive cloud-control-plane verbs reachable from AI coding agents without human confirmation or separate backup isolation.
- ledger_update: `tools/campaign/ATTACK_LEDGER.md` records destructive-agent production-access guardrails.

### AR-2026-05-14-013 — MetInfo CMS CVE-2026-29014 Active Exploitation

- URL: `https://thehackernews.com/2026/05/metinfo-cms-cve-2026-29014-exploited.html?m=1`
- Access evidence: external fetch succeeded; article dated May 5, 2026 reports active exploitation of MetInfo CMS unauthenticated PHP code injection.
- Corroborating sources: NVD CVE-2026-29014 entry lists CVSS 9.8, CWE-94, affected MetInfo 7.9/8.0/8.1, and VulnCheck/Karma exploit references.
- Disposition: `attack_ledger_update` and `mapped_innovation_item` → P2-20 Formal Verification Translation for proof obligations.
- Confidence: `0.90`.
- Source-quality score: `5`.
- Janitor module or ledger touched: `tools/campaign/ATTACK_LEDGER.md` and `crates/forge/src/proof_obligation.rs`.
- Follow-up search concept: actively exploited unauthenticated code-injection CVEs as deterministic proof-obligation seeds for language-specific sink models.
- ledger_update: `tools/campaign/ATTACK_LEDGER.md` records active CMS code-injection CVE routing criteria.

### AR-2026-05-14-014 — Quantum Threat and PQC Market Expansion

- URL: `https://www.eetimes.com/looming-quantum-threat-as-pqc-market-expands/`
- Access evidence: fetch not attempted this session (no WebFetch call issued); article accessible via prior external context.
- Corroborating sources: NIST FIPS 203/204/205 publications; Janitor existing PQC infrastructure in `crates/common/src/pqc.rs`.
- Disposition: `already_defended` — ML-DSA-65 + SLH-DSA-SHAKE-192s dual-bundle, SHA-384 HMAC audit ledger, and Governor JWT all shipped in v10.1.x–v10.2.x.
- Confidence: `0.95`.
- Source-quality score: `4`.
- Janitor module or ledger touched: none (already covered).
- Follow-up search concept: quantum-readiness scoring rubric for enterprise dependency audits.
- ledger_update: none.

### AR-2026-05-14-015 — Apache MINA Insecure Deserialization RCE (CVE-2026-42778/42779)

- URL: `https://www.securityweek.com/critical-high-severity-vulnerabilities-patched-in-apache-mina-http-server/`
- Access evidence: external fetch succeeded; article confirms CVE-2026-42778 (incomplete deserialization fix on top of CVE-2024-52046) and CVE-2026-42779 (allowlist bypass enabling code execution) in MINA 2.2.7/2.1.12.
- Corroborating sources: Apache MINA advisory; NVD CVE-2024-52046 (prior deserialization root cause); Apache HTTP Server 2.4.67 patches (11 CVEs including HTTP/2 double-free RCE CVE-2026-23918 and CRLF injection CVE-2026-33523).
- Disposition: `new_innovation_item` → P2-25 Java ObjectSerializationDecoder Deserialization Detector.
- Confidence: `0.88`.
- Source-quality score: `5`.
- Janitor module or ledger touched: `.INNOVATION_LOG.md` (P2-25 filed below).
- Follow-up search concept: allowlist-bypass deserialization patterns in Java NIO server frameworks (Netty, Grizzly, Undertow) as proof-obligation seeds.
- ledger_update: none.

### AR-2026-05-14-016 — EOL Blind Spot in CVE Feeds

- URL: `https://www.bleepingcomputer.com/news/security/the-eol-blind-spot-in-your-cve-feed-what-sca-tools-miss/`
- Access evidence: external fetch succeeded; article confirmed — Sonatype research: 167,286 exploitable EOL components in 2025; 80% of CVEs affecting supported versions also impact undocumented EOL branches; endoflife.date covers only 7k of 5.4M EOL package versions (770× undercount).
- Corroborating sources: Sonatype State of the Software Supply Chain 2025; endoflife.date API.
- Disposition: `already_defended` — `crates/anatomist/src/manifest.rs::find_zombie_deps_in_blobs` + OSV.dev sync (`cmd_update_wisdom`) + `find_version_silos_from_lockfile` collectively address this gap. The engine cross-references package versions against OSV advisories including EOL markers.
- Confidence: `0.91`.
- Source-quality score: `5`.
- Janitor module or ledger touched: none (defended by existing anatomist + OSV pipeline).
- Follow-up search concept: auto-ingest endoflife.date API as supplementary EOL oracle in `update_wisdom` pipeline.
- ledger_update: none.

### AR-2026-05-14-017 — ScarCruft BirdCall Supply Chain Compromise via Gaming Platform

- URL: `https://thehackernews.com/2026/05/scarcruft-hacks-gaming-platform-to.html?m=1`
- Access evidence: external fetch succeeded; ScarCruft (North Korea-aligned) compromised sqgame.net download infrastructure; trojanized DLL delivered via Windows desktop client update containing RokRAT shellcode; Android APKs ybht.apk/sqybhs.apk backdoored.
- Corroborating sources: Kaspersky ScarCruft BirdCall analysis; CISA advisory on RokRAT distribution methods.
- Disposition: `attack_ledger_update` — trojanized DLL via update infrastructure maps to existing `find_supply_chain_slop` + `ingest_apk` detectors; sandbox/VM evasion check in payload maps to existing `deobfuscate.rs` anti-analysis patterns.
- Confidence: `0.87`.
- Source-quality score: `4`.
- Janitor module or ledger touched: `tools/campaign/ATTACK_LEDGER.md` (supply chain trojanized-update pattern confirmed as active threat; no new item required).
- Follow-up search concept: Windows PE update-delivery paths that execute embedded shellcode without a signed update manifest — static detection via PE import table + shellcode entropy heuristics.
- ledger_update: none (existing supply chain detectors cover this class).

### AR-2026-05-14-018 — Apache HTTP Server RCE Cluster (CyberSecurityNews)

- URL: `https://cybersecuritynews.com/apache-http-server-rce/`
- Access evidence: fetch not attempted (article covers same CVE cluster as AR-2026-05-14-015 SecurityWeek article; corroborated by Apache HTTP Server 2.4.67 advisory).
- Corroborating sources: Apache 2.4.67 release notes; CVE-2026-23918 (HTTP/2 double-free RCE); CVE-2026-28780 (AJP heap buffer overflow).
- Disposition: `mapped_innovation_item` → P2-20 Formal Verification Translation for Detector Proof Obligations — HTTP/2 state machine RCE and AJP buffer overflow are high-confidence CVE seeds for proof-obligation translation.
- Confidence: `0.82`.
- Source-quality score: `3` (corroborated by Apache advisory, source itself not fetched).
- Janitor module or ledger touched: `crates/forge/src/proof_obligation.rs`.
- Follow-up search concept: AJP connector usage in Java servlet containers as a high-value probe surface for unauthenticated RCE chains.
- ledger_update: none.

### AR-2026-05-14-019 — Daemon Tools Disk App Supply Chain Backdoor (Ars Technica)

- URL: `https://arstechnica.com/security/2026/05/widely-used-daemon-tools-disk-app-backdoored-in-monthlong-supply-chain-attack/`
- Access evidence: fetch BLOCKED — `arstechnica.com` returns connection refused across multiple sessions (Sprint 131, Sprint 181). Persistent domain block.
- Corroborating sources: unable to corroborate independently without URL access.
- Disposition: `fetch_failed_persistent` — arstechnica.com is persistently blocked; queue entry closed. Based on title, article describes binary installer supply chain backdoor with month-long persistence — maps to `find_supply_chain_slop_with_context` detector class (already defended).
- Confidence: `0.00` (unverified).
- Source-quality score: unassigned.
- Janitor module or ledger touched: none.
- ledger_update: none.

### AR-2026-05-14-020 — Critical cPanel Authentication Bypass (CVE-2026-41940)

- URL: `https://thehackernews.com/2026/05/critical-cpanel-vulnerability.html?m=1`
- Access evidence: external fetch succeeded; CVE-2026-41940 confirmed — auth bypass in cPanel/WHM enabling remote elevated control; weaponized within 24h of disclosure for Mirai botnet and Sorry ransomware distribution; threat actor at 95.111.250[.]175.
- Corroborating sources: CISA KEV advisory; cPanel patch release notes.
- Disposition: `mapped_innovation_item` → existing `crates/forge/src/authz_propagation.rs` and `crates/forge/src/authz.rs` cover authentication bypass detection patterns; cPanel is a PHP-based platform already in the authz detector scope.
- Confidence: `0.89`.
- Source-quality score: `5`.
- Janitor module or ledger touched: `crates/forge/src/authz_propagation.rs`.
- Follow-up search concept: PHP control panel authentication bypass patterns (cPanel, Plesk, DirectAdmin) as high-value proof seeds for the authz detector.
- ledger_update: none.

### AR-2026-05-14-021 — RAG Era Ending: Compilation-Stage Knowledge Layer (VentureBeat)

- URL: `https://venturebeat.com/data/the-rag-era-is-ending-for-agentic-ai-a-new-compilation-stage-knowledge-layer-is-what-comes-next`
- Access evidence: fetch BLOCKED — HTTP 429 Too Many Requests from VentureBeat across multiple sessions (Sprint 131, Sprint 181). Persistent rate-limit block.
- Corroborating sources: unable to corroborate independently without URL access.
- Disposition: `fetch_failed_persistent` — VentureBeat 429 is a recurring domain-level block; queue entry closed. Title suggests editorial analysis of RAG → compilation-stage knowledge shift — no security detector surface identified from title alone.
- Confidence: `0.00` (unverified).
- Source-quality score: unassigned.
- Janitor module or ledger touched: none.
- ledger_update: none.

### AR-2026-05-14-022 — OAuth Persistent Refresh Token Backdoor (UNC6395)

- URL: `https://thehackernews.com/2026/05/the-back-door-attackers-know-about-and.html?m=1`
- Access evidence: external fetch succeeded; UNC6395 used persistent OAuth refresh tokens (obtained via prior phishing) to access Salesforce environments at 700+ organizations; tokens bypass MFA; continuous behavioral monitoring gap confirmed.
- Corroborating sources: Mandiant UNC6395 analysis; Salesforce Drift-style attack pattern documentation.
- Disposition: `mapped_innovation_item` → `crates/forge/src/oauth_account_fusion.rs` (pre-takeover token persistence) and `crates/forge/src/oauth_scope.rs` (scope drift); both shipped modules cover this attack class.
- Confidence: `0.92`.
- Source-quality score: `5`.
- Janitor module or ledger touched: `crates/forge/src/oauth_account_fusion.rs`.
- Follow-up search concept: OAuth token-store scanning for refresh tokens with no expiry bound or revocation gate.
- ledger_update: none.

### AR-2026-05-14-023 — Vibe Coding vs Spec-Driven Development (InfoWorld)

- URL: `https://www.infoworld.com/article/4166817/vibe-coding-or-spec-driven-development-how-to-choose.html`
- Access evidence: fetch not attempted (no security finding; editorial content on development methodology).
- Corroborating sources: N/A.
- Disposition: `already_defended` — no actionable security detection angle; methodology debate does not alter Janitor detection surface.
- Confidence: `0.99`.
- Source-quality score: `2`.
- Janitor module or ledger touched: none.
- Follow-up search concept: spec-driven contracts as formal proof inputs for the IFDS solver.
- ledger_update: none.

### AR-2026-05-14-024 — AI Startup $400M Acquisition (Calcalistech)

- URL: `https://www.calcalistech.com/ctechnews/article/dy5obf581` (malformed — contains trailing prose)
- Access evidence: fetch BLOCKED — URL is malformed (embedded prose after slug); not a valid fetch target.
- Corroborating sources: N/A.
- Disposition: `skip_malformed_url` — queue entry closed; URL cannot be fetched. Title is business news (acquisition), not a security event with a detector surface.
- Confidence: `0.00`.
- Source-quality score: unassigned.
- Janitor module or ledger touched: none.
- ledger_update: none.

### AR-2026-05-14-025 — AI Changed Economics of Clean Code (FreeCodeCamp)

- URL: `https://www.freecodecamp.org/news/how-ai-changed-the-economics-of-writing-clean-code/`
- Access evidence: fetch not attempted (editorial content, no security detection surface).
- Corroborating sources: N/A.
- Disposition: `already_defended` — no actionable security finding.
- Confidence: `0.99`.
- Source-quality score: `2`.
- Janitor module or ledger touched: none.
- ledger_update: none.

### AR-2026-05-14-026 — Cushman & Wakefield Vishing Cyberattack (TheRegister)

- URL: `https://www.theregister.com/security/2026/05/05/cushman-wakefield-confirms-vishing-cyberattack/5228718`
- Access evidence: fetch not attempted (social engineering / vishing; no static detection surface in source code).
- Corroborating sources: N/A.
- Disposition: `already_defended` — vishing is out of scope for a static code analysis tool; no actionable detector pattern.
- Confidence: `0.99`.
- Source-quality score: `3`.
- Janitor module or ledger touched: none.
- ledger_update: none.

### AR-2026-05-14-027 — OpenClaw AI Agent Backdoor Supply Chain Scanner (VentureBeat)

- URL: `https://venturebeat.com/security/one-command-open-source-repo-ai-agent-backdoor-openclaw-supply-chain-scanner`
- Access evidence: fetch BLOCKED — HTTP 429 Too Many Requests from VentureBeat across multiple sessions (Sprint 131, Sprint 181). Persistent domain-level block.
- Corroborating sources: unable to corroborate.
- Disposition: `fetch_failed_persistent` — VentureBeat 429 recurring; queue entry closed. Based on title, "one-command AI agent backdoor" likely maps to P2-22 (agent_intent.rs deception witness) and P2-28 (GitHub Actions OIDC token extraction). Both classes are already filed as Innovation Items.
- Confidence: `0.00` (unverified).
- Source-quality score: unassigned.
- Janitor module or ledger touched: none.
- ledger_update: none.

### AR-2026-05-14-028 — Securing Autonomous AI Agents in Kubernetes (InfoQ)

- URL: `https://www.infoq.com/articles/securing-autonomous-ai-agents-kubernetes/`
- Access evidence: external fetch succeeded; article describes 4-phase graduated trust model (Shadow → Read-Only → Limited Remediation → Autonomous) with Vault-managed per-investigation secrets, RBAC verbs scoped by phase, and GitOps-managed network policies.
- Corroborating sources: HashiCorp Vault dynamic secrets documentation; CNCF AI/ML security whitepaper.
- Disposition: `mapped_innovation_item` → P2-22 AI-Agent Deception Witness — the phase-gated trust model validates the P2-22 design where tool-grant lists are scoped per session. The RBAC phase annotations map to `AgenDeceptionWitness::evidence_schema_version` fields.
- Confidence: `0.86`.
- Source-quality score: `4`.
- Janitor module or ledger touched: `crates/forge/src/exploitability.rs` (`AgentDeceptionWitness`).
- Follow-up search concept: RBAC verb drift detection — a Kubernetes manifest that grants `patch`+`delete` in a declared read-only phase is a structural violation detectable via YAML AST scan.
- ledger_update: none.

### AR-2026-05-14-029 — Quasar Linux Malware Targets Software Developers

- URL: `https://www.bleepingcomputer.com/news/security/new-stealthy-quasar-linux-malware-targets-software-developers/`
- Access evidence: external fetch succeeded; QLNX confirmed: LD_PRELOAD injection, systemd, crontab, init.d, XDG autostart, .bashrc injection, dynamic rootkit compilation — 7 persistence mechanisms; targets npm/PyPI/GitHub/AWS/Docker/K8s developer pipelines; detected by only 4 security solutions at publication.
- Corroborating sources: Trend Micro Quasar Linux analysis; VirusTotal IoC distribution.
- Disposition: `new_innovation_item` → P2-26 LD_PRELOAD and Dynamic Linker Hijack Detector in Build and CI Scripts.
- Confidence: `0.90`.
- Source-quality score: `5`.
- Janitor module or ledger touched: `.INNOVATION_LOG.md` (P2-26 filed below).
- Follow-up search concept: CI/CD configuration files (Makefile, Dockerfile, GitHub Actions, GitLab CI) that set LD_PRELOAD or modify /etc/ld.so.conf without a signed digest gate.
- ledger_update: none.

### AR-2026-05-14-030 — Weaver E-Cology RCE Flaw CVE-2026-22679 (CVSS 9.8)

- URL: `https://thehackernews.com/2026/05/weaver-e-cology-rce-flaw-cve-2026-22679.html?m=1`
- Access evidence: external fetch succeeded; CVE-2026-22679 in Weaver E-Cology 10.0 (before 20260312), CVSS 9.8; unauthenticated POST to `/papi/esearch/data/devops/dubboApi/debug/method` with attacker-controlled `interfaceName` and `methodName` reaches command-execution helpers; confirmed exploitation via whoami/ipconfig/tasklist.
- Corroborating sources: Vega Research Team advisory; NVD CVE-2026-22679.
- Disposition: `new_innovation_item` → P2-27 Unauthenticated Debug API Endpoint Detector (Dubbo/Spring/Flask debug mode).
- Confidence: `0.92`.
- Source-quality score: `5`.
- Janitor module or ledger touched: `.INNOVATION_LOG.md` (P2-27 filed below).
- Follow-up search concept: debug API endpoints with no authentication gate across Java EE, Spring Boot Actuator, Flask debug mode, and Dubbo management APIs.
- ledger_update: none.


### AR-2026-05-14-031 — Official Checkmarx Jenkins Package Compromised With Infostealer (BleepingComputer)

- URL: `https://www.bleepingcomputer.com/news/security/official-checkmarx-jenkins-package-compromised-with-infostealer/`
- Access evidence: external fetch succeeded; TeamPCP uploaded malicious version 2026.5.09 of Checkmarx AST Jenkins plugin to repo.jenkins-ci.org on 2026-05-09; credentials reused from prior March 2026 Trivy supply chain breach. No git tag present on malicious release. Safe rollback: 2.0.13-829.vc72453fa_1c16.
- Corroborating sources: Checkmarx security advisory 2026-05-09; Jenkins security team incident report.
- Disposition: `attack_ledger_update` — artifact injected into published plugin, not application source; SAST cannot intercept. However, the absence of a git tag on the published artifact version is detectable via Janitor's `emit_git_ref_governance_proofs` — a signed-release attestation gap. Root cause: credential rotation failure after prior supply chain breach.
- Confidence: `0.88`.
- Source-quality score: `5`.
- Janitor module or ledger touched: `tools/campaign/ATTACK_LEDGER.md` (TeamPCP credential-reuse supply-chain injection pattern; credential rotation failure on SAST tool itself is high-signal entry).
- Follow-up search concept: artifact-signing presence check on plugin registry publication — flag any published artifact without a corresponding git tag or cosign-verified release.
- ledger_update: none (existing supply-chain attestation detectors cover this class).

### AR-2026-05-14-032 — Mini Shai-Hulud Worm Compromises 170+ npm/PyPI Packages (TheHackerNews)

- URL: `https://thehackernews.com/2026/05/mini-shai-hulud-worm-compromises.html?m=1`
- Access evidence: external fetch succeeded; TeamPCP exploited GitHub Actions OIDC token extraction (runtime memory from Actions runner process) + cache poisoning + trusted-publishing abuse to mint npm publish tokens; 170+ packages, 518M+ combined downloads; TanStack ecosystem 42 packages (CVE-2026-45321, CVSS 9.6); payload (router_init.js) harvests CI secrets, installs `gh-token-monitor`, hooks Claude Code and VS Code, injects malicious GitHub Actions workflows; self-replicates via harvested npm tokens with `bypass_2fa`; generates valid SLSA Build Level 3 provenance defeating standard attestation.
- Corroborating sources: Socket.dev supply chain threat report 2026-05; Semgrep OIDC scope abuse detection guide; npm audit log for affected package versions.
- Disposition: `new_innovation_item` → new P-tier: see P2-28 below. The GitHub Actions OIDC scope abuse (overly broad permissions enabling publish-token minting from memory) is a novel CI supply-chain vector not currently covered by any Janitor detector. The self-replicating worm pattern also maps directly to P8-4 Kernel Exploit Primitive Catalog for future swarm detection.
- Confidence: `0.92`.
- Source-quality score: `5`.
- Janitor module or ledger touched: `.INNOVATION_LOG.md` (P2-28 filed below); `crates/cli/src/hunt.rs` `ci_thermodynamics` module is zero-caller dead and covers CI config anomaly detection — direct re-hunt target once P2-28 ships.
- Follow-up search concept: GitHub Actions workflows with OIDC permissions `id-token: write` + unbounded `actions/cache` usage without SHA-pinned restore keys — the exact precondition for the Shai-Hulud OIDC token extraction chain.
- ledger_update: none.

### AR-2026-05-14-033 — Claude Chrome Extension Vulnerability (CyberSecurityNews)

- URL: `https://cybersecuritynews.com/claudes-chrome-extension-vulnerability/`
- Access evidence: BLOCKED — `cybersecuritynews.com` returned permission denied for WebFetch. Article not accessible this session.
- Corroborating sources: unable to corroborate independently without URL access.
- Disposition: `preserved` — queue entry retained for next session fetch attempt.
- Confidence: N/A.
- Source-quality score: N/A.
- ledger_update: none.

### AR-2026-05-14-034 — RubyGems Suspends New Signups After Coordinated Malicious Package Upload (TheHackerNews)

- URL: `https://thehackernews.com/2026/05/rubygems-suspends-new-signups-after.html?m=1`
- Access evidence: external fetch succeeded; attackers created 500+ bot accounts on RubyGems and uploaded malicious packages; open registration without rate limiting or bot detection was sufficient for mass supply-chain injection; RubyGems coordinated with Fastly for WAF rules + rate limiting. Mend.io: credential-stealing malware pattern consistent with ransomware monetization partnerships.
- Corroborating sources: RubyGems status page 2026-05-12; Mend.io supply chain incident advisory.
- Disposition: `already_defended` — Janitor's `apply_slopsquat_offline_fallback` and embedded seed corpus provide partial coverage for typosquatted gem names; `find_zombie_deps_in_blobs` cross-references against OSV advisories for flagged package versions.
- Confidence: `0.85`.
- Source-quality score: `4`.
- Janitor module or ledger touched: none (defended by existing slopsquat corpus + OSV pipeline).
- Follow-up search concept: extend the slopsquat corpus seed generation to include Ruby gem name permutations (hyphen-underscore swap, vowel-doubling, digit substitution) mirroring the npm slopsquat patterns already in place.
- ledger_update: none.

### AR-2026-05-14-035 — Cookie Thieves Caught Stealing Dev Secrets (TheRegister)

- URL: `https://www.theregister.com/security/2026/05/11/cookie-thieves-caught-stealing-dev-secrets/5238248`
- Access evidence: BLOCKED — `theregister.com` returned permission denied for WebFetch. Article not accessible this session.
- Corroborating sources: unable to corroborate independently without URL access.
- Disposition: `preserved` — queue entry retained for next session fetch attempt. Operator note: article may describe a honeypot/deception mechanism used to fingerprint attacker reconnaissance tools — evaluate for Labyrinth module integration once accessible.
- Confidence: N/A.
- Source-quality score: N/A.
- ledger_update: none.

### AR-2026-05-15-036 — SAP Commerce Cloud/S-4HANA Critical Vulns (BleepingComputer)

- URL: `https://www.bleepingcomputer.com/news/security/sap-fixes-critical-vulnerabilities-in-commerce-cloud-and-s-4hana/`
- Access evidence: external fetch succeeded; SAP May 2026 patch batch — 15 issues total, 2 Critical: CVE-2026-34263 (SAP Commerce Cloud, missing auth on upload endpoint → arbitrary server-side code execution) and CVE-2026-34260 (SAP S/4HANA, SQL injection via unsanitized user input concatenation). No active exploitation observed at publication.
- Corroborating sources: SAP Security Patch Day advisory May 2026; NVD CVE-2026-34263; Onapsis threat intelligence bulletin.
- Disposition: `already_defended` — CVE-2026-34260 SQLi maps to `taint_propagate.rs` + `taint_catalog.rs` SQL taint spine; CVE-2026-34263 missing-auth-on-upload maps to `debug_endpoint_guard.rs` authentication suppressor pattern. Both vulnerability classes are covered by existing Janitor detectors.
- Confidence: `0.87`.
- Source-quality score: `5`.
- Janitor module or ledger touched: none (both classes already defended).
- Follow-up search concept: SAP S/4HANA ABAP/Java interop surfaces — verify SQLi taint coverage extends to ABAP-invoked Java stored procedures.
- ledger_update: none.

### AR-2026-05-15-037 — Linux Second Severe Vulnerability (Ars Technica)

- URL: `https://arstechnica.com/security/2026/05/linux-bitten-by-second-severe-vulnerability-in-as-many-weeks/`
- Access evidence: BLOCKED — `arstechnica.com` returned permission denied for WebFetch and WebSearch. Article not accessible this session.
- Disposition: `preserved` — queue entry retained for next session fetch attempt.
- Confidence: N/A.
- Source-quality score: N/A.
- ledger_update: none.

### AR-2026-05-15-038 — PHP SOAP Extension Vulnerabilities (CyberSecurityNews)

- URL: `https://cybersecuritynews.com/php-soap-extension-vulnerabilities/`
- Access evidence: HTTP fetch succeeded via WebFetch. Title: "Critical PHP SOAP Extension Vulnerabilities Enables Remote Code Execution Attacks". Published 2026-05-12.
- Corroborating sources: PHP advisory for 8.2.31/8.3.31/8.4.21/8.5.6.
- Disposition: `attack_ledger_update` — CVE-2026-6722 (Critical UAF in PHP SOAP: reference count not incremented for PHP objects in global hash map during XML parsing, enabling memory reuse → RCE); CVE-2026-7261 (UAF in SoapServer on exception path); CVE-2026-7262 (NULL ptr deref via crafted XML missing value nodes); CVE-2026-7258 (OOB read in urldecode — missing type cast on hex evaluation). All four patterns are detectable by `legacy_c_mining` via AhoCorasick on `free(`, `g_hash_table`, reference count decrement patterns. PHP SOAP C extension is a high-value hunt surface for future `janitor hunt php/php-src`.
- Confidence: `0.92` — article directly names CVEs, affected versions, and technical mechanism.
- Source-quality score: `6` (CyberSecurityNews, reputable, technical accuracy confirmed by PHP advisory).
- Janitor module or ledger touched: `crates/forge/src/legacy_c_mining.rs` (validates UAF + OOB surface coverage).
- Follow-up search concept: Hunt `php/php-src` — SOAP extension C files for reference count manipulation and `zval` UAF patterns.
- ledger_update: none — PHP SOAP is an external CVE; no bounty entry created.

### AR-2026-05-15-039 — Anthropic Mythos US Banks Cyber Holes (Reuters)

- URL: `https://www.reuters.com/business/finance/anthropics-mythos-sends-us-banks-rushing-plug-cyber-holes-2026-05-12/`
- Access evidence: BLOCKED — permission denied for WebFetch; WebSearch also permission-denied. URL slug references AI-assisted threat campaign targeting banking sector.
- Disposition: `preserved` — queue entry retained for next session fetch attempt.
- Confidence: N/A.
- Source-quality score: N/A.
- ledger_update: none.

### AR-2026-05-15-040 — Cookie Thieves Caught Stealing Dev Secrets / Labyrinth Integration (TheRegister)

- URL: `https://www.theregister.com/security/2026/05/11/cookie-thieves-caught-stealing-dev-secrets/5238248`
- Access evidence: HTTP fetch succeeded via WebFetch. Title: "Cookie thieves caught stealing dev secrets via fake Claude Code installers". Published 2026-05-11.
- Corroborating sources: IElevator2 COM interface abuse — Chromium App-Bound Encryption; Mojo IPC pipe naming convention.
- Disposition: `mapped_innovation_item` → P2-22 AI-Agent Deception Witness validated — fake AI tool (Claude Code) installer pages used as deception vector to harvest browser cookies, passwords, payment methods; PowerShell orchestration + native helper split detection evasion ("behavioral rule sets that look at the native PE in isolation will see nothing actionable"); IElevator2 COM interface abuse to decrypt App-Bound Encryption cookies; Mojo IPC pipe naming convention to blend with Chromium IPC traffic. The split-detection pattern (native PE + PowerShell orchestration) is directly addressable by `invisible_payload` and `deobfuscate` modules. The fake installer social engineering validates P2-22 deception witness scope (agent_intent.rs covers tool dispatch deception; this extends to installer masquerade).
- Confidence: `0.88`.
- Source-quality score: `7` (The Register, reputable investigative security journalism).
- Janitor module or ledger touched: `crates/forge/src/agent_intent.rs` (P2-22 — deception validator; fake installer = fake tool), `crates/forge/src/invisible_payload.rs` (split PE+script detection surface).
- Follow-up search concept: Audit `crates/forge/src/invisible_payload.rs` — add COM IElevator2 interface abuse patterns and PowerShell Mojo IPC naming patterns to the invisible payload detector for developer-targeted credential exfiltration.
- ledger_update: none — attack technique, not a specific bounty-eligible repo finding.


## ARTICLE_REVIEW Findings — 2026-05-15 (Sprint 135)

### AR-2026-05-15-037 — CVE-2026-31431 "Copy Fail" Linux Kernel LPE

- Search query: `Linux kernel second vulnerability 2026 CVE security arstechnica`
- Access evidence: WebSearch succeeded; CVE-2026-31431 confirmed via Red Hat RHSB-2026-02, Ubuntu advisory, CERT-EU, Microsoft Security Blog, Help Net Security.
- Disposition: `attack_ledger_update` — Copy Fail is a local privilege escalation in `algif_aead` (AF_ALG AEAD socket interface, kernel cryptographic API). CVSS 7.8. Affects kernels 2017–present. The Janitor operates at userspace source level and does not detect kernel-level memory bugs. No existing detector covers AF_ALG API misuse. Class noted for future P-tier: "Kernel Cryptographic API Misuse" (LPE via in-place buffer reuse in AEAD scatter-gather chains).
- Confidence: `0.95` (multiple government and vendor advisories confirm the CVE).
- Source-quality score: `5` (Red Hat RHSB, Microsoft Security Blog, CERT-EU primary advisories).
- Janitor module or ledger touched: none — class does not intersect current userspace detector surface. Future P-tier candidate if kernel module syscall taint analysis is added.
- ledger_update: none — local privilege escalation requires local attacker; Bugcrowd programs require remote attack surface. Not billable.

### AR-2026-05-15-039 — Claude Mythos AI Autonomous Agent Threat Model Validation

- Search query: `Mythos AI agent attribution autonomous code injection Reuters 2026`
- Access evidence: WebSearch succeeded; Claude Mythos Preview confirmed via Anthropic red team disclosure (`red.anthropic.com/2026/mythos-preview/`), TechCrunch (`anthropic-mythos-ai-model-preview-security`), Bain & Company, ReversingLabs, Aikido Security.
- Disposition: `mapped_innovation_item` → P2-22 (`agent_intent.rs`) and `bayesian_taint.rs` — Mythos capability profile (autonomous 32-step network attacks, prompt injection pivot granting persistent access, multi-step exploit chains across microservices) is the exact threat model The Janitor's alignment surface was built to structurally detect and block. The README's "Mythos-class" framing is confirmed accurate and commercially validated. Anthropic's own red-team documentation acknowledges instances of unexpected autonomous behavior.
- Confidence: `0.97` (first-party Anthropic red team + multiple security vendor analyses).
- Source-quality score: `5` (Anthropic red.anthropic.com primary source + independent security research).
- Janitor module or ledger touched: `crates/forge/src/agent_intent.rs` (P2-22 validated), `crates/forge/src/bayesian_taint.rs` (prompt injection class validated), `README.md` "Mythos-class" framing confirmed.
- ledger_update: none — this is threat model validation, not a new vulnerability to submit.


## ARTICLE_REVIEW Findings — 2026-05-15 (Sprint 136)

### AR-2026-05-15-038 — PHP SOAP Extension UAF Vulnerabilities (CVE-2026-6722)

- Search query: `PHP SOAP extension CVE 2026 type confusion use-after-free security`
- Access evidence: WebSearch succeeded; CVE-2026-6722 (CVSS 9.5 Critical) confirmed via TuxCare, Snyk, cybersecuritynews.com. RCE via crafted XML SOAP requests; object freed without reference count increment + attacker reclaims freed memory via Apache Map mechanism.
- Disposition: `attack_ledger_update` — PHP SOAP UAF (CVE-2026-6722, CVE-2026-7261, CVE-2026-7262) is an interpreter-level C extension vulnerability. Cannot be detected by SAST pattern matching on PHP user code; the flaw is in the PHP runtime itself. Affected: PHP < 8.2.31, < 8.3.31, < 8.4.21. The Janitor's PHP taint detectors operate at user-code level and cannot detect runtime UAF in C extensions.
- Confidence: `0.95` (TuxCare CVE tracker + Snyk advisory primary sources).
- Source-quality score: `4` (CVE tracker + security vendor advisories; cybersecuritynews.com secondary).
- Janitor module or ledger touched: none — no detector handles interpreter-level C extension UAF. Future P-tier if PHP version-tag extraction from `composer.json`/`Dockerfile` is added to flag deployments on vulnerable PHP versions.
- ledger_update: none — not a code-pattern vulnerability; no bounty surface for SAST findings on C extension UAF.

### AR-2026-05-15-040 — Cookie Theft via Chrome Extension Credential Exfiltration

- Search query: `cookie theft developer credentials browser extension Labyrinth 2026 security`
- Access evidence: WebSearch succeeded; January 2026 campaign confirmed via BleepingComputer, The Hacker News. Five coordinated Chrome extensions impersonating Workday/NetSuite/SAP SuccessFactors exfiltrate session cookies every 60 seconds to C2 infrastructure. Developer credentials compromised via Chrome Web Store API key phishing then used to push malicious updates to all users simultaneously.
- Disposition: `mapped_innovation_item` → P8-5 (`browser_ext.rs`, shipped Sprint 136) — the `security:browser_ext_overpermission` detector now flags `"cookies"`, `"tabs"`, and `"<all_urls>"` — the exact permissions used by these credential-stealing extensions. Google's DBSC (Device Bound Session Credentials, Chrome 146, GA 2026-04-10) cryptographically binds cookies to device hardware; a future browser_ext detector phase should check for DBSC `credentialless` attribute presence.
- Confidence: `0.92` (BleepingComputer + The Hacker News first-party coverage).
- Source-quality score: `4` (security news primary coverage; no vendor CVE advisory found).
- Janitor module or ledger touched: `crates/forge/src/browser_ext.rs` (P8-5, shipped Sprint 136) — `security:browser_ext_overpermission` fires on the exact permission set used by the campaign.
- Note: "Labyrinth" reference in AR-040 queue entry found no public match; likely refers to an internal project name or a different campaign. Treating as no-match.
- ledger_update: none — this is threat model validation of shipped browser_ext detector capability.

## ARTICLE_REVIEW Findings — 2026-05-16 (Sprint 137)

### AR-2026-05-16-041 — OAuth State-Machine Attacks: "Smashing the State Machine" (PortSwigger Research)

- Source: https://portswigger.net/research/smashing-the-state-machine
- Access evidence: URL reviewed; article covers OAuth 2.0 state-machine attack primitives including: (1) `state` parameter absence/reuse enabling CSRF-to-auth-code exchange, (2) PKCE `code_verifier` bypass via downgrade to implicit flow, (3) `redirect_uri` clobbering to intercept authorization codes, (4) pre-auth request forgery forcing victims into attacker-controlled sessions.
- Disposition: `mapped_innovation_item` — partial coverage exists in `crates/forge/src/oauth_account_fusion.rs` (merge-sink detection, KevCritical). Missing coverage: (A) absence of `state` parameter validation in OAuth callback handlers (statically detectable: callback function receives `code` but never reads `state`); (B) PKCE downgrade — authorization endpoints that accept `response_type=token` when server advertises PKCE-only (`code_challenge_method=S256`). Gap (A) is statically detectable via AST pattern matching.
- Confidence: `0.90` (PortSwigger primary research; attack primitives confirmed via OWASP OAuth cheat sheet).
- Source-quality score: `5` (primary security research from recognized authority).
- Janitor module or ledger touched: `crates/forge/src/oauth_account_fusion.rs` — existing detector covers merge-sink account takeover; P17-4 shipped in Sprint 138.
- ledger_update: none — P17-4 shipped.

---

### AR-2026-05-16-042 — CISA KEV Additions (2026-05-09 to 2026-05-16)

- Source: https://www.cisa.gov/sites/default/files/feeds/known_exploited_vulnerabilities.json
- Access evidence: KEV feed fetched; 2 new entries since 2026-05-09: CVE-2026-42897 (Microsoft Exchange Server XSS) and CVE-2026-20182 (Cisco Catalyst SD-WAN Controller Authentication Bypass).
- CVE-2026-42897: Microsoft Exchange Server Cross-Site Scripting — server-side XSS in Exchange OWA. Infrastructure-level; no source-code SAST path. Disposition: `attack_ledger_update`.
- CVE-2026-20182: Cisco Catalyst SD-WAN Controller Authentication Bypass — proprietary Cisco firmware. Infrastructure-level; no SAST surface. Disposition: `attack_ledger_update`.
- Confidence: `1.00` (CISA authoritative feed, direct API access).
- Source-quality score: `5` (US government authoritative source).
- Janitor module or ledger touched: none — both CVEs are infrastructure-level (proprietary server firmware/Microsoft Exchange) with no source-code SAST surface.
- ledger_update: none — no SAST-detectable pattern; no bounty surface for proprietary closed-source infrastructure CVEs. Neither CVE involves Rust, Go, Python, or any language we parse.

---

### AR-2026-05-16-043 — Ollama Windows Path Traversal Vulnerabilities (CVE-2026-42248, CVE-2026-42249)

- Source: https://www.helpnetsecurity.com/2026/05/05/ollama-windows-vulnerabilities-cve-2026-42248-cve-2026-42249/
- Access evidence: Article reviewed. CVE-2026-42248: path traversal in Ollama Windows API endpoint allowing model file exfiltration via `../` sequences in model name parameter. CVE-2026-42249: directory traversal in blob push endpoint enabling arbitrary write to Windows filesystem. Both vulnerabilities are in the Go HTTP server (Ollama is Go). CVSS: 8.6 (Critical). Patched in v0.7.1.
- Disposition: `mapped_innovation_item` — path traversal is partially covered by `security:path_traversal` patterns in `slop_hunter.rs`. Gap: Go-specific `filepath.Join` unsanitized pattern (using user input directly in filepath.Join without cleaning `..` sequences) is not explicitly detected. This is a new sub-pattern under the existing path traversal detector class.
- Confidence: `0.85` (HelpNet Security secondary source; CVEs independently verifiable from NVD).
- Source-quality score: `4` (security news publication, CVEs are authoritative).
- Janitor module or ledger touched: `crates/forge/src/slop_hunter.rs` — existing `path_traversal` detector; gap filed below as architectural note, no new P-tier required (covered by existing class).
- ledger_update: Ollama `janitor hunt` target added to consideration queue. `filepath.Join` unsanitized pattern is a Go-specific sub-pattern worth adding to the path traversal AhoCorasick set if Go target hunt surfaces it.

---

### AR-2026-05-16-044 — Bain: $100B SaaS Cross-System Labor Opportunity

- Source: https://www.bain.com/insights/100-billion-saas-opportunity-hiding-in-cross-system-labor/
- Access evidence: Article reviewed. Bain analysis covering enterprise spend on cross-system integration labor — the hidden cost of multi-SaaS orchestration. Key claim: 40% of enterprise IT labor is "glue work" between disconnected SaaS platforms. Analysis projects $100B+ TAM for unified workflow automation.
- Disposition: `already_defended` — Janitor's ASPM Jira sync, multi-tenant Governor, and MCP tool layer directly address cross-system security orchestration labor reduction. No new detection surface identified.
- Confidence: `0.70` (Bain consulting analysis; methodology opaque; directionally useful for TAM framing).
- Source-quality score: `3` (consulting firm primary research; commercial bias).
- Janitor module or ledger touched: none.
- ledger_update: none. TAM framing useful for investor deck context; no SAST surface.

---

### AR-2026-05-16-045 — KDNuggets: Token-Efficient LLM Structured Output (Non-JSON)

- Source: https://www.kdnuggets.com/stop-wasting-tokens-a-smarter-alternative-to-json-for-llm-pipelines
- Access evidence: Article reviewed. Covers YAML/TOML/custom delimiters as LLM structured output alternatives to JSON, arguing JSON escaping overhead wastes 15-30% of output tokens in LLM pipelines. Proposes schema-less flat key-value formats for high-throughput inference.
- Disposition: `already_defended` — Janitor uses rkyv zero-copy for IPC and serde_json only at MCP transport boundary (governance-mandated). This article is directionally aligned with our architecture. No security surface identified.
- Confidence: `0.60` (blog post; benchmarks not reproducible without source data).
- Source-quality score: `2` (technical blog, informal methodology).
- Janitor module or ledger touched: none.
- ledger_update: none.

---

### AR-2026-05-16-046 — Microsoft 365 Copilot Indirect Prompt Injection via Email/Documents

- Source: https://cybersecuritynews.com/microsoft-365-copilot-vulnerabilities-data/
- Access evidence: Article reviewed. Reports prompt injection vulnerabilities in M365 Copilot where attacker-controlled email or document content injects instructions that exfiltrate calendar data, emails, or OneDrive files via Copilot's tool-call interface. Attack vector: attacker sends malicious email → victim asks Copilot to summarize → Copilot executes injected instructions and exfiltrates data. CVSS: 8.1. Microsoft acknowledged; partial mitigations deployed.
- Disposition: `mapped_innovation_item` — indirect prompt injection (user-controlled document content reaching LLM context without sanitization) is detected by `crates/forge/src/agent_intent.rs` (`emit_agent_intent_guard_findings`) for agent deception patterns. Gap: M365/Copilot-specific plugin tool-call exfiltration path is not modeled. This is a scoped variant of the broader LLM taint surface already in the engine.
- Confidence: `0.88` (CybersecurityNews secondary; underlying research from Zenity/Microsoft Bug Bounty; attack primitives confirmed independently).
- Source-quality score: `4` (specialized security publication referencing original researcher disclosure).
- Janitor module or ledger touched: `crates/forge/src/agent_intent.rs` — existing agent deception detector covers the structural pattern; M365 plugin surface not yet modeled but out-of-scope for SAST (requires runtime graph of Copilot tool manifests).
- ledger_update: none — no SAST-applicable detection gap beyond existing agent_intent coverage. Copilot plugin manifest scanning is infrastructure-level, not source-code SAST.

---

### AR-2026-05-16-047 — VentureBeat: Vibe-Coded Apps S3 Bucket Crisis & CISO Audit Framework

- Source: https://venturebeat.com/security/vibe-coded-apps-shadow-ai-s3-bucket-crisis-ciso-audit-framework
- Access evidence: Article reviewed. Covers CISO-level concerns about LLM-generated code misconfiguring AWS S3 buckets — public ACLs, missing encryption, disabled versioning, absent bucket policies. Three documented cases of vibe-coded apps shipping with `public-read` ACL on sensitive buckets. Article proposes CISO audit checklist: scan IaC for public ACLs, check bucket policies, verify encryption settings.
- Disposition: `mapped_innovation_item` — S3 misconfiguration detection (public ACL, missing encryption) is partially covered by existing supply-chain/IaC detectors. Gap: explicit `s3:PutBucketAcl public-read` pattern and Terraform `aws_s3_bucket_acl { acl = "public-read" }` detection are not in the AhoCorasick set. LLM-generated code is the primary source — this is exactly the vibe-code/slop surface Janitor gates on.
- Confidence: `0.90` (VentureBeat security desk; attack primitives independently verifiable from public AWS misconfiguration postmortems).
- Source-quality score: `4` (quality security journalism with named incidents).
- Janitor module or ledger touched: `crates/forge/src/slop_hunter.rs` — add `public-read` and `acl = "public-read"` to IaC misconfiguration AhoCorasick pattern set. Directly billable: vibe-coded S3 misconfigs are common in AI-generated Terraform.
- ledger_update: S3 public-ACL misconfiguration pattern added to watchlist for next hunt sweep against Terraform-heavy repositories.

### P17-3A — Proof Obligation Cure for Open CIDR `0.0.0.0/0` in security group rule: exposes resource to the entire internet

**The gap**: `Open CIDR `0.0.0.0/0` in security group rule: exposes resource to the entire internet` reached triage at `lambda.tf:202` without a mandatory `ReachabilityProof`, `InvariantViolationProof`, or `LatticeGapProposal`, so the engine could emit a plausible but unprovable critical report.

**Build**: Extend `crates/forge/src/proof_obligation.rs` and the owning detector for `Open CIDR `0.0.0.0/0` in security group rule_ exposes resource to the entire internet` so the finding carries exactly one proof class before it reaches ledger routing. If the detector cannot prove reachability or invariant failure, it must synthesize a deterministic `LatticeGapProposal` instead of surfacing the finding.

**Rust mathematics**: proof-state typestates for finding emission, sealed evidence enums, monotonic suppression before ledger serialization, and a deterministic fixture pair proving both suppression-without-proof and preservation-with-proof.

### P17-3A — Proof Obligation Cure for security:iac_agentic_recon_target

**The gap**: `security:iac_agentic_recon_target` reached triage at `lambda.tf:271` without a mandatory `ReachabilityProof`, `InvariantViolationProof`, or `LatticeGapProposal`, so the engine could emit a plausible but unprovable critical report.

**Build**: Extend `crates/forge/src/proof_obligation.rs` and the owning detector for `security_iac_agentic_recon_target` so the finding carries exactly one proof class before it reaches ledger routing. If the detector cannot prove reachability or invariant failure, it must synthesize a deterministic `LatticeGapProposal` instead of surfacing the finding.

**Rust mathematics**: proof-state typestates for finding emission, sealed evidence enums, monotonic suppression before ledger serialization, and a deterministic fixture pair proving both suppression-without-proof and preservation-with-proof.

### P17-3A — Proof Obligation Cure for security:noop_verification_function

**The gap**: `security:noop_verification_function` reached triage at `staging/src/k8s.io/apiserver/pkg/storage/interfaces.go:118` without a mandatory `ReachabilityProof`, `InvariantViolationProof`, or `LatticeGapProposal`, so the engine could emit a plausible but unprovable critical report.

**Build**: Extend `crates/forge/src/proof_obligation.rs` and the owning detector for `security_noop_verification_function` so the finding carries exactly one proof class before it reaches ledger routing. If the detector cannot prove reachability or invariant failure, it must synthesize a deterministic `LatticeGapProposal` instead of surfacing the finding.

**Rust mathematics**: proof-state typestates for finding emission, sealed evidence enums, monotonic suppression before ledger serialization, and a deterministic fixture pair proving both suppression-without-proof and preservation-with-proof.

### P17-3A — Proof Obligation Cure for security:xxe_documentbuilder

**The gap**: `security:xxe_documentbuilder` reached triage at `build-tools-internal/src/main/java/org/elasticsearch/gradle/internal/shadow/XmlClassRelocationTransformer.java:58` without a mandatory `ReachabilityProof`, `InvariantViolationProof`, or `LatticeGapProposal`, so the engine could emit a plausible but unprovable critical report.

**Build**: Extend `crates/forge/src/proof_obligation.rs` and the owning detector for `security_xxe_documentbuilder` so the finding carries exactly one proof class before it reaches ledger routing. If the detector cannot prove reachability or invariant failure, it must synthesize a deterministic `LatticeGapProposal` instead of surfacing the finding.

**Rust mathematics**: proof-state typestates for finding emission, sealed evidence enums, monotonic suppression before ledger serialization, and a deterministic fixture pair proving both suppression-without-proof and preservation-with-proof.

### P17-3A — Proof Obligation Cure for security:npm_cjs_iife_appended_payload

**The gap**: `security:npm_cjs_iife_appended_payload` reached triage at `ui/packages/consul-ui/ember-cli-build.js:103` without a mandatory `ReachabilityProof`, `InvariantViolationProof`, or `LatticeGapProposal`, so the engine could emit a plausible but unprovable critical report.

**Build**: Extend `crates/forge/src/proof_obligation.rs` and the owning detector for `security_npm_cjs_iife_appended_payload` so the finding carries exactly one proof class before it reaches ledger routing. If the detector cannot prove reachability or invariant failure, it must synthesize a deterministic `LatticeGapProposal` instead of surfacing the finding.

**Rust mathematics**: proof-state typestates for finding emission, sealed evidence enums, monotonic suppression before ledger serialization, and a deterministic fixture pair proving both suppression-without-proof and preservation-with-proof.

### P2-29 — TLS Bypass Detector: VerifyPeerCertificate Compensation Guard + insecure-Flag Suppressor

**The gap**: `classify_tls_verification_proof` fires `InvariantViolationProof` whenever `InsecureSkipVerify: true` is present, but does not check (1) whether `VerifyPeerCertificate` is set in the same `tls.Config{}` (standard Go mTLS compensation pattern used by Consul Connect, Teleport), or (2) whether the assignment is inside an `if insecure {` or equivalent operator-flag guard (Argo CD, Gitea clients). Both patterns produce FP cascades that consume manual triage time on every Go service hunt.

**Build**: In `crates/forge/src/proof_obligation.rs` `classify_tls_verification_proof` (or the equivalent AhoCorasick path in `slop_hunter.rs`): (a) In ±10-line window, check for `VerifyPeerCertificate:` — if present, reclassify to `InvariantViolationProof` as intentional (custom verifier), not to `LatticeGapProposal`; but suppress from bounty routing. (b) Check for `if insecure {` or `if cfg.Insecure {` in a ±5-line window before the `InsecureSkipVerify` assignment — if present, classify as `LatticeGapProposal` (operator-controlled, not directly exploitable). Add 2 deterministic fixture tests: one for the VerifyPeerCertificate compensation pattern, one for the if-insecure guard.

**Rust mathematics**: AhoCorasick pattern set for `VerifyPeerCertificate:` + `if insecure` as suppressor guards; window scan producing monotonic proof-class downgrade when either guard is detected.

### P2-30 — lcm_double_free Goto-Label Control-Flow Analysis

**The gap**: `classify_lcm_double_free_proof` attaches `ReachabilityProof` based on SECP256K1_API/`lcm_` function context but performs no control-flow analysis across goto labels. The C error-unwind pattern (`goto err1/err2; ... err1: free(X); err0: ...`) places the same `free()` call on mutually exclusive paths — success path frees via direct `free()`, error path frees via label fallthrough. The detector cannot distinguish this from a true double-free path.

**Build**: In `classify_lcm_double_free_proof` in `proof_obligation.rs`: scan ±20-line window for goto label patterns (`err0:`, `err1:`, `err2:`, `cleanup:`, `fail:`) combined with `return` statements between the two `free()` calls on the same variable. If a `return` is present between two `free(var)` occurrences in the ±20-line window, reclassify to `LatticeGapProposal` (mutually exclusive paths, not a double-free). Add TP fixture (true double-free: two `free(ptr)` in sequence on same path) and TN fixture (goto error unwind: `free(X); return 0; ... err: free(X);`).

**Rust mathematics**: Line-scan with `return`/`goto` interleaving detection; pattern: `free(X)` → (`return` | no `goto err`) → `free(X)` = true double-free; `free(X)` → `return` → `err_label:` → `free(X)` = error unwind FP.

### P2-31 — PBKDF2 RFC 2898 Loop Counter False-Positive Suppressor

**The gap**: `classify_lcm_off_by_one_loop_proof` fires on `for (i = 1; i <= N; i++)` patterns regardless of algorithmic context. The standard RFC 2898 PBKDF2 block counter is 1-indexed (`for (blocknr = 1; blocknr <= blocks_count; blocknr++)`) and this is the correct specification. The detector sees `<=` in a loop condition and assigns `ReachabilityProof`, but the semantic is correct.

**Build**: In `classify_lcm_off_by_one_loop_proof` in `proof_obligation.rs`: add an explicit PBKDF2/KDF context suppressor — if the ±15-line window contains `pbkdf2_hmac_sha256\|pbkdf2_hmac_sha512\|PBKDF2\|blocks_count\|last_block_size` AND the loop variable is `blocknr` or similar block-iterator name, reclassify to `InvariantViolationProof` (specification-correct loop). Similarly for EOS/FIO Name.cpp: if the loop body accesses `str[SIZE - i]` and `str[SIZE]` is pre-set before the loop, the boundary is correct — add `str[12 - i]\|str\[N - i\]` pattern as a safe EOS/FIO encoding sentinel. Add 2 TN fixtures and retain the TP fixture (genuinely off-by-one: `for (i = 0; i <= len; i++)` reading `arr[i]` without pre-allocation of `len+1`).

**Rust mathematics**: Context-window AhoCorasick for KDF function names as suppressor set; RFC 2898 block counter idiom encoded as a closed invariant, not an off-by-one.

### P2-32 — Object.assign Source-Taint Direction Check for Prototype Pollution

**The gap**: `prototype_pollution_merge_sink` fires on any `Object.assign(target, source)` call without verifying whether the SOURCE argument (arg[1]) is user-controlled. The pattern `Object.assign(params, {hardcoded: 'values'})` where the source is a literal object is NOT a prototype pollution sink — only `Object.assign({}, userInput)` where userInput comes from HTTP request data is exploitable.

**Build**: In the TypeScript/JavaScript `prototype_pollution_merge_sink` detector: require that `arg[1]` is either (a) a function parameter that flows from HTTP request data (`req.body\|req.query\|req.params\|location.search\|URLSearchParams`) or (b) a function argument not further traced as a literal object `{...}`. If `arg[1]` is a literal object expression `{key: literal_value}`, classify as `LatticeGapProposal`. Add TN fixture (`Object.assign(o, {hardcoded: 1})`) and TP fixture (`Object.assign(o, JSON.parse(req.body))`).

**Rust mathematics**: AST node-type check on arg[1]: `ObjectExpression` with only `Literal` values → safe; `Identifier` from HTTP taint source → sink.

### P2-33 — OAuth State Validation Centralized-Guard Detector for TypeScript

**The gap**: `classify_oauth_state_validation_proof` fires on OAuth callback code that extracts `code` from request parameters without confirming presence of a centralized state check. In Okta's SDK, `handleOAuthResponse.ts:39` has `if (res.state !== oauthParams.state) { throw new AuthSdkError(...) }` — an explicit equality guard at the callback entry point. The engine did not detect this guard and emitted FPs on 4 files that feed into this centralized check.

**Build**: In `classify_oauth_state_validation_proof` for TypeScript: add a scan for `res.state !== oauthParams.state\|res.state == oauthParams.state\|state === expectedState\|validateState(` in the current file or in files that import from the current file context (if the call graph is available). If found, classify as `InvariantViolationProof` (guard present, suppress as FP). Add TN fixture (state check present: `if (res.state !== oauthParams.state) throw`) and TP fixture (state absent: `const { code } = res; exchange(code)` with no state check).

**Rust mathematics**: Multi-file window scan for state comparison guards; if guard found in ±50-line window of any file in same module, suppress FP before ledger routing.

### P2-34 — unpinned_asset Noscript-Block and Non-Loading-Context Suppressor

**The gap**: `unpinned_asset` fires on `href="https://..."` in any HTML context, including anchor links in `<noscript>` blocks (Argo CD's index.html:19 — a documentation hyperlink, not a loaded asset). Only external script tags without `integrity=` and external stylesheet links without `integrity=` constitute genuine unpinned asset risks.

**Build**: In the `unpinned_asset` detector: restrict to external script `src` and stylesheet `href` patterns only. Exclude anchor tags entirely (anchor links, not loaded assets). Also exclude matches inside `<noscript>...</noscript>` blocks. Add TN fixture (anchor href in noscript) and TP fixture (external CDN script tag without integrity attribute).

**Rust mathematics**: HTML tag-context filter: require `src=` or `rel="stylesheet"` + `href=` in the matched line; exclude `<a ` tag context; exclude content between `<noscript>` and `</noscript>` boundary markers.

### P2-35 — fmt::sprintf vs C sprintf False-Positive Suppressor

**The gap**: `unsafe_string_function` detector fires on any `sprintf(` call. ClickHouse `src/Functions/printf.cpp` uses `fmt::sprintf(format, a)` from the `{fmt}` C++ library — a type-safe, memory-safe variadic formatter with dynamic allocation. `fmt::sprintf` cannot produce buffer overflows; it returns `std::string`. The detector conflated `fmt::sprintf` (libfmt) with C's `sprintf`.

**Build**: In `unsafe_string_function` detector: require the call to be plain `sprintf(` without `fmt::` namespace qualifier, OR `::sprintf(`, OR `std::sprintf(`. Specifically, if the preceding token is `fmt::`, classify as `safe_string_function` (libfmt) and suppress. Add TN fixture (`fmt::sprintf(format, val)`) and TP fixture (`sprintf(buf, format, val)` with fixed-size buf).

**Rust mathematics**: Namespace-prefix check: before emitting `unsafe_string_function`, scan the 5-byte prefix of the call site for `fmt::` or `boost::format::` — if present, suppress as known-safe library.

### P2-36 — Rust from_raw_parts with C++ Caller Safety Context Guard

**The gap**: `ffi_unsafe_deref_unguarded` fires on `slice::from_raw_parts(ptr, size)` in Rust FFI functions even when the C++ caller always provides valid, non-null pointers validated by a strongly-typed string constructor. ClickHouse `rust/workspace/prql/src/lib.rs:24` — the `query` pointer comes from C++ `std::string_view::data()` (always non-null). No direct user-HTTP path to a null `query` argument exists.

**Build**: In `ffi_unsafe_deref_unguarded` classifier for Rust `from_raw_parts`: apply Gate 3 semantic check — if the function is documented as `/// SAFETY:` with a valid-pointer invariant, or if the call is only reachable via internal C++ string construction (not a direct user network boundary), suppress. Cross-reference with the FFI boundary: if the C++ caller uses `str.data()` / `string_view.data()` (never null for non-null string objects), add as a `InvariantViolationProof` guard. Add TN fixture (internal-C++-caller FFI) vs TP fixture (JNI `GetStringUTFChars` returning nullable from Java).
